---
phase: 03-chain-link-ribbon-glow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/kratos-lab/chain.js
  - tools/kratos-lab/test/chain.test.js
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: false
requirements: [CHAIN-01]
tags: [webgl1, gow1, chain, ribbon, geometry, twist, arc-length, mvp, tdd]

must_haves:
  truths:
    - "Chain renders as individual links, one 32px cell per LINK_PITCH along the arc (~15-16 links over CHAIN_LEN 14), not a squashed flat strip"
    - "Adjacent links alternate a ~90 degree cross-axis (wide face-on / narrow edge-on) for the 3D segmented read"
    - "Link pass renders usual alpha blend with depth-write ON via Fx.applyMaterial(MAT_chainlink); hero/blade occlusion stays correct"
    - "The binary-alpha chainlink texture's gaps show through the links"
    - "Trail inner edge is biased toward the tip arc (lerp(hilt, tip, TRAIL_INNER_T=0.6)) — fair-game footage-fidelity overlap"
  artifacts:
    - path: "tools/kratos-lab/chain.js"
      provides: "Pure link-walker: arc-length placement at LINK_PITCH, alternating per-link twist frames, per-link U in [0,1], interleaved vertex emission"
      exports: ["buildRibbon", "LINK_PITCH", "LINKS_PER_TILE"]
      min_lines: 60
    - path: "tools/kratos-lab/test/chain.test.js"
      provides: "Geometry known-answer suite (pitch, perpendicular twist, per-link constant axis, u-invariant, square-texel, vertical-curve degenerate guard)"
      contains: "LINK_PITCH"
    - path: "tools/kratos-lab/app.js"
      provides: "drawFx PASS 1 driven by Chain.buildRibbon; KratosLab.chainInfo() hook; trail tip-arc bias"
      contains: "Chain.buildRibbon"
    - path: "tools/kratos-lab/index.html"
      provides: "chain.js script tag, all cache-busters at ?v=21"
      contains: "chain.js?v=21"
  key_links:
    - from: "tools/kratos-lab/app.js drawFx"
      to: "tools/kratos-lab/chain.js buildRibbon"
      via: "per-frame call with sampled sag curve + LINK_PITCH"
      pattern: "Chain\\.buildRibbon"
    - from: "tools/kratos-lab/app.js drawFx PASS 1"
      to: "Fx.applyMaterial(matDb.byName.MAT_chainlink)"
      via: "decoded MAT state (no hardcoded blend/depth)"
      pattern: "MAT_chainlink"
---

<objective>
Replace the current squashed flat-strip chain with a data-correct, segmented, twisted link ribbon. The current code tiles the whole 16-link texture per 0.9 model units (`reps = len/0.9`), squashing every link ~16x versus square texels. This plan builds a pure, Node-testable link-walker (`chain.js`) that places one 32px link cell per `LINK_PITCH` of arc length with an alternating ~90-degree per-link twist, then wires it into `drawFx` PASS 1 so the lab shows countable links with visible alpha gaps — the CHAIN-01 vertical slice. The fair-game trail tip-arc bias (footage-fidelity overlap) ships in the same drawFx rework.

Purpose: CHAIN-01 — "the chain renders as a ribbon textured with the real chainlink strip at correct link pitch (32px/link, 16 links/tile) with visible alpha gaps and 'usual' alpha blending, depth-write on." The link pass writing depth is what lets Phase-6 fire/glow occlude correctly against the chain.

Output: `tools/kratos-lab/chain.js` (pure walker) + `tools/kratos-lab/test/chain.test.js` (geometry known-answers) + `drawFx` PASS 1 consuming the walker + `KratosLab.chainInfo()` proof hook + trail tip-arc bias.

DEFERRED (carried, not dropped): ROADMAP success-criterion 1 also says "on-screen link count matches Phase-1 measurements." `reference/MEASUREMENTS.md` does not exist yet (Phase 1 paused mid-01-03). Interim validation this plan CAN check: 32px/link and 16 links/tile against the decoded texture (in 03-02's wad.test.js), the square-texel invariant (`RIBBON_WIDTH === LINK_PITCH`) in chain.test.js, and the `KratosLab.chainInfo()` world-scale sanity hook. Carry verbatim in-code: "DEFERRED to Phase-1 polish (01-04): calibrate LINK_PITCH against measured on-screen link counts; expected change: one constant in chain.js."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chain-link-ribbon-glow/03-RESEARCH.md
@.planning/phases/03-chain-link-ribbon-glow/03-PATTERNS.md
@.planning/todos/pending/trail-fidelity-from-footage.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase this session. -->
<!-- Use these directly; no codebase exploration needed for the layout/anchors. -->

FX vertex layout (app.js:585-586) — chain.js MUST emit this interleave, 6 floats/vert, stride 24:
  aP = pos xyz  (offset 0)
  aT = (u, v, alpha)  (offset 12)
  vertexAttribPointer(fxLocs.aP, 3, FLOAT, false, 24, 0)
  vertexAttribPointer(fxLocs.aT, 3, FLOAT, false, 24, 12)

Existing row->triangle emitter kept for the TRAIL only (app.js:528-537):
  pushRibbon(rows, out) — rows: [{a:[3], b:[3], u, alpha}]; emits triangles between CONSECUTIVE rows.
  NOTE: pushRibbon bridges every consecutive row pair, so it CANNOT be used for the chain — a bridge quad
  between two differently-twisted links would smear (03-RESEARCH Pitfall 4). chain.js emits its own
  per-link triangles (2 tris/link, no inter-link bridge).

Dual-environment export guard (identical in loop.js:50-51, fx.js:68-69, fxparse.js:171-172):
  if (typeof module !== "undefined" && module.exports) module.exports = Chain;

Current chain block being replaced (app.js:547-566): anchor `a` from world[chainJ*16+12..14];
  pommel `bpt = xformM(bladeSim[key].mat, blade.hilt)`; sag = sin(PI*t) * (0.4 + len*0.35*slack).
  Dead constants that die: hw=0.14 (556), segs=10 & reps=Math.round(len/0.9) (559), u: t*reps (564).
  Surviving: CHAIN_LEN=14 (app.js:448, slack reference only).

Current PASS 1 (app.js:590-603) keeps its shape (Fx.applyMaterial, fxLog.push, uMaterialColor/uLayerColor,
  uCutoff 0.35 INFERRED, bind chainTex) — only the vertex source changes from the inline block to
  Chain.buildRibbon output.

GL init insertion point (app.js:356-357): gl.clearColor(0,0,0,1); gl.enable(gl.DEPTH_TEST);
KratosLab hook shape to copy (app.js:1004-1013 fxState): plain object of primitives, doc comment stating
  what it proves. Any on-page debug text uses textContent, never innerHTML (02-REVIEW IN-06).
Trail row emission (app.js:568-575): rows.map a: e.hilt -> bias inner edge toward tip.
index.html script block (index.html:111-117): all tags currently ?v=20.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — chain.test.js geometry known-answer suite</name>
  <files>tools/kratos-lab/test/chain.test.js</files>
  <read_first>
    - tools/kratos-lab/test/loop.test.js (whole file — the pure-module known-answer scaffold to copy: node:assert only, braced scenario blocks with `// --- title ---` rule lines, message on every assert, final console.log summary, exit code is the signal)
    - .planning/phases/03-chain-link-ribbon-glow/03-RESEARCH.md ("Arc-length link walk" code example + Pitfalls 2/4/6 + Interim Validation Design)
    - .planning/phases/03-chain-link-ribbon-glow/03-PATTERNS.md (chain.test.js section — assertion list)
  </read_first>
  <behavior>
    Suite requires `../chain.js` (module does not exist yet — this is the RED failure) and asserts, on the interleaved verts / metadata returned by `Chain.buildRibbon(curvePts, Chain.LINK_PITCH)`:
    - API shape: Chain.LINKS_PER_TILE === 16; Chain.LINK_PITCH is a finite number > 0; buildRibbon is a function.
    - Square-texel invariant: emitted ribbon half-width === LINK_PITCH/2 (so RIBBON_WIDTH === LINK_PITCH).
    - Pitch spacing: for a straight curve of known length L, nLinks === ceil(L / LINK_PITCH); link k's start position sits k*LINK_PITCH along the arc (within 1e-6).
    - Per-link constant cross-axis: within a single link, the (b - a) direction is identical across the link's rows (dot ≈ 1).
    - Alternating ~90 twist: consecutive links' cross-axes are perpendicular (|dot| < 1e-6).
    - U invariant: every emitted u is in [0,1]; link k uses u0 = (k mod 16)/16; the fractional tail link truncates u proportionally (u never exceeds 1, never grows unbounded — Pitfall 6).
    - Vertical-curve degenerate guard (Pitfall 2): a straight-down curve (T parallel to UP) produces NO NaN in any vert and a stable cross-axis (no frame flip between links).
    - No inter-link bridge: vert count === nLinks * 6 * subrows-per-link (2 triangles per sub-quad); assert no quad connects link k's last row to link k+1's first row.
  </behavior>
  <action>Create `tools/kratos-lab/test/chain.test.js` following the loop.test.js scaffold verbatim (header comment naming CHAIN-01 + run command `node tools/kratos-lab/test/chain.test.js`; `const assert = require("node:assert"); const Chain = require("../chain.js");`). Write the assertions in the behavior block above as braced scenario blocks. Build straight-line and vertical-line curve-point arrays inline as known inputs. This task commits RED first: `require("../chain.js")` throws MODULE_NOT_FOUND until Task 2 — that is the intended failing state. Commit `test(03-01): add failing chain-walker geometry suite`.</action>
  <verify>
    <automated>node tools/kratos-lab/test/chain.test.js 2>&1 | grep -qi "cannot find module\|MODULE_NOT_FOUND" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/kratos-lab/test/chain.test.js` exists and `require`s `../chain.js`.
    - Running the suite fails with a module-not-found error (RED) before Task 2 lands.
    - Source contains assertion messages for: square-texel invariant, LINK_PITCH pitch spacing, perpendicular consecutive cross-axes, per-link constant axis, u ∈ [0,1], vertical-curve no-NaN.
    - `grep -c "assert" tools/kratos-lab/test/chain.test.js` returns ≥ 8.
  </acceptance_criteria>
  <done>The failing geometry suite exists, committed as a RED test, and fails only because chain.js is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN + wire — chain.js walker, drawFx PASS 1, chainInfo, trail bias</name>
  <files>tools/kratos-lab/chain.js, tools/kratos-lab/app.js, tools/kratos-lab/index.html</files>
  <read_first>
    - tools/kratos-lab/test/chain.test.js (the contract just written — implement to satisfy it)
    - tools/kratos-lab/loop.js (module scaffold: IIFE + named-constant header comments + dual-env export guard to copy)
    - tools/kratos-lab/app.js lines 280-360 (makeTex, fxProg layout, modelMat, GL init), 528-619 (pushRibbon, drawFx, PASS 1, restore), 985-1020 (KratosLab hooks)
    - .planning/phases/03-chain-link-ribbon-glow/03-RESEARCH.md (Patterns 1/2/3, "Arc-length link walk" + "Two-pass chain draw" code examples, Assumptions A4/A7/A9)
    - .planning/phases/03-chain-link-ribbon-glow/03-PATTERNS.md (chain.js, drawFx two-pass, chainInfo, trail-bias, index.html sections)
  </read_first>
  <action>
Build `tools/kratos-lab/chain.js` as a pure IIFE `const Chain = (() => { ... return { buildRibbon, LINK_PITCH, LINKS_PER_TILE }; })();` with the dual-env export guard verbatim. Header comment cites CHAIN-01, the VERIFIED decoded texture facts (32px/link, 16 links/tile, content rows 6-25), and labels `LINK_PITCH = 0.9` INFERRED (A4) with the square-texel derivation (`RIBBON_WIDTH === LINK_PITCH`, both spans 32px; ≈4.6 cm/link at ≈5.1 cm/unit) AND the deferred note verbatim: "DEFERRED to Phase-1 polish (01-04): calibrate LINK_PITCH against measured on-screen link counts; expected change: one constant in chain.js." Constants: `LINKS_PER_TILE = 16`, `LINK_PITCH = 0.9`, half-width `hw = LINK_PITCH / 2`, `SUBROWS = 2` (sub-rows per link so sag does not facet — a smoothness parameter, not a data value).

`buildRibbon(curvePts, linkPitch, opts)`: (1) accumulate cumulative arc length `s[]` over `curvePts`; `arcLen = s[last]`; `nLinks = Math.ceil(arcLen / linkPitch)`. (2) For link k: sample positions at `s0 = k*linkPitch` and `s1 = min((k+1)*linkPitch, arcLen)` via arc-length interpolation; tangent `T = normalize(p1 - p0)`; `S = cross(T, UP)`; if `len3(S) < 1e-4` fall back to previous frame's S else world X (Pitfall 2 hysteresis); `N = cross(T, S)`; cross-axis `C = (k % 2 === 0) ? S : N` (link 0 face-on per Open Q3). (3) Per-link U: `u0 = (k % LINKS_PER_TILE)/LINKS_PER_TILE`; `u1 = u0 + ((s1-s0)/linkPitch)/LINKS_PER_TILE` (fractional tail truncates U, keeps u ∈ [0,1] — Pitfall 6). (4) Emit the link's own triangles directly in the aP+aT stride-24 interleave (pos xyz, then u, v, alpha=1), 2 tris per sub-quad across SUBROWS, all sub-rows sharing C (NO bridge to link k+1 — Pitfall 4). Return `{ verts, nLinks, arcLen, linkPitch, ribbonWidth: linkPitch }`. No GL, no DOM, no clock reads.

In `app.js` `drawFx`: replace the inline chain block (current 547-566) — sample the existing sag curve (anchor `a` -> pommel `bpt`, existing `sag = sin(PI*t)*(0.4 + len*0.35*slack)` formula, ~64 samples) into a `curvePts` array, call `Chain.buildRibbon(curvePts, Chain.LINK_PITCH)`, store the result per side (stash on `bladeSim[key].chain` for chainInfo), and feed `.verts` into the PASS 1 upload. Delete dead constants hw=0.14 / segs / reps and the `u: t*reps` mapping. PASS 1 keeps its exact shape (`Fx.applyMaterial(matDb.byName.MAT_chainlink)`, `fxLog.push`, uMaterialColor/uLayerColor from the MAT, `uCutoff 0.35` INFERRED, bind `chainTex`) — only the vertex source changes.

Trail tip-arc bias (Pattern 7, fair-game overlap): in the trail row emission (app.js:568-575) change `a: e.hilt` to `a: lerp(e.hilt, e.tip, TRAIL_INNER_T)` with a new module-level `const TRAIL_INNER_T = 0.6;` labeled INFERRED (A9, footage analysis in trail-fidelity todo — NOT a decoded value). Do NOT touch u/v orientation, add color, or add particles. `TRAIL_AGE` (app.js:441) and the trail history push (app.js:953) stay untouched — bias at emission only.

Add `KratosLab.chainInfo()` (copy the fxState object-of-primitives shape, app.js:1004-1013): returns `{ l: {linkCount, arcLen, linkPitch, ribbonWidth}, r: {...} }` from the stashed per-side buildRibbon results — the checkpoint's world-scale proof surface. If any on-page debug text is added, use `textContent` only (IN-06). Add `<script src="chain.js?v=21"></script>` after loop.js in index.html and bump ALL script tags ?v=20 -> ?v=21 (lockstep — Pitfall 7).

Commit `feat(03-01): segmented twisted link ribbon walker + trail tip-arc bias`.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/chain.test.js && node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/loop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/chain.test.js` exits 0 (all geometry known-answers green — the RED suite is now GREEN).
    - `chain.js` exports `buildRibbon`, `LINK_PITCH`, `LINKS_PER_TILE`; contains the dual-env guard line `module.exports = Chain`.
    - `chain.js` contains the strings `INFERRED` and `DEFERRED to Phase-1 polish (01-04)` (labels present).
    - `grep -n "Chain.buildRibbon" tools/kratos-lab/app.js` returns a match in drawFx; `grep -c "len / 0.9" tools/kratos-lab/app.js` returns 0 (dead reps constant removed).
    - `grep -n "TRAIL_INNER_T" tools/kratos-lab/app.js` shows the constant defined AND used in the trail row emission; value literal is `0.6`.
    - `grep -n "chainInfo" tools/kratos-lab/app.js` shows the hook on window.KratosLab.
    - `grep -c "?v=21" tools/kratos-lab/index.html` returns 8 (7 existing scripts + chain.js); `grep -c "?v=20" tools/kratos-lab/index.html` returns 0.
    - All four Node suites exit 0.
  </acceptance_criteria>
  <done>chain.test.js is green; the lab's drawFx builds the chain from Chain.buildRibbon; dead flat-strip constants are gone; trail inner edge is tip-biased; chainInfo() and the ?v=21 lockstep bump are in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — segmented links read correct (CHAIN-01)</name>
  <files>none (verification only — browser session against the running lab)</files>
  <read_first>
    - .planning/phases/03-chain-link-ribbon-glow/03-RESEARCH.md (Interim Validation Design — the perceptual criteria + deferred footage note)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-03-SESSION-LOG.md (the 49-PNG partial library incl. the 12-frame mid-swing burst — informal comparison only)
  </read_first>
  <what-built>The flat squashed chain strip is replaced by an arc-length link walker: one 32px link cell per LINK_PITCH (0.9 units), alternating ~90-degree per-link twist, per-link U kept in [0,1], usual alpha blend + depth-write ON via MAT_chainlink. Trail inner edge biased toward the tip arc. KratosLab.chainInfo() exposes link counts.</what-built>
  <action>Run the automated pre-proof (verify block) first and paste the output, then present the how-to-verify steps to the human and wait. Manual-only because the acceptance is perceptual (segmented/twisted read, visible gaps, stable geometry in motion) and comparative (informal 12-frame mid-swing burst) — the automatable geometry is already pinned by chain.test.js and KratosLab.chainInfo().</action>
  <how-to-verify>
    Automated pre-proof (run first, paste output):
    1. `node tools/kratos-lab/test/chain.test.js && node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/loop.test.js` — all exit 0.
    2. In the lab browser console: `KratosLab.chainInfo()` — link counts ≈ 15-16 per side over CHAIN_LEN 14 (world-scale sanity; NOT the footage cross-check, which is DEFERRED).
    3. `KratosLab.fxState()` between frames — blendEnabled false, depthMask true (restore discipline intact).
    Perceptual (human):
    4. Load `tools/kratos-lab/index.html` via the preview server. Orbit the camera on an idle blade: the chain reads as SEGMENTED links with visible alpha gaps and alternating wide (face-on) / narrow (edge-on) links — not a smooth ribbon and not a squashed strip.
    5. Run a combo swing: links do not conveyor-belt / crawl along the chain at idle (Pitfall 3); no ribbon shimmer/roll when the chain hangs near-vertical (Pitfall 2).
    6. Trail now hugs the tip arc (outer part of the sweep), not the full hilt->tip sheet.
    7. Occlusion vs the hero mesh is correct; magenta-background test shows no tint change.
    8. Informal only: eyeball against the 12-frame mid-swing burst in the Phase-1 partial library — do NOT treat as pass/fail (chain-at-rest measurement stills are still uncaptured; link-count calibration is DEFERRED to 01-04).
  </how-to-verify>
  <verify>
    <automated>node tools/kratos-lab/test/chain.test.js && node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/loop.test.js && grep -q "chainInfo" tools/kratos-lab/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Steps 1-3 automated pre-proof pass (four suites exit 0; chainInfo link count in the 14-18 range; fxState restore clean).
    - Human confirms segmented/twisted read with gaps (step 4), stable geometry in motion (step 5), tip-biased trail (step 6), correct occlusion + magenta test (step 7).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the visual issue (e.g. "links smear / no edge-on links", "ribbon rolls at idle", "trail unchanged").</resume-signal>
  <done>User approves the segmented-link read, or files specific perceptual issues for revision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curve/anchor data -> chain.js | Per-frame world-space points (from decoded pose + blade sim) cross into the pure walker; degenerate/NaN inputs must not produce NaN geometry |
| decoded metadata -> page (chainInfo debug) | Data-derived numbers surfaced to the DOM; must not become an injection sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Denial of Service | chain.js buildRibbon fed a near-vertical (T∥UP) or zero-length curve | mitigate | Epsilon degenerate-frame guard (`len3(cross(T,UP)) < 1e-4` -> previous-frame S / world X) + hysteresis; unit-tested vertical-curve no-NaN case in chain.test.js |
| T-3-02 | Injection | KratosLab.chainInfo() debug values reaching the page | mitigate | Return numeric primitives only; any on-page display uses `textContent`, never `innerHTML` (02-REVIEW IN-06) |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | Zero packages this plan (CLAUDE.md bans runtime deps; node:assert-only tests) — no supply-chain surface |
</threat_model>

<verification>
- `node tools/kratos-lab/test/chain.test.js && node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/loop.test.js` — all exit 0.
- `KratosLab.chainInfo()` reports ≈15-16 links/side (interim world-scale sanity).
- `KratosLab.fxState()` shows restore discipline intact (blendEnabled false, depthMask true).
- Human checkpoint (Task 3) approves the segmented/twisted perceptual read.
- DEFERRED: on-screen link count vs footage measurement — requires 01-04 (reference/MEASUREMENTS.md); one-constant recalibration expected.
</verification>

<success_criteria>
- chain.js is a pure, Node-testable link-walker; chain.test.js green (pitch, twist perpendicularity, per-link constant axis, u ∈ [0,1], square-texel invariant, vertical-curve degenerate guard).
- drawFx PASS 1 builds the chain from Chain.buildRibbon with usual/depth-write-ON state from the decoded MAT_chainlink — zero hardcoded blend/depth.
- Links render segmented at LINK_PITCH with alternating twist and visible alpha gaps; dead flat-strip constants removed.
- Trail inner edge biased to lerp(hilt, tip, 0.6); no color/particle changes.
- LINK_PITCH labeled INFERRED with the deferred-calibration note; single source of truth in chain.js.
</success_criteria>

<output>
Create `.planning/phases/03-chain-link-ribbon-glow/03-01-SUMMARY.md` when done.
</output>
