---
phase: 06-particle-runtime-fire-sparks-trails
plan: 06
type: execute
wave: 5
depends_on: [06-04, 06-05]
files_modified:
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: true
requirements: [FIRE-02]
user_setup: []

must_haves:
  truths:
    - "Impact sparks fire on hit events from the combat state machine: simStep edge-detects machine.st.hits and bursts sparks on each change (FIRE-02, Pattern 5)"
    - "Spark bursts use the decoded FXC_BDEsparkemit family, spawned at the blade world position, as velocity-aligned STRETCHED billboards (Particles.stretchAxis) via the shared pool (CLAUDE.md Part 3)"
    - "Burst count/velocity are INFERRED (param semantics undecoded, A1) and labeled; color traces to the real MAT_pticleMat.blendColor (Pitfall 4); rendered additive-premult via Fx.applyMaterial, depth off"
    - "Sparks integrate on simStep at Loop.STEP and cull by lifetime; the burst is edge-triggered (once per hit), not per-frame (Pitfall 5)"
  artifacts:
    - path: "tools/kratos-lab/app.js"
      provides: "Hit-edge spark burst in simStep (prevHits edge detect) + velocity-aligned stretched-spark render in the pool"
      contains: "st.hits"
    - path: "tools/kratos-lab/index.html"
      provides: "Lockstep ?v= bump"
      contains: "app.js?v="
  key_links:
    - from: "tools/kratos-lab/app.js simStep"
      to: "machine.st.hits edge → fxPool burst"
      via: "prevHits !== st.hits triggers a spark burst"
      pattern: "prevHits"
    - from: "tools/kratos-lab/app.js spark render"
      to: "Particles.stretchAxis(vel, camUp)"
      via: "velocity-aligned stretched billboard axis"
      pattern: "stretchAxis"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** viewer landing a hit in kratos-lab, **I want to** see a burst of stretched sparks fly off the blade at the moment of impact, **so that** hits read with the percussive spark shower of GoW1 footage instead of a silent, spark-less clash.

This is **slice 3** (D-01): impact sparks on hit events, reusing the pool (06-04) and the same BDEsparkemit family as blade fire (06-05) — differing only by trigger (edge-on-hit) and the stretched-billboard look. After this task a viewer sees sparks burst on each hit. (Derived from ROADMAP Phase-6 SC2 + FIRE-02.)
</phase_goal>

<objective>
Fire impact-spark bursts on hit events by edge-detecting `machine.st.hits` in `simStep` and bursting velocity-aligned stretched sparks from the decoded `FXC_BDEsparkemit` family at the blade, through the shared pool, additive-premult.

Purpose: FIRE-02 is the on-hit trigger of the same emitter family FIRE-01 uses continuously (A6). The pool, premult path, and real color already exist; this plan adds the edge-triggered burst + the velocity-aligned stretched-billboard render.
Output: hit-edge burst in simStep + stretched-spark render; index.html ?v bump. Pure suites green; the hit-edge burst-count + stretch-axis behavior covered by the 06-01 contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-PATTERNS.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-04-SUMMARY.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-05-SUMMARY.md
@tools/kratos-lab/combat.js
@tools/kratos-lab/particles.js

<interfaces>
<!-- combat.js hit counter + 06-01 Particles + 06-04 pool + 06-05 fire color (VERIFIED). -->

combat.js: machine.st.hits (combat.js:146,172) — increments in start() on each non-idle, non-block move. Edge-detect across ticks: on `st.hits !== prevHits`, a new attacking move just started → burst sparks. isIdle() (combat.js:157) available.

Particles (06-01): stretchAxis(vel, camUp) — velocity-aligned axis for stretched sparks; burst(n, template, sampler) — deterministic in tests (sampler 0), stochastic at runtime (Math.random). spawnAnchor for the blade world position.
Pool (06-04): shared fxPool + drawPool (additive-premult, depth off). Sparks ride the SAME pool as fire.
FxDb (built in 06-05): the runtime `db = FxParse.buildFxDb(wadRecords, wadBuf)` and `KratosLab.fxdb()` are established at init by 06-05 Task 1 (06-06 is Wave 5, downstream of 06-05 Wave 4) — this plan CONSUMES that `db`, it does not rebuild it. `db.fxc['FXC_BDEsparkemit']` and `db.meta.colorSource` are in-WAD (no standalone fetch).
Fire family (06-05): FXC_BDEsparkemit is the same emitter family used for continuous fire (A6); the on-hit burst reuses it with an INFERRED higher rate/velocity. Color traces to db.meta.colorSource (real MAT_pticleMat.blendColor) — NOT a fabricated spark color (Pitfall 4).

Velocity-aligned stretched billboard (CLAUDE.md Part 3 / RESEARCH "Code Examples → Velocity-aligned stretch"): per-particle aVel attribute; the stretch axis = normalize(vel) (camUp fallback), corner.x rides the axis, corner.y the perpendicular. This is the "stretched spark" look — a quad scaled along motion. Needs a per-particle velocity attribute in the pool vertex pack (the pool buffer from 06-04 may need a vel attribute added if not already present).

simStep site (app.js:1063-1081): edge-detect + burst beside the fire spawn (06-05). Keep a module-level `prevHits`. INTEGRATE already at Loop.STEP (06-04). Pitfall 5: burst on the sim tick where the edge is detected, not per rendered frame.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hit-edge spark burst in simStep (FIRE-02 trigger)</name>
  <files>tools/kratos-lab/app.js</files>
  <read_first>
    - tools/kratos-lab/app.js:1047-1086 — simStep (the per-tick block + where prevHits edge-detect and the burst land).
    - tools/kratos-lab/combat.js:144-174 — st.hits increment in start(); isIdle(); the windows{queue,branch,cancel}.
    - tools/kratos-lab/particles.js — burst + spawnAnchor + stretchAxis contracts (06-01).
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Pattern 5" (combat-gated burst) + § "Code Examples → Combat-gated spark burst" + § "Assumptions Log" A1/A6.
  </read_first>
  <action>
    In `simStep`, add a module-scoped `let prevHits = machine.st.hits` (initialize once). Each tick, after `machine.tick(STEP)`, detect the edge: `if (machine.st.hits !== prevHits) { ...burst...; prevHits = machine.st.hits; }`. On the edge, for the attacking blade(s), compute the blade world position (`Particles.spawnAnchor(bladeSim[key].mat, sparkFxc.matrix)` reusing the FXC_BDEsparkemit placement) and `fxPool.burst(N, template, sampler)` an INFERRED count `N` of spark particles with INFERRED outward velocity (higher than continuous fire — the "impact" burst, A1/A6), INFERRED size + short life, tagged `kind:"spark"`. Velocity is jittered by the runtime `Math.random` sampler (D-07) so each spark flies a different direction (the burst fan). Sample bladeSim only at the burst instant (sparks decouple after, like fire). Label every INFERRED emission constant with a comment citing A1/A6. Do NOT add a separate emitter decode — reuse the already-real FXC_BDEsparkemit family (D-09a resolved; A6).
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq 'prevHits' tools/kratos-lab/app.js && grep -Eq 'st\.hits' tools/kratos-lab/app.js && echo BURST-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0 (the hit-edge burst-count contract — burst(n) adds exactly n — is green).
    - `app.js` keeps a `prevHits` edge state and bursts on `machine.st.hits !== prevHits` (grep both `prevHits` and the inequality).
    - The burst calls `fxPool.burst(` and reuses the FXC_BDEsparkemit family (grep — no new emitter decode added).
    - The burst is inside simStep (per sim tick), NOT in renderFrame/drawFx (grep — burst not present in the render path).
    - Burst count/velocity/life are commented INFERRED citing A1/A6.
  </acceptance_criteria>
  <done>simStep edge-detects machine.st.hits and bursts an INFERRED count of sparks from the real FXC_BDEsparkemit family at the blade on each hit, decoupling after the burst; the trigger is per-sim-tick (Pitfall 5) and backed by the 06-01 burst-count contract.</done>
</task>

<task type="auto">
  <name>Task 2: Velocity-aligned stretched-spark render + real color + ?v bump</name>
  <files>tools/kratos-lab/app.js, tools/kratos-lab/index.html</files>
  <read_first>
    - tools/kratos-lab/app.js — the pool program + drawPool (06-04) and the per-particle vertex pack (where the velocity attribute + stretch axis go).
    - tools/kratos-lab/particles.js — stretchAxis(vel, camUp) (06-01).
    - tools/kratos-lab/index.html:115-122 — lockstep ?v tags.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Code Examples → Velocity-aligned stretched spark" (the VS axis-from-velocity snippet) + § "Pattern 1" (billboard pool).
    - CLAUDE.md § "Part 3" (spark stretching — a quad scaled along motion).
  </read_first>
  <action>
    Render spark-kind particles as velocity-aligned STRETCHED billboards. In the pool vertex path (06-04), add a per-particle velocity attribute (`aVel`) if not already packed, and in the pool VERTEX shader build the stretch axis from the projected velocity: `axis = length(aVel) > eps ? normalize(aVel) : uCamUp`, `perp = normalize(cross(axis, camDir/uCamRight))`, `world = aCenter + axis*aCorner.x + perp*aCorner.y` — for spark particles the corner.x extent is scaled by an INFERRED stretch factor tied to speed (the "stretched spark" look). Non-spark particles (fire/trail-spark) keep the plain camRight/camUp billboard (branch on a per-particle flag or a uniform per batch). Color the sparks from the REAL `db.meta.colorSource` (MAT_pticleMat.blendColor) with the additive-premult alpha128 for brightness (no fabricated spark color, Pitfall 4). Keep `Fx.applyMaterial(gl, {mode:'additivePremult', disableDepthWrite:true})` as the only blend/depth source and append to fxLog; existing restoreFxState guards leaks. Then bump ALL index.html `?v=` tags in lockstep to the next integer.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq 'stretchAxis|aVel' tools/kratos-lab/app.js && ! grep -q '?v=23' tools/kratos-lab/index.html && echo STRETCH-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0 (stretchAxis velocity-aligned contract green).
    - The pool VS builds the stretch axis from a per-particle velocity (grep `aVel` + the normalize-of-velocity axis; camUp fallback present).
    - Spark color reads from `db.meta.colorSource` — no fabricated spark RGB (grep review, Pitfall 4).
    - Sparks render through `Fx.applyMaterial` additive-premult, depth off; no new hardcoded `gl.blendFunc(` (grep).
    - `index.html` ?v tags all bumped in lockstep (no `?v=23` remains).
    - Full suite regression: all eight suites exit 0.
  </acceptance_criteria>
  <done>Impact sparks render as velocity-aligned stretched billboards from the shared pool, colored by the real MAT_pticleMat.blendColor, additive-premult + depth off via Fx.applyMaterial; index.html ?v bumped. Hits produce a stretched spark shower with no fabricated color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combat st.hits edge → burst | A rapidly-changing hit counter could over-trigger bursts (perf) — pool cap bounds it |
| runtime-jittered spark velocity → stretched vert pack | Random velocity must be finite before packing (06-01 guard); zero-velocity falls back to camUp axis |
| spark pool pass → shared GL state | additive-premult depth-off must be restored (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Denial of Service (perf) | burst spam growing the pool | mitigate | Pool hard cap + short spark life cull (06-01); edge-triggered once per hit |
| T-06-06-02 | Denial of Service (render corruption) | zero/NaN velocity → degenerate stretch axis | mitigate | stretchAxis camUp fallback for |vel|<eps; Particles.spawn skips non-finite (06-01) |
| T-06-06-03 | Integrity (data-first) | fabricating a spark color | mitigate | Spark color = db.meta.colorSource (real); only INFERRED emission labeled (Pitfall 4) |
| T-06-06-SC | Tampering | supply chain | accept | Zero packages; classic <script> tags only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/particles.test.js` green (burst-count + stretchAxis contracts).
- Full suite (all eight) green — no regression.
- Manual (Phase-7 harness): sparks burst on each landed hit; stretched along motion; magenta-bg additive add; fxState() clean.
</verification>

<success_criteria>
- Sparks burst on hit events (edge-detected machine.st.hits) from the real FXC_BDEsparkemit family at the blade.
- Rendered as velocity-aligned stretched billboards via the shared pool, additive-premult, depth off.
- Burst count/velocity INFERRED and labeled; color from the real MAT_pticleMat.blendColor (no fabrication).
- Edge-triggered per sim tick (not per frame); integrated at Loop.STEP; fxState() clean.
</success_criteria>

<output>
Create `.planning/phases/06-particle-runtime-fire-sparks-trails/06-06-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 06-CONTEXT.md decisions:
- D-01: impact sparks are slice 3 (after fire).
- D-02: velocity-aligned stretched billboards from the shared pool; no instancing; batched additive; no depth sort.
- D-03: spawn at the blade then decouple (sparks fly free after the burst).
- D-07: emission stochastic at runtime; blend via Fx.applyMaterial; real color from MAT_pticleMat.
- D-09(a): FXC_BDEsparkemit reused as the already-real db.fxc key (same family as fire; no top-up).
</decisions_honored>
