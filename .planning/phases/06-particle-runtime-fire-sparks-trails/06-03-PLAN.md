---
phase: 06-particle-runtime-fire-sparks-trails
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: true
requirements: [TRL-01, TRL-02]
user_setup: []

must_haves:
  truths:
    - "The swordtrail ribbon renders with a RUNTIME age→color ramp (white-hot core → orange → ember over row age) applied in the FX fragment shader — INFERRED (no painted length-wise ramp exists, 05-04 proof), driven by Particles.rampColor, additive, depth-write OFF via MAT_swordtrail through Fx.applyMaterial (TRL-01)"
    - "The trail selects BFT (crimson fire) vs BGT (neutral swoosh) per move via Particles.variantFor(machine.st.current) — both variants render on the correct moves (TRL-02, INFERRED table)"
    - "Extrusion stays stepped at 60Hz (trailHist recorded per sim tick, NOT per frame) with NO spline smoothing; TRAIL_INNER_T tip-arc bias preserved (TRL-01 / SC3 explicit)"
    - "Every trail draw's blend/depth comes from its decoded MAT via Fx.applyMaterial — no hardcoded blendFunc; drawFx still ends with Fx.restoreFxState (DEC-01, D-07)"
  artifacts:
    - path: "tools/kratos-lab/app.js"
      provides: "Trail ribbon extension in drawFx: age→color ramp uniform + BFT/BGT variant selection; fxProg fragment gains the ramp mix"
      contains: "variantFor"
    - path: "tools/kratos-lab/index.html"
      provides: "Lockstep ?v= script-tag bump (browser behavior changed)"
      contains: "app.js?v="
  key_links:
    - from: "tools/kratos-lab/app.js drawFx trail pass"
      to: "Particles.variantFor / Particles.rampColor"
      via: "combat state → variant + per-row age → ramp uniform"
      pattern: "variantFor"
    - from: "tools/kratos-lab/app.js trail pass"
      to: "Fx.applyMaterial(gl, MAT_swordtrail)"
      via: "decoded additive + depth-off blend (no hardcoded blendFunc)"
      pattern: "applyMaterial"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** viewer watching a blade swing in kratos-lab, **I want to** see a thick, hot swing trail with a white-hot core fading to ember, in the right color for the move (crimson fire vs neutral swoosh), **so that** the trail reads like GoW1 footage instead of the thin, single-tint ribbon the user judged "way off."

This is **slice 1a** of the payoff-first order (D-01): the lowest-lift trail richness win — a runtime color ramp + dual-variant selection on the EXISTING ribbon substrate, before the particle pool adds spark riders in 06-04. After this task a real viewer sees a materially richer, correctly-tinted trail. (Derived from ROADMAP Phase-6 SC3 + the trail-fidelity-from-footage folded todo.)
</phase_goal>

<objective>
Extend the existing `drawFx` swordtrail ribbon (do NOT replace it — it IS the game's authentic trail mechanism) with a runtime INFERRED age→color ramp and per-move BFT-vs-BGT variant selection, both driven from the pure `Particles` helpers, keeping the stepped-60Hz extrusion and decoded-blend discipline intact.

Purpose: The user's top complaint is trail thinness/flatness. Closing the ramp + dual-variant gaps (D-04 a/d) is the first visible payoff and requires no new GL infrastructure — it reuses fxProg + MAT_swordtrail. The spark riders (D-04 c) follow in 06-04 once the pool exists.
Output: extended `drawFx` trail pass + fxProg fragment ramp; index.html ?v bump. Pure-suite green (no regression); ramp/variant logic covered by the 06-01 particles.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-PATTERNS.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-01-SUMMARY.md
@.planning/phases/03-chain-link-ribbon-glow/03-02-SUMMARY.md
@tools/kratos-lab/fx.js
@tools/kratos-lab/combat.js

<interfaces>
<!-- Contracts from the codebase (VERIFIED) + the 06-01 Particles module. -->

Existing drawFx trail pass (app.js:624-634, 687-699 — extend, do not rewrite):
  trailHist[key] = [{ tip:[3], hilt:[3], age }]  (recorded per sim tick in simStep:1063-1081)
  rows = hst.map((e,i) => ({ a: lerp3(e.hilt, e.tip, TRAIL_INNER_T), b: e.tip, u: i/(len-1), alpha: max(0,1-age/TRAIL_AGE)*0.85 }))
  pushRibbon(rows, trailV)  -> interleaved verts [x,y,z, u, v, alpha] stride 24 (6 floats)
  MAT_swordtrail (matDb.byName.MAT_swordtrail): mode "additive", disableDepthWrite true (0x48090080) — applied via Fx.applyMaterial.
  fxProg / fxLocs: uMVP, uM, uTex, aP(loc 0, offset 0), aT(loc 1, offset 12), uMaterialColor, uLayerColor, uCutoff. trailTex bound at unit 0.
  TRAIL_INNER_T = 0.6 (app.js:498, INFERRED tip-arc bias — KEEP, do not re-derive).

Particles (from 06-01 — pure, already tested):
  variantFor(moveName) -> "BFT" | "BGT"   (INFERRED per-move table)
  rampColor(t) -> [r,g,b]                  (INFERRED white-hot→orange→ember, monotone)

combat.js: machine.st.current (current move name), machine.isIdle() — read to pick the variant + gate the trail.

fxProg fragment (app.js:357-370): currently MODULATE + cutout. Add a per-row age→ramp mix:
  the ribbon already carries per-vertex `alpha` (age fraction proxy). Add a `uRampLo`/`uRampHi` (or a small ramp uniform) so the fragment tints texels toward rampColor by row age — INFERRED. The v coordinate + `alpha` are available; choose the age proxy that is already per-vertex (alpha decreases with age).

Script versioning (05-PATTERNS): bump EVERY <script> ?v= tag in index.html in lockstep to the next integer (currently v23 → v24). Never bump one tag alone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runtime age→color ramp on the swordtrail ribbon (TRL-01)</name>
  <files>tools/kratos-lab/app.js</files>
  <read_first>
    - tools/kratos-lab/app.js:582-702 — pushRibbon + the full drawFx trail pass (the exact rows.map + MAT_swordtrail draw to extend).
    - tools/kratos-lab/app.js:316-370 — the fxProg vertex/fragment shaders + fxLocs (where the ramp uniform + fragment mix land).
    - tools/kratos-lab/fx.js:24-55 — applyMaterial contract (blend/depth from MAT; never inline a blendFunc).
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Pattern 4" (trail ribbon extension) + § "State of the Art" (thin ribbon → runtime ramp) + § "Assumptions Log" A3 (ramp INFERRED).
    - .planning/phases/05-fx-record-decode/05-04-SUMMARY.md — the proof that GFX_swordtrail carries NO painted length-wise ramp (so the ramp MUST be runtime, labeled INFERRED).
  </read_first>
  <action>
    In `app.js`, add a runtime age→color ramp to the trail draw. In the fxProg FRAGMENT shader (app.js:357-370), add uniform(s) for the ramp (e.g. `uRampHot` white-hot RGB, `uRampCool` ember RGB, or a compact 3-stop set) and a `uTrailRamp` enable flag; when enabled, mix the MODULATE texel color toward the ramp color selected by the per-vertex age proxy (`vAlpha` — already varying; high alpha = young = hot, low alpha = old = ember). Wire the new uniforms into `fxLocs`. In `drawFx`'s trail pass, set `uTrailRamp = 1` and upload the ramp endpoint colors from `Particles.rampColor(0)` (hot) and `Particles.rampColor(1)` (ember) — label these INFERRED in a comment (no painted ramp exists, 05-04). Keep the existing per-row `alpha = max(0,1-age/TRAIL_AGE)*0.85` fade (fast-fade, TRL-01) and the `TRAIL_INNER_T` tip-arc bias unchanged. Keep `Fx.applyMaterial(gl, MAT_swordtrail)` as the ONLY blend/depth source and ensure the chain passes still set `uTrailRamp = 0` (so the ramp does not leak into the chain/glow draws). Do NOT add spline smoothing — the row list stays the discrete per-tick trailHist (SC3). Append the trail draw to `fxLog` as before.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq 'uTrailRamp|rampColor' tools/kratos-lab/app.js && echo RAMP-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0 (rampColor monotonicity contract green — the ramp source is the tested pure fn).
    - `app.js` references `Particles.rampColor` in the trail pass and a `uTrailRamp` uniform gate in the fragment (grep both).
    - The trail pass still calls `Fx.applyMaterial(gl, ...MAT_swordtrail...)` and NO literal `gl.blendFunc(` was added anywhere in drawFx (grep: `blendFunc` count in app.js drawFx region unchanged — the only blend source is applyMaterial).
    - The chain/glow passes set `uTrailRamp` to 0 before their draws (grep — ramp does not bleed into chain passes).
    - A comment labels the ramp INFERRED and cites 05-04 (no painted ramp).
  </acceptance_criteria>
  <done>The swordtrail ribbon tints per-row age toward a runtime white-hot→ember ramp (INFERRED, Particles.rampColor) via an fxProg fragment mix gated by uTrailRamp; blend/depth still from MAT_swordtrail via applyMaterial; stepped extrusion + tip bias intact; ramp isolated from chain passes.</done>
</task>

<task type="auto">
  <name>Task 2: Per-move BFT vs BGT trail-variant selection (TRL-02) + ?v bump</name>
  <files>tools/kratos-lab/app.js, tools/kratos-lab/index.html</files>
  <read_first>
    - tools/kratos-lab/app.js:593-702 — drawFx (where the variant selection + texture/ramp choice slots in).
    - tools/kratos-lab/combat.js:144-174 — machine.st.current + start()/isIdle() (the move-name source for variantFor).
    - tools/kratos-lab/index.html:115-122 — the lockstep ?v=23 script tags to bump.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Pattern 4" (d: BFT vs BGT per move) + § phase_requirements TRL-02 row (BFT crimson / BGT neutral).
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-01-SUMMARY.md — the Particles.variantFor contract + which representative move names it keys on.
  </read_first>
  <action>
    In `drawFx`, select the trail variant per side from the current move: `const variant = Particles.variantFor(machine.st.current)` → "BFT" (crimson fire) or "BGT" (neutral swoosh). Use the variant to choose the ramp endpoint colors passed to `uTrailRamp` (BFT = crimson-biased hot→ember; BGT = neutral/white swoosh with little tint) — both derived from `Particles.rampColor` scaled by an INFERRED per-variant tint, labeled INFERRED (color still ultimately traces to the runtime ramp, never a fabricated real color — Pitfall 4). Keep the SAME `trailTex` (GFX_swordtrail) and the SAME MAT_swordtrail blend for both variants (the variant differs in runtime tint, not decoded texture/blend). Guard the idle case: when `machine.isIdle()` and no trail history exists, draw nothing (existing `hst.length >= 2` guard already covers this — confirm it still holds). Then bump EVERY `<script>` `?v=` tag in `index.html` in lockstep to the next integer (v23 → v24) — browser-loaded `app.js` changed this wave. Do not add a particles.js script tag yet (06-04 adds it when the browser first consumes the pool).
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq "variantFor\(machine\.st\.current\)|variantFor\(" tools/kratos-lab/app.js && grep -Eq 'app\.js\?v=2[4-9]' tools/kratos-lab/index.html && echo VARIANT-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0 (variantFor determinism contract green).
    - `app.js` calls `Particles.variantFor(machine.st.current)` in drawFx (grep).
    - Both BFT and BGT branches exist and are labeled (grep `BFT` and `BGT` in the trail region of app.js).
    - `index.html` ?v= tags are ALL bumped in lockstep to the same integer ≥ 24 — verify no tag still reads `?v=23` (grep `?v=23` returns nothing).
    - The variant tints are commented INFERRED; the crimson/neutral distinction never introduces a `real`-tagged effect color.
  </acceptance_criteria>
  <done>drawFx picks BFT vs BGT per move via Particles.variantFor and tints the ramp accordingly (INFERRED), reusing the decoded GFX_swordtrail texture + MAT_swordtrail blend for both; index.html ?v= bumped in lockstep. Both trail variants render on the correct moves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combat state → trail render | Move name drives variant selection; a bad/unknown move name must default safely (no crash, pick a variant) |
| new fragment uniforms → GL | Ramp colors uploaded per frame must be finite (from the tested Particles.rampColor, so bounded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Denial of Service (render leak) | uTrailRamp bleeding into chain/glow passes | mitigate | Chain passes set uTrailRamp=0; drawFx ends with Fx.restoreFxState (existing); no new blendFunc added |
| T-06-03-02 | Integrity (data-first) | fabricating a real crimson trail color | mitigate | Variant tint derived from the runtime INFERRED ramp; labeled INFERRED; decoded texture/blend unchanged (Pitfall 4) |
| T-06-03-SC | Tampering | supply chain | accept | Zero packages; browser loads classic <script> tags only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/particles.test.js` green (ramp + variant pure contracts).
- Full suite regression: fxdb/wad/fx/loop/chain/anm/particles/light all exit 0.
- app.js trail pass uses Particles.rampColor + variantFor; blend/depth only from Fx.applyMaterial; index.html ?v bumped lockstep.
</verification>

<success_criteria>
- Trail renders a runtime white-hot→ember ramp (INFERRED) on the existing ribbon, additive + depth-off via decoded MAT_swordtrail.
- BFT vs BGT selected per move; both render on the correct moves with the decoded texture/blend.
- Stepped-60Hz extrusion + tip-arc bias preserved; no spline smoothing; no hardcoded blendFunc.
</success_criteria>

<output>
Create `.planning/phases/06-particle-runtime-fire-sparks-trails/06-03-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 06-CONTEXT.md decisions:
- D-01: trails first (payoff-first) — the lowest-lift richness win before the pool.
- D-04(a): runtime crimson/white-hot age→color ramp, INFERRED (no painted ramp, 05-04).
- D-04(b): TRAIL_INNER_T tip-arc bias preserved.
- D-04 (dual variant / TRL-02): BFT vs BGT selected per move from combat state.
- D-07: all blend via Fx.applyMaterial; no hardcoded blendFunc.
</decisions_honored>
</output>
