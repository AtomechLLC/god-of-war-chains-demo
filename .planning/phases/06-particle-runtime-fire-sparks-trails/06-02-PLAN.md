---
phase: 06-particle-runtime-fire-sparks-trails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/kratos-lab/fxparse.js
  - tools/kratos-lab/test/light.test.js
  - tools/kratos-lab/test/fxdb.test.js
autonomous: true
requirements: [REND-02, FIRE-01, FIRE-02, CHAIN-03]
user_setup: []

must_haves:
  truths:
    - "parseLight decodes LeftBladeLight/RightBladeLight (88-B WAD records, tag 0x1e @0x6a60/0x6b20) to EXACT decoded values: color (1.0,0.622,0.288)@+0x2c, intensity 2.5@+0x38, range 160@+0x44, anchor (-0.32,-8.0,1.0)@+0x10 — REAL/decoded, not INFERRED (D-09b, REND-02)"
    - "parseLight mirrors parseTxr's fail-loud idiom: size-gate (rec.size < 0x48) BEFORE any field read, throw naming the record — Security V5 (OOB read guard)"
    - "Left and Right blade light records are byte-identical in the decoded values"
    - "fxdb.test.js proves FXC_BDEsparkemit is a real db.fxc key (subtype 0x3, shapeRef in {flame6Shape,flame3Shape,flame5Shape}) — no decode top-up needed (D-09a, FIRE-01/02)"
    - "fxdb.test.js proves the fire family binds emitter→particle by shapeRef NAME (FXC_BDEsparkemit.shapeRef == a PTC's shapeRef), and FXC_CNGemit→PTC_CNGpart is a name-confirmed ref (shapeNameMatch===true) — D-08 / Pitfall 6 / CHAIN-03"
  artifacts:
    - path: "tools/kratos-lab/fxparse.js"
      provides: "FxParse.parseLight decoder (added to the export return line)"
      contains: "function parseLight"
    - path: "tools/kratos-lab/test/light.test.js"
      provides: "Byte-exact known-answers for Left/RightBladeLight + range-attenuation math"
      contains: "LeftBladeLight"
    - path: "tools/kratos-lab/test/fxdb.test.js"
      provides: "BDEsparkemit key/subtype/shapeRef + CNG name-confirmed ref + fire shapeRef-name binding assertions"
      contains: "FXC_BDEsparkemit"
  key_links:
    - from: "tools/kratos-lab/test/light.test.js"
      to: "assets/wads/R_WPN0_0.WAD"
      via: "Parsers.parseWad + resolve LeftBladeLight/RightBladeLight"
      pattern: "R_WPN0_0.WAD"
    - from: "tools/kratos-lab/fxparse.js parseLight"
      to: "the 88-byte LIGHT record fields"
      via: "size-gate then DataView getFloat32 at the decoded offsets"
      pattern: "0x48"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** viewer watching a Level-1 blade attack in kratos-lab, **I want to** see each blade cast its own warm point light onto Kratos and the environment using the game's real decoded light values, **so that** the fire reads as an actual light source (as in GoW1 footage) rather than a flat unlit sprite.

This plan is the **Wave 0 decode top-up** (D-09) for REND-02, and it locks the FxDb binding contracts the fire/spark/glow render slices depend on. `parseLight` is the one thin decoder this phase adds; the spark/CNG bindings are asserted (already real from Phase 5). (Derived from the ROADMAP Phase-6 goal SC5.)
</phase_goal>

<objective>
Add `FxParse.parseLight` — a thin binary decoder (mirrors `parseTxr`) for the `LeftBladeLight`/`RightBladeLight` 88-byte WAD records, decoding the REAL color/intensity/range/anchor values REND-02 needs — and prove, via test, that the fire/spark emitters (`FXC_BDEsparkemit`) and the chain-glow pair (`FXC_CNGemit`→`PTC_CNGpart`) are already real, name-confirmed FxDb entries the render waves can bind by shapeRef name.

Purpose: D-09 flagged two "not yet surfaced" sources; RESEARCH resolved both (blade-light = real bytes; BDEsparkemit = already a db.fxc key). This plan converts those findings into a decoder + regression tests so the render slices consume verified contracts, not assumptions.
Output: `parseLight` in fxparse.js, `test/light.test.js` (new), extended `test/fxdb.test.js` — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-PATTERNS.md
@.planning/phases/05-fx-record-decode/05-03-SUMMARY.md
@tools/kratos-lab/fxparse.js
@tools/kratos-lab/parsers.js

<interfaces>
<!-- Contracts the executor implements. parseLight mirrors parseTxr (fxparse.js:159). -->

FxParse.parseLight(buf, rec) -> { anchor:[3], color:[3], intensity, range, evidence:[...] }
  Size-gate FIRST: if rec.size < 0x48 throw `LIGHT ${rec.name}: size ${rec.size} < 0x48`.
  DataView over buf.buffer/byteOffset/byteLength; b = rec.dataOff.
  Byte-exact offsets (VERIFIED probe — RESEARCH "Decoded Data Inventory" + "Code Examples → parseLight"):
    anchor    = f32[b+0x10, +0x14, +0x18]   -> (-0.32, -8.0, 1.0)
    color     = f32[b+0x2c, +0x30, +0x34]   -> (1.0, 0.622, 0.288)
    intensity = f32[b+0x38]                 -> 2.5
    range     = f32[b+0x44]                 -> 160
  The 4 core values are REAL/decoded (byte-exact). Ancillary fields (+0x24 triple, +0x3c=8.0, +0x40=1.5) are read verbatim only if stored; tag them INFERRED-meaning (A5). Do NOT invent a falloff exponent as "real".

Parsers.parseWad(buf) -> records[{ idx, off, tag, size, name, dataOff }]   (locate the LIGHT records by tag 0x1e + name)
FxParse.readName(buf, off, maxLen) -> string   (reuse for record naming if needed)
FxParse.buildFxDb(records, wadBuf, standaloneRecs=[]) -> { meta, msh, ptc, fxc, refs }  (already exists; used to assert BDEsparkemit/CNG)

LIGHT record locations (VERIFIED probe): LeftBladeLight @0x6a60, RightBladeLight @0x6b20, both 88 B, WAD tag 0x1e; the two records are byte-identical in the decoded values.

Range-attenuation math (pure, used by REND-02 render wave 06-08; assert here so the math is tested before it is wired):
  atten(dist, range) = max(0, 1 - dist/range)   ->  atten(0)=1 ; atten(range)=0 ; atten(range/2)=0.5

FxDb assertions (real from Phase 5 — RESEARCH "Decoded Data Inventory"):
  db.fxc["FXC_BDEsparkemit"] exists, subtype 0x3 (spark), shapeRef in {flame6Shape, flame3Shape, flame5Shape}; variant ".0" -> flame3Shape.
  Fire binding is by shapeRef NAME (Pitfall 6): FXC_BDEsparkemit uses placeholder slot 0x0 (NOT paired in db.refs); join to PTC_flame6 by matching shapeRef "flame6Shape".
  db.refs contains a name-confirmed FXC_CNGemit→PTC_CNGpart pair (shapeNameMatch === true, ref CNGpartShape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — parseLight byte-exact known-answers + attenuation math + BDEsparkemit/CNG binding assertions</name>
  <files>tools/kratos-lab/test/light.test.js, tools/kratos-lab/test/fxdb.test.js</files>
  <read_first>
    - tools/kratos-lab/test/fxdb.test.js — the WAD-read + resolve + known-answer scaffold to copy (WAD_PATH, Parsers.parseWad, ImageData shim if needed, {} scenario blocks); this task extends it AND clones its header for light.test.js.
    - tools/kratos-lab/test/wad.test.js:263-280 — the synthetic bad-magic / short-size throw idiom to copy for the parseLight size-gate test.
    - tools/kratos-lab/fxparse.js:159-173 — parseTxr (the exact size-gate-then-DataView idiom parseLight will follow).
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Decoded Data Inventory" (Left/RightBladeLight row + BDEsparkemit row + CNG row), § "Code Examples → parseLight", § "Open Questions" (Q2 variant selection, Q3 ancillary fields).
  </read_first>
  <action>
    Create `tools/kratos-lab/test/light.test.js` (node:assert, header naming the run command + REND-02 + RED note). Read `assets/wads/R_WPN0_0.WAD`, `Parsers.parseWad(buf)`, resolve `LeftBladeLight` and `RightBladeLight` (by name, tag 0x1e). RED assertions (must FAIL — parseLight not yet exported):
    (a) `FxParse.parseLight(buf, recLeft)` → assert color ≈ (1.0, 0.622, 0.288) (tolerance 1e-3), intensity ≈ 2.5, range ≈ 160, anchor ≈ (-0.32, -8.0, 1.0). Cite the byte offsets in comments.
    (b) Left ≡ Right: parseLight on both → assert the four decoded values are equal (byte-identical records).
    (c) size-gate (Security): a synthetic rec with `size` 0x20 (< 0x48) throws `/LIGHT .*size .*0x48/` BEFORE reading fields.
    (d) attenuation math: assert a local `atten(dist,range)=Math.max(0,1-dist/range)` gives atten(0,160)=1, atten(160,160)=0, atten(80,160)=0.5 (this is the pure math REND-02 wave 06-08 will wire into the mesh shader; test it now).
    THEN extend `tools/kratos-lab/test/fxdb.test.js` with a `// --- Phase-6 binding contracts ---` block: build `db = FxParse.buildFxDb(recs, buf, standaloneRecs)` (load the standalone CNG/BFT/BGT .bin via the existing loadBin helper as needed). Assert: `db.fxc["FXC_BDEsparkemit"]` exists with `subtype === 0x3` and `shapeRef` ∈ {flame6Shape,flame3Shape,flame5Shape}; a variant resolving to flame3Shape exists; a `db.ptc` entry with the SAME shapeRef as `FXC_BDEsparkemit` exists (fire shapeRef-name binding, Pitfall 6); and `db.refs` contains an `FXC_CNGemit`→`PTC_CNGpart` entry with `shapeNameMatch === true`.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/light.test.js 2>&1 | grep -Eq 'parseLight|is not a function|TypeError' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/light.test.js` exits NON-zero (RED) naming `parseLight`.
    - The literals `LeftBladeLight`, `RightBladeLight`, `0.622`, `0.288`, `2.5`, `160`, `-8`, `0x48` each appear in light.test.js (grep each).
    - light.test.js asserts atten(0)=1, atten(range)=0, atten(range/2)=0.5 (grep the attenuation block).
    - fxdb.test.js now references `FXC_BDEsparkemit`, `subtype`, `flame3Shape`, `FXC_CNGemit`, `shapeNameMatch` (grep each).
    - The size-gate RED test uses a synthetic short rec and expects a `/LIGHT .*0x48/` throw (grep).
  </acceptance_criteria>
  <done>light.test.js RED against parseLight; pins the four byte-exact blade-light values, Left≡Right identity, the fail-loud size-gate, and the attenuation math. fxdb.test.js extended to assert the BDEsparkemit key/subtype/shapeRef, the fire shapeRef-name binding, and the CNG name-confirmed ref — the contracts the render waves bind against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement parseLight in fxparse.js</name>
  <files>tools/kratos-lab/fxparse.js</files>
  <behavior>
    - parseLight(buf, rec) → { anchor:[3], color:[3], intensity, range, evidence:[...] }.
    - LeftBladeLight → color (1.0,0.622,0.288), intensity 2.5, range 160, anchor (-0.32,-8.0,1.0); Right identical.
    - rec.size < 0x48 → named throw before any read.
    - The 4 core values tagged real in evidence[]; ancillary offsets (if read) tagged INFERRED-meaning (A5).
  </behavior>
  <read_first>
    - tools/kratos-lab/fxparse.js:148-173 — parseTxr header + size-gate idiom to mirror exactly.
    - tools/kratos-lab/fxparse.js:436-502 — parseAnmType5 vec reads bounded by rec.size + the real/INFERRED evidence-array discipline.
    - tools/kratos-lab/fxparse.js:719 — the export return line to extend with parseLight.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Code Examples → parseLight" (the exact offsets) + § "Common Pitfalls" (fail-loud size-gate).
  </read_first>
  <action>
    In `tools/kratos-lab/fxparse.js`, add `function parseLight(buf, rec)` inside the FxParse IIFE, immediately after `parseTxr`, with a JSDoc byte-layout header citing the offsets + REND-02/D-09 + the VERIFIED-probe provenance. Size-gate FIRST: `if (rec.size < 0x48) throw new Error(\`LIGHT ${rec.name}: size ${rec.size} < 0x48\`);`. Create a `DataView(buf.buffer, buf.byteOffset, buf.byteLength)`, `const b = rec.dataOff`. Read `anchor = [f32(b+0x10), f32(b+0x14), f32(b+0x18)]`, `color = [f32(b+0x2c), f32(b+0x30), f32(b+0x34)]`, `intensity = f32(b+0x38)`, `range = f32(b+0x44)` (all little-endian). Build `evidence[]` tagging color/intensity/range/anchor as `real` (byte-exact, cite offsets); if you also record the ancillary +0x24 triple / +0x3c / +0x40 fields, tag their MEANING `INFERRED` (A5 — do NOT claim a falloff exponent as real). Return the light object. Extend the module's `return { ... }` line to include `parseLight`. Do NOT modify buildFxDb here (the blade-light record is consumed directly by the render wave; it is not part of the FXC/PTC/MSH graph).
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/light.test.js && node tools/kratos-lab/test/fxdb.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/light.test.js` exits 0 (byte-exact values + Left≡Right + size-gate + attenuation green).
    - `node tools/kratos-lab/test/fxdb.test.js` exits 0 (BDEsparkemit + CNG + fire-binding assertions green, no Phase-5 regression).
    - `grep -c 'function parseLight' tools/kratos-lab/fxparse.js` === 1; the export return line lists `parseLight`.
    - `grep -Eq 'LIGHT .*0x48' tools/kratos-lab/fxparse.js` (named size-gate throw present).
    - `node -e "const F=require('./tools/kratos-lab/fxparse.js'),P=require('./tools/kratos-lab/parsers.js'),fs=require('fs');const b=new Uint8Array(fs.readFileSync('assets/wads/R_WPN0_0.WAD'));const recs=P.parseWad(b);const r=recs.find(x=>x.name==='LeftBladeLight');const L=F.parseLight(b,r);if(Math.abs(L.intensity-2.5)>1e-3||Math.abs(L.range-160)>1e-2)process.exit(1)"` exits 0 (real decoded values).
    - Regression: `node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/anm.test.js` still exit 0.
  </acceptance_criteria>
  <done>parseLight decodes the real blade-light values byte-exact with a fail-loud size-gate; light.test.js + fxdb.test.js green; the BDEsparkemit/CNG FxDb binding contracts are asserted for the render waves; no Phase-5 regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disc-derived WAD bytes → parseLight | Untrusted binary; a short/corrupt LIGHT record must not read past its 88-byte extent into the adjacent record (WR-01 / ASVS V5) |
| assets/ fetch policy | Reads stay under `assets/wads/`; never widen to `extracted/` or the ISO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information disclosure / Tampering | parseLight short-record OOB read | mitigate | Size-gate `rec.size < 0x48` before any field read, throw naming the record; Task-1 (c) short-record throw test |
| T-06-02-02 | Integrity (data-first) | claiming an ancillary field (falloff) as real | mitigate | Only color/intensity/range/anchor tagged real; ancillary +0x24/+0x3c/+0x40 meaning tagged INFERRED (A5) |
| T-06-02-SC | Tampering | supply chain | accept | Zero packages — node:assert + built-ins only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/light.test.js` green (byte-exact blade-light values + attenuation + size-gate).
- `node tools/kratos-lab/test/fxdb.test.js` green (BDEsparkemit + CNG + fire-binding contracts).
- `node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/anm.test.js` green (regression).
</verification>

<success_criteria>
- parseLight decodes Left/RightBladeLight to the exact color/intensity/range/anchor — REAL/decoded (REND-02 becomes real, not INFERRED).
- Fail-loud size-gate mirrors parseTxr; Left≡Right proven.
- FXC_BDEsparkemit is confirmed a real db.fxc key with the fire shapeRef-name binding; CNG name-confirmed pair confirmed.
- No regression to the Phase-5 suites.
</success_criteria>

<output>
Create `.planning/phases/06-particle-runtime-fire-sparks-trails/06-02-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 06-CONTEXT.md decisions:
- D-06: blade-light values sourced from a decoded LIGHT record (parseLight), not hardcoded roadmap constants.
- D-08: fire binds emitter→particle by shapeRef NAME; CNG via a name-confirmed ref (shapeNameMatch true) — asserted, not assumed.
- D-09: the two decode top-ups resolved — parseLight (blade lights, REAL) added; BDEsparkemit confirmed already-real (no top-up), asserted in test.
</decisions_honored>
