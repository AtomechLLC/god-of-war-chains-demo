---
phase: 06-particle-runtime-fire-sparks-trails
plan: 05
type: execute
wave: 4
depends_on: [06-02, 06-04]
files_modified:
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: true
requirements: [FIRE-01]
user_setup: []

must_haves:
  truths:
    - "Both flame systems (flame3 + flame6) burn on each blade in every combat frame: FXC_BDEsparkemit→flame6Shape and FXC_BDEsparkemit.0→flame3Shape emitters bind to PTC_flame6/PTC_flame3 by shapeRef NAME (Particles.fireBindings / D-08 / Pitfall 6), spawned via the shared pool (FIRE-01)"
    - "Fire spawns at the decoded FXC emitter matrix translation transformed to WORLD by the live blade matrix (Particles.spawnAnchor), then DECOUPLES — so fire LAGS a whipping blade instead of sticking to it (SC1 / D-03 / Pitfall 4)"
    - "Fire color is the REAL MAT_pticleMat.blendColor (db.meta.colorSource, [2,2,2,1] overbright), rendered additive-premult (alpha-over-1.0) via Fx.applyMaterial; rates/sizes/lifetimes are INFERRED, labeled (A1)"
    - "Fire integrates on simStep at Loop.STEP (Pitfall 5); the pool draw stays additive/depth-off through Fx.applyMaterial with fxState() clean (Pitfall 3)"
  artifacts:
    - path: "tools/kratos-lab/app.js"
      provides: "Blade-fire emission in simStep: fireBindings resolution + world-space spawn-decouple for flame3/flame6; real-color pool draw"
      contains: "fireBindings"
    - path: "tools/kratos-lab/index.html"
      provides: "Lockstep ?v= bump (app.js changed)"
      contains: "app.js?v="
  key_links:
    - from: "tools/kratos-lab/app.js simStep"
      to: "Particles.spawnAnchor(bladeSim[key].mat, fxc.matrix)"
      via: "blade-local FXC translation → world spawn, then decouple"
      pattern: "spawnAnchor"
    - from: "tools/kratos-lab/app.js fire binding"
      to: "db.fxc FXC_BDEsparkemit → db.ptc PTC_flame6/flame3 by shapeRef name"
      via: "Particles.fireBindings(db)"
      pattern: "fireBindings"
    - from: "tools/kratos-lab/app.js fire color"
      to: "db.meta.colorSource (MAT_pticleMat.blendColor)"
      via: "real decoded effect color into the premult pool draw"
      pattern: "colorSource"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** viewer watching a blade in combat, **I want to** see both layered flame systems burning on each blade, hugging it every frame but trailing behind when the blade whips, **so that** the blades look ignited and alive as in GoW1 footage — not a static sprite stuck to the geometry.

This is **slice 2** (D-01): blade fire flame3/flame6 in world space, reusing the pool from 06-04. After this task a viewer sees continuous blade fire that lags a fast swing — the SC1 blade-lag behavior. (Derived from ROADMAP Phase-6 SC1 + FIRE-01.)
</phase_goal>

<objective>
Spawn the two decoded flame systems (flame3 + flame6) on each blade every combat tick — bound emitter→particle by shapeRef NAME, anchored at the decoded FXC emitter matrix transformed to world space and then decoupled (blade-lag), colored by the real `MAT_pticleMat.blendColor`, rendered additive-premult through the shared pool.

Purpose: This is the core fire payoff (SC1). The pool + premult path (06-04) and the FxDb bindings (06-02) already exist; this plan wires the real decoded emitters into world-space emission with the correct lag behavior and the one sanctioned real effect color.
Output: fire emission in simStep (fireBindings + spawnAnchor + real color) drawn by the existing pool pass; index.html ?v bump. Pure suites green; blade-lag proven by the 06-01 divergence contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-PATTERNS.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-02-SUMMARY.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-04-SUMMARY.md
@tools/kratos-lab/particles.js
@tools/kratos-lab/fxparse.js

<interfaces>
<!-- FxDb (Phase 5) + 06-01 Particles + 06-04 pool (VERIFIED). -->

FxDb (FxParse.buildFxDb, built at app load): db.fxc, db.ptc, db.refs, db.meta.colorSource.
  db.fxc["FXC_BDEsparkemit"]   : subtype 0x3, shapeRef "flame6Shape", real blade-local matrix (translation @ slice(12,15), e.g. (0,0.226,-9.172))
  db.fxc["FXC_BDEsparkemit.0"] : shapeRef "flame3Shape"
  db.ptc["PTC_flame6"]         : shapeRef "flame6Shape"  (632 B)
  db.ptc["PTC_flame3"]         : shapeRef "flame3Shape"
  db.meta.colorSource          : MAT_pticleMat.blendColor = [2,2,2,1]  (REAL, overbright — the ONE sanctioned effect color; DO NOT fabricate a crimson)

Binding rule (D-08 / Pitfall 6): FXC_BDEsparkemit uses PLACEHOLDER slot 0x0 and is NOT in db.refs slot pairs — join to its PTC by matching shapeRef STRING ("flame6Shape" == "flame6Shape"). Use Particles.fireBindings(db) (06-01) which returns [{emitter, particle, shapeRef}].
Variant selection (Pitfall 7 / A4): use FXC_BDEsparkemit (→flame6) + FXC_BDEsparkemit.0 (→flame3) as the level-1 flame6/flame3 pair; a shapeRef resolving to flame5Shape is god/other tier — do NOT use it for level-1 fire.

Particles (06-01): spawnAnchor(bladeMat, fxcMatrix), makePool/spawn/integrate (the shared fxPool from 06-04), fireBindings(db).
Pool (06-04): fxPool + drawPool (additive-premult, depth off, via Fx.applyMaterial). Fire particles ride the SAME pool.

simStep site (app.js:1063-1081): the per-tick block. bladeSim[key].mat is the live blade world matrix this tick (key = "l"/"r"). Spawn fire when `attacking = !machine.isIdle()`. INTEGRATE once per tick at Loop.STEP (already added in 06-04).

Blade-lag (SC1 / Pitfall 4): sample bladeSim[key].mat ONLY at spawn (inside spawnAnchor); after spawn the particle integrates in world space and never re-reads the blade. INFERRED: rate/velocity/size/lifetime (A1) — label them.

Fire texture: bind the flame particle texture (flame3/flame6 shape's texture via the existing fxTexFromMat / decodeTexture path) for the fire batch; batch by blend+texture (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve fire bindings + world-space spawn-decouple for flame3/flame6 (SC1 blade-lag)</name>
  <files>tools/kratos-lab/app.js</files>
  <read_first>
    - tools/kratos-lab/app.js:1047-1086 — simStep (the attacking gate + bladeSim[key].mat + the per-tick block where fire spawn lands).
    - tools/kratos-lab/app.js:499-546 — bladeSim, driveBlade (.mat producer), xformM (the transform spawnAnchor mirrors).
    - tools/kratos-lab/particles.js — fireBindings + spawnAnchor + spawn contracts (from 06-01).
    - tools/kratos-lab/fxparse.js — buildFxDb output shape (db.fxc/db.ptc/db.meta.colorSource); how the FxDb is built at load.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Pattern 2" (spawn→world decouple) + § "Pitfall 4" (blade-lag) + § "Pitfall 6/7" (shapeRef-name binding, variant choice) + § "Assumptions Log" A1/A4.
  </read_first>
  <action>
    At app load (where the FxDb is built), resolve the fire emitters once: `const fireBinds = Particles.fireBindings(db)` and select the level-1 flame6/flame3 pair (emitter FXC_BDEsparkemit → PTC_flame6, FXC_BDEsparkemit.0 → PTC_flame3; reject any binding whose shapeRef resolves to flame5Shape — god/other tier, Pitfall 7). Stash the two decoded FXC placement matrices (blade-local). In `simStep`, inside the per-tick blade loop (app.js:1069-1080), when `attacking`: for each blade side `key` and each of the two flame systems, compute the world spawn position `Particles.spawnAnchor(bladeSim[key].mat, fxc.matrix)` and `fxPool.spawn(...)` an INFERRED small number of fire particles per tick with INFERRED velocity (slight upward/outward jitter via the runtime sampler, D-07), INFERRED size, and INFERRED life (tick units, NTSC 60Hz). Sample bladeSim[key].mat ONLY at spawn — the particle then integrates in world space (already wired in 06-04) and decouples (SC1). Do NOT parent live fire particles to the blade (Pitfall 4). Tag `kind:"fire3"|"fire6"` so the draw can batch/texture them. Label every INFERRED emission constant with a comment; leave a TODO citing Open Q1 (param-semantics top-up is the upgrade path to promote rates/lifetimes to real).
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && node tools/kratos-lab/test/fxdb.test.js && grep -Eq 'fireBindings|spawnAnchor' tools/kratos-lab/app.js && echo FIRE-BIND-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0 (spawnAnchor + blade-lag divergence contracts green — the exact behavior this wiring relies on).
    - `node tools/kratos-lab/test/fxdb.test.js` exits 0 (FXC_BDEsparkemit shapeRef binding contract green).
    - `app.js` calls `Particles.fireBindings(` at load and `Particles.spawnAnchor(bladeSim[` in the attacking branch of simStep (grep both).
    - The fire spawn is inside the `attacking`/`!machine.isIdle()` gate and samples bladeSim[key].mat only at spawn (grep — no live-particle re-parenting to bladeSim in an integrate/draw path).
    - A flame5Shape / god-tier rejection guard is present (grep `flame5` with a reject/skip comment).
    - Emission constants are commented INFERRED with an Open-Q1 upgrade-path TODO.
  </acceptance_criteria>
  <done>Fire emitters resolved by shapeRef name to the level-1 flame3/flame6 PTCs; both systems spawn at the decoded FXC anchor transformed to world per blade each attacking tick, then decouple (blade-lag). God-tier flame5 rejected. Emission INFERRED and labeled; the blade-lag behavior is backed by the 06-01 divergence known-answer.</done>
</task>

<task type="auto">
  <name>Task 2: Real-color fire draw through the pool (additive-premult) + ?v bump</name>
  <files>tools/kratos-lab/app.js, tools/kratos-lab/index.html</files>
  <read_first>
    - tools/kratos-lab/app.js:637-700 — drawFx + the drawPool pass (from 06-04) where fire particles render.
    - tools/kratos-lab/app.js:316-435 — fx textures / fxTexFromMat / decodeTexture (the flame texture bind path).
    - tools/kratos-lab/fxparse.js — db.meta.colorSource (MAT_pticleMat.blendColor [2,2,2,1]).
    - tools/kratos-lab/index.html:115-122 — the lockstep ?v tags.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Pattern 3" (premult) + § "Don't Hand-Roll" (colorSource) + § "Pitfall 4" (no fabricated color).
  </read_first>
  <action>
    In `drawPool` (or the pool pass in drawFx), color the fire particles from the REAL `db.meta.colorSource` (MAT_pticleMat.blendColor, [2,2,2,1] overbright) — pack that RGB into the fire particles' per-vertex color with the alpha128 carrying the overbright factor so the additive-premult fragment (`rgb * alpha128`, ONE,ONE) recovers the true GS brightness (alpha up to ~1.99, CLAUDE.md Part 1). Bind the correct flame texture (flame3/flame6 shape texture via the existing fxTexFromMat/decodeTexture path) and batch the fire particles by blend+texture (D-02); if flame3 and flame6 use different textures, draw them as two batched sub-draws. Keep `Fx.applyMaterial(gl, { name:'fxFire', mode:'additivePremult', disableDepthWrite:true })` as the only blend/depth source (no hardcoded blendFunc), append to fxLog, and rely on the existing drawFx `Fx.restoreFxState` to prevent leaks. Do NOT introduce any hand-picked crimson — the fire color is the decoded MAT_pticleMat value (Pitfall 4); only the runtime age fade (via rampColor alpha) is INFERRED. Then bump ALL index.html `?v=` tags in lockstep to the next integer.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq 'colorSource' tools/kratos-lab/app.js && grep -Eq 'additivePremult' tools/kratos-lab/app.js && grep -vq '\?v=23' tools/kratos-lab/index.html && echo FIRE-DRAW-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0.
    - Fire color reads from `db.meta.colorSource` (MAT_pticleMat.blendColor) — grep `colorSource` in the fire draw; NO literal crimson/hand-picked RGB is assigned to fire (grep review — no fabricated real color, Pitfall 4).
    - The fire batch renders through `Fx.applyMaterial` with `additivePremult` and depth-write off (grep additivePremult; no new `gl.blendFunc(` literal in the fire draw).
    - `index.html` ?v tags all bumped in lockstep (no `?v=23` remains); particles.js tag still present from 06-04.
    - The overbright alpha128 path is used so fire brightness exceeds the 1.0 clamp (comment cites CLAUDE.md Part 1 alpha-over-1.0).
    - Full suite regression: all eight suites exit 0.
  </acceptance_criteria>
  <done>Blade fire renders with the real MAT_pticleMat.blendColor via the additive-premult pool pass (alpha-over-1.0 brightness), flame3/flame6 batched by texture, blend/depth only from Fx.applyMaterial; index.html ?v bumped. Both flame systems burn on each blade in combat, lagging a whipping blade, with no fabricated color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decoded FXC matrix + INFERRED emission → pool.spawn | Placement matrix is real; emission params are INFERRED — both must be finite before packing to GL (06-01 guard) |
| bladeSim[key].mat (pre-existing approximate solver) → spawn anchor | Blade motion is a Phase-4-owned approximation; fire rides it as-is (do NOT fix blade motion here) |
| fire pool pass → shared GL state | additive-premult depth-off must be restored (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Denial of Service (render corruption) | non-finite emission param into vert pack | mitigate | Particles.spawn skips non-finite (06-01); INFERRED constants are finite by construction |
| T-06-05-02 | Integrity (data-first) | fabricating a crimson fire color | mitigate | Fire color = db.meta.colorSource (real MAT_pticleMat); only age fade INFERRED (Pitfall 4) |
| T-06-05-03 | Denial of Service (state leak) | fire pool pass leaving depth/blend set | mitigate | Blend/depth via Fx.applyMaterial; drawFx ends with restoreFxState; fxState() clean |
| T-06-05-SC | Tampering | supply chain | accept | Zero packages; classic <script> tags only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/particles.test.js` green (spawnAnchor + blade-lag divergence).
- `node tools/kratos-lab/test/fxdb.test.js` green (BDEsparkemit shapeRef binding).
- Full suite (all eight) green — no regression.
- Manual (Phase-7 harness): fire visibly lags a fast swing (not blade-locked); magenta-bg additive add; fxState() clean.
</verification>

<success_criteria>
- flame3 + flame6 both spawn on each blade every combat tick, bound by shapeRef name (level-1 pair; flame5 rejected).
- Spawn is world-space (spawnAnchor) then decoupled → fire lags a whipping blade (SC1).
- Fire color is the real MAT_pticleMat.blendColor via additive-premult (alpha-over-1.0); rates/sizes/lifetimes INFERRED and labeled.
- Integrated on simStep at Loop.STEP; no hardcoded blendFunc; fxState() clean.
</success_criteria>

<output>
Create `.planning/phases/06-particle-runtime-fire-sparks-trails/06-05-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 06-CONTEXT.md decisions:
- D-01: fire is slice 2 (after trails).
- D-03: world-space spawn-then-decouple (fire lags the blade).
- D-06/Part-1: additive-premult alpha-over-1.0 for fire brightness.
- D-07: emission stochastic at runtime; blend via Fx.applyMaterial; real color from MAT_pticleMat.
- D-08: emitter→particle bound by shapeRef NAME (fireBindings), not slot pairs.
- D-09(a): FXC_BDEsparkemit consumed as the already-real db.fxc key (no top-up).
</decisions_honored>
</output>
