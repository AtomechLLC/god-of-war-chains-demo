---
phase: 06-particle-runtime-fire-sparks-trails
plan: 07
type: execute
wave: 6
depends_on: [06-01, 06-04, 06-06]
files_modified:
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: true
requirements: [CHAIN-03]
user_setup: []

must_haves:
  truths:
    - "Chain glow is state-dependent: dark links at rest → hot streak during attack/throw active windows, driven by Particles.glowGain(machine.isIdle()/window state) — the dark↔hot rule is INFERRED (no decoded state-gate field exists, verified in Phase 5), labeled (D-05, A2, CHAIN-03)"
    - "The glow brightness uses the alpha-over-1.0 fix (03-02 carry-forward): the chainglow PASS applies the glow gain via shader-premultiply of raw alpha128 with additivePremult (blendFunc ONE,ONE), recovering true GS brightness — data-grounded, NOT a hand-tuned multiplier"
    - "The glow color is the in-WAD decoded chainglow texture (chainglowTex, Phase 3 — real texels, identity material/blend so they pass through); no hand-picked glow color. NOTE: FXC_CNGemit/PTC_CNGpart is STANDALONE-only (absent from the WAD, Phase-5 Pitfall 1) — it is NOT a runtime dependency here; its name-confirmed pair is asserted in 06-02 fxdb.test.js (in-test standalone load only). This slice needs NO db.fxc at runtime (D-08 corroboration, not a render input)"
    - "The chainglow draw remains additive + depth-off via Fx.applyMaterial with the coplanar LEQUAL overlay intact; drawFx ends with restoreFxState (Pitfall 3, DEC-01)"
  artifacts:
    - path: "tools/kratos-lab/app.js"
      provides: "Combat-gated glow gain on the chainglow pass via additivePremult + a uGlowGain uniform; glowGain from Particles"
      contains: "glowGain"
    - path: "tools/kratos-lab/index.html"
      provides: "Lockstep ?v= bump"
      contains: "app.js?v="
  key_links:
    - from: "tools/kratos-lab/app.js drawFx chainglow pass"
      to: "Particles.glowGain(isIdle, {rest, hot})"
      via: "combat state → glow intensity uniform"
      pattern: "glowGain"
    - from: "tools/kratos-lab/app.js chainglow pass"
      to: "additivePremult (blendFunc ONE,ONE) alpha-over-1.0"
      via: "shader-premultiply raw alpha128 × gain for true brightness"
      pattern: "additivePremult"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** viewer watching Kratos idle then attack, **I want to** see the chain links stay dark at rest and flare into a hot streak during attacks and throws, **so that** the chain reads as heat-reactive like GoW1 footage — closing the "glow VERY subtle" complaint from the 03-02 checkpoint.

This is **slice 4** (D-01): state-dependent chain glow. It reuses the additive-premult path (06-04) to recover the true brightness the 03-02 lever identified, gated by combat state. After this task a viewer sees the glow respond to attacks. (Derived from ROADMAP Phase-6 SC4 + CHAIN-03 + the 03-02 glow-intensity carry-forward.)
</phase_goal>

<objective>
Gate the existing chainglow overlay pass by combat state — dark at rest, hot during attack/throw active windows — via the pure `Particles.glowGain` rule, and apply the alpha-over-1.0 brightness fix (shader-premultiply of raw alpha128 × gain, additivePremult ONE,ONE) so the glow reads at its true GS brightness rather than the ~half-brightness clamped result.

Purpose: 03-02 left two levers open; this closes the glow one. The dark↔hot transition is INFERRED (Phase 5 verified NO decoded state-gate field exists), but the brightness recovery is data-grounded (alpha-over-1.0, CLAUDE.md Part 1), and the glow color stays the in-WAD decoded chainglow texture (chainglowTex) — no hand-tuning of color. (FXC_CNGemit is standalone-only and NOT consumed at runtime here; it is test-only corroboration.)
Output: combat-gated glow gain + premult brightness on the chainglow pass; index.html ?v bump. glowGain contract green (06-01); fxState() clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-PATTERNS.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-01-SUMMARY.md
@.planning/phases/06-particle-runtime-fire-sparks-trails/06-04-SUMMARY.md
@.planning/phases/03-chain-link-ribbon-glow/03-02-SUMMARY.md
@tools/kratos-lab/combat.js
@tools/kratos-lab/particles.js

<interfaces>
<!-- Existing chainglow PASS 2 (app.js:671-685) + combat state + 06-01 glowGain + 06-04 premult. -->

Existing chainglow pass (app.js:671-685): PASS 2 draws the SAME chain vertex bytes as PASS 1 (bit-identical depth), MAT_chainglow (additive, depth off, 0x48090080), depthFunc(LEQUAL) coplanar overlay, chainglowTex, identity material/blend colors (texels pass through — the heat-ramp colors come from the DECODED texture; NO hand-picked color). This pass is the thing to gate + brighten.

combat.js: machine.isIdle() (combat.js:157) — false during attacks; windows{queue:0.20, branch:0.70, cancel:0.50} (combat.js:145) as fractions of the clip; st.t/st.dur is the clip fraction. Use isIdle() for the dark↔hot base and optionally the attack-window fraction to ramp the gain up/down (INFERRED).

Particles (06-01): glowGain(isIdle, { rest, hot }) -> number (INFERRED rule; isIdle ? rest : hot). rest is a small value (dark), hot > 1.0 (bright streak).

alpha-over-1.0 fix (03-02 carry-forward / CLAUDE.md Part 1): to make the glow BRIGHTER than the 1.0 clamp, switch the glow draw to the additivePremult mode (06-04, blendFunc ONE,ONE) and, in the fxProg fragment, output rgb premultiplied by (alpha128 × uGlowGain) so glowGain > 1.0 exceeds the clamp — the data-grounded brightness recovery (NOT a hand-tuned color multiplier; the color is still the decoded texel).

CNG (D-08 / Phase 5): FXC_CNGemit→PTC_CNGpart is a name-confirmed pair, but it is STANDALONE-only (absent from the WAD, Pitfall 1) and is therefore NOT built into the runtime `db` (which uses NO 3rd arg — see 06-05) and NOT consumed by this slice at runtime. Do NOT add an async standalone fetch for it here (checker guidance). The name-confirmed CNG pair is asserted in 06-02 fxdb.test.js (in-test standalone load). There is NO decoded state-gate field (verified) — so the dark↔hot RULE is INFERRED. The glow COLOR/blend stays the in-WAD decoded chainglowTex (already loaded, real texels, identity material/blend — Phase 3); the runtime uses that texture, not any db.fxc CNG entry.

fxProg fragment (app.js:357-370): add a `uGlowGain` uniform; when the glow premult branch is active, gl_FragColor.rgb = texel.rgb * (alpha128 * uGlowGain). Ensure non-glow passes set uGlowGain such that they are unaffected (e.g. 1.0 with the premult branch off). (Rationale — WARNING-6, non-blocking: the fxProg fragment now carries two parallel per-effect flags, uTrailRamp (06-03) and uGlowGain (06-07); each pass resets its own flag. The explicit-reset pattern is intentional and correct. Consolidating to a single uFxMode enum uniform is DEFERRED — the flags are introduced in separate waves and a cross-wave shader-contract refactor is not cheap; keep the explicit resets.)

Script versioning: bump ALL <script> ?v= tags in lockstep to the next integer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combat-gated glow gain + alpha-over-1.0 premult brightness on the chainglow pass (CHAIN-03)</name>
  <files>tools/kratos-lab/app.js</files>
  <read_first>
    - tools/kratos-lab/app.js:649-702 — drawFx PASS 1/PASS 2 (the chainglow overlay to gate/brighten) + the fxProg uniforms.
    - tools/kratos-lab/app.js:316-370 — fxProg fragment (where uGlowGain + the premult branch land).
    - tools/kratos-lab/combat.js:144-174 — isIdle() + windows{queue,branch,cancel} + st.t/st.dur (the gating source).
    - tools/kratos-lab/particles.js — glowGain contract (06-01).
    - tools/kratos-lab/fx.js:24-55 — the additivePremult mode (06-04) + applyMaterial.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "CHAIN-03" (no decoded gate → INFERRED rule) + § "Pattern 3/5" (premult + combat-gated) + § "Assumptions Log" A2.
    - .planning/phases/03-chain-link-ribbon-glow/03-02-SUMMARY.md — the glow-intensity lever + the coplanar LEQUAL overlay to preserve.
  </read_first>
  <action>
    In the fxProg FRAGMENT shader, add a `uGlowGain` uniform and a premult branch: when the glow premult flag is set, output `gl_FragColor = vec4(texel.rgb * (alpha * uGlowGain), 0.0)` (alpha carrying the texel's alpha128; gain > 1.0 exceeds the 1.0 clamp — the alpha-over-1.0 recovery, CLAUDE.md Part 1). Wire `uGlowGain` into fxLocs. In `drawFx`'s chainglow PASS 2 (app.js:671-685): compute `const gain = Particles.glowGain(machine.isIdle(), { rest: GLOW_REST, hot: GLOW_HOT })` where GLOW_REST (dark, small) and GLOW_HOT (>1.0, bright streak) are INFERRED constants — label them and cite A2/D-05; optionally ramp `gain` by the attack-window fraction (`st.t/st.dur` vs windows.branch) for a smoother flare (INFERRED). Switch the chainglow draw to `Fx.applyMaterial(gl, { name:'MAT_chainglow', mode:'additivePremult', disableDepthWrite:true })` (or synthesize a mat-like carrying mode additivePremult so the assert-on-unknown contract holds), set `uGlowGain = gain`, keep the SAME chainglowTex + the SAME chain vertex bytes (no re-upload — bit-identical depth) + the LEQUAL coplanar overlay. Ensure PASS 1 (links) and the trail/pool passes set `uGlowGain`/premult-flag so they are NOT affected by the glow branch. Append to fxLog. Do NOT hand-pick a glow color — the color is the decoded chainglow texel (Pitfall 4); only the gain rule + constants are INFERRED.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq 'glowGain\(machine\.isIdle\(\)|glowGain\(' tools/kratos-lab/app.js && grep -Eq 'uGlowGain' tools/kratos-lab/app.js && echo GLOW-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0 (glowGain isIdle→rest / active→hot contract green).
    - `app.js` computes `Particles.glowGain(machine.isIdle(), ...)` in the chainglow pass and sets a `uGlowGain` uniform (grep both).
    - The chainglow draw uses `additivePremult` (grep) and keeps the SAME chain vertex bytes (no new bufferData for PASS 2) + the LEQUAL overlay (grep — depthFunc LEQUAL / coplanar comment preserved).
    - GLOW_REST and GLOW_HOT are INFERRED constants with A2/D-05 comments; GLOW_HOT > 1.0 (grep the constants).
    - No hand-picked glow RGB is introduced (grep review — glow color is the decoded texel, Pitfall 4).
    - PASS 1 / trail / pool passes are unaffected by the glow branch (grep — uGlowGain reset/flag off for them).
  </acceptance_criteria>
  <done>The chainglow overlay is gated dark↔hot by combat state (Particles.glowGain, INFERRED rule) and brightened via the alpha-over-1.0 premult path (additivePremult ONE,ONE, gain>1.0) using the decoded chainglow texel color; the coplanar LEQUAL overlay + bit-identical depth are preserved. The 03-02 "glow too subtle" lever is closed data-grounded.</done>
</task>

<task type="auto">
  <name>Task 2: State-leak guard verification + ?v bump</name>
  <files>tools/kratos-lab/app.js, tools/kratos-lab/index.html</files>
  <read_first>
    - tools/kratos-lab/app.js:1111-1137 — the KratosLab.fxState() hook (the between-frames clean-check).
    - tools/kratos-lab/app.js:687-702 — the end of drawFx (restoreFxState) — confirm the glow premult mode is restored.
    - tools/kratos-lab/index.html:115-122 — the lockstep ?v tags.
    - .planning/phases/06-particle-runtime-fire-sparks-trails/06-RESEARCH.md § "Pitfall 3" (state leak) + § "Anti-Patterns" (restore after the pass).
  </read_first>
  <action>
    Confirm the glow premult pass does not leak state: `drawFx` must still end with `Fx.restoreFxState(gl)` (depthMask true, blend off, FUNC_ADD) after all passes including the gated glow. Verify `KratosLab.fxState()` would report a clean state between frames (blendEnabled false, FUNC_ADD, depthMask true) — the existing hook already exposes this; ensure no new pass leaves the premult mode set. If the glow premult introduced a `uGlowGain`/flag that other passes read, ensure it is reset at the top of drawFx so a mid-phase render is deterministic. Then bump ALL index.html `?v=` tags in lockstep to the next integer.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/particles.test.js && grep -Eq 'restoreFxState' tools/kratos-lab/app.js && ! grep -q '?v=23' tools/kratos-lab/index.html && echo LEAK-GUARD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/particles.test.js` exits 0.
    - `drawFx` still ends with `Fx.restoreFxState(gl)` after the gated glow pass (grep).
    - The `uGlowGain`/premult flag is reset at the top of drawFx (grep — deterministic per-frame state).
    - `index.html` ?v tags all bumped in lockstep (no `?v=23` remains).
    - Full suite regression: all eight suites exit 0.
  </acceptance_criteria>
  <done>The gated-glow pass leaves no GL state leak (restoreFxState intact, premult flag reset per frame, fxState() clean between frames); index.html ?v bumped in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combat state → glow gain uniform | isIdle()/window fraction drives the gain; must stay finite and bounded (from the tested Particles.glowGain) |
| glow premult pass → shared GL state | additivePremult + uGlowGain must be restored/reset so later passes and the next frame are unaffected (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Denial of Service (state leak) | glow premult mode / uGlowGain leaking into other passes or next frame | mitigate | Reset flag at drawFx top; restoreFxState at end; fxState() clean-check |
| T-06-07-02 | Integrity (data-first) | hand-tuning glow color instead of decoded texel | mitigate | Glow color = decoded chainglow texel (identity material/blend); only the gain rule + constants INFERRED (Pitfall 4) |
| T-06-07-SC | Tampering | supply chain | accept | Zero packages; classic <script> tags only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/particles.test.js` green (glowGain rule).
- Full suite (all eight) green — no regression.
- Manual (Phase-7 harness): glow dark at idle, hot streak during attacks/throws; brighter than the 03-02 clamped result; fxState() clean; magenta-bg additive add.
</verification>

<success_criteria>
- Chain glow is dark at rest, hot during attack/throw windows (Particles.glowGain, INFERRED rule, labeled).
- Brightness recovered via alpha-over-1.0 premult (additivePremult, gain>1.0) — data-grounded, not hand-tuned.
- Glow color stays the in-WAD decoded chainglow texel (chainglowTex); coplanar LEQUAL overlay + bit-identical depth preserved. (CNG standalone-only; test-only corroboration, not a runtime input.)
- No state leak; fxState() clean between frames.
</success_criteria>

<output>
Create `.planning/phases/06-particle-runtime-fire-sparks-trails/06-07-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 06-CONTEXT.md decisions:
- D-01: chain-glow state gating is slice 4.
- D-05: glow driven by combat state (dark↔hot); rule INFERRED (no decoded gate, verified Phase 5); alpha-over-1.0 premult brightness fix, not hand-tuning.
- D-07: blend via Fx.applyMaterial (additivePremult mode); no hardcoded blendFunc.
- D-08: CNG name-confirmed pair asserted in-test (standalone-only, NOT a runtime dependency); the glow color is the in-WAD decoded chainglow texel, not fabricated.
</decisions_honored>
