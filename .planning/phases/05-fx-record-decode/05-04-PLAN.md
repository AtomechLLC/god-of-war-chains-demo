---
phase: 05-fx-record-decode
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - tools/kratos-lab/fxparse.js
  - tools/kratos-lab/test/fxdb.test.js
autonomous: false
requirements: [DEC-02]
user_setup: []

must_haves:
  truths:
    - "The GFX_swordtrail texture is inspected and confirmed to carry NO length-wise painted age→color ramp (uniform additive streak) — so the trail ramp is runtime, tagged INFERRED"
    - "Effect color is traced to MAT_pticleMat.blendColor via buildMats and recorded in FxDb as the color-provenance source (DEC-02 'colors come from these records' closed honestly)"
    - "The remaining priority-corpus records decode into FxDb via the existing generic decoders: WAD-native flame5/EG through the WAD loop, and the standalone-only FXCF + CNG (fire + chain-glow) through the standaloneRecs 3rd arg — FXC_CNGemit/PTC_CNGpart/FXC_FXCFemit/PTC_FXCFpart become real keys in db.fxc/db.ptc"
    - "Every DEC-02 field in FxDb carries either a real byte-exact known-answer or an explicit INFERRED label with a corroboration note"
    - "Region NTSC-U / 60Hz tick basis is recorded in FxDb.meta as the rate/lifetime interpretation basis (D-05)"
  artifacts:
    - path: "tools/kratos-lab/fxparse.js"
      provides: "buildFxDb color-provenance + full-corpus coverage (WAD + standalone) + meta closeout"
      contains: "pticleMat"
    - path: "tools/kratos-lab/test/fxdb.test.js"
      provides: "swordtrail no-ramp proof + MAT_pticleMat color trace + corpus-key completeness (BFT/BGT/CNG/FXCF actual keys) + evidence-tag audit"
      contains: "GFX_swordtrail"
  key_links:
    - from: "tools/kratos-lab/fxparse.js buildFxDb"
      to: "FxParse.buildMats (MAT_pticleMat)"
      via: "color provenance trace"
      pattern: "pticleMat"
    - from: "tools/kratos-lab/test/fxdb.test.js"
      to: "Parsers.decodeTexture (GFX_swordtrail)"
      via: "no-length-ramp proof"
      pattern: "swordtrail"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** Phase-6 particle runtime (and the developer wiring it), **I want to** query the game's real decoded particle / emitter / shape values from a JSON-dumpable `FxDb`, **so that** fire, sparks, and trails render from authentic GoW1 data instead of hand-tuned approximations.

This plan is the **DEC-02 closeout**: it resolves where effect COLOR actually lives (RESEARCH found it is NOT in PTC/texture), extends the FxDb to the full priority corpus (fire + chain-glow, standalone families via the 3rd arg), audits that every field is real-or-INFERRED, and folds in the optional PCSX2 GS-dump corroboration (D-06).
</phase_goal>

<objective>
Close the "colors/rates/sizes/lifetimes come from these records" clause of DEC-02 honestly: trace effect color to `MAT_pticleMat.blendColor` (RESEARCH Open Q1 — it is NOT a static PTC field and the `GFX_swordtrail` texture has no painted ramp), extend `FxDb` to the remaining priority families (fire: FXCF/flame5/EG; chain-glow: CNG) so they are ACTUAL keys, and audit that every decoded field is real-or-INFERRED. Optionally fold a GS-dump ABCD/color reading in as HIGH-confidence corroboration (D-06).

Purpose: Slice-1 (trails+sparks) shipped in 05-03; this plan adds slice-2 (fire) + the deferred-but-in-corpus chain-glow, and makes the data-first color story truthful instead of fabricated.
Output: `buildFxDb` color-provenance + full-corpus coverage (WAD + standalone), extended `test/fxdb.test.js`, and the GS-dump decision recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-fx-record-decode/05-RESEARCH.md
@.planning/phases/05-fx-record-decode/05-CONTEXT.md
@.planning/phases/05-fx-record-decode/05-03-SUMMARY.md
@tools/kratos-lab/fxparse.js
@tools/kratos-lab/parsers.js

<interfaces>
<!-- Contracts from prior slices + codebase, verified this session. -->

FxParse.buildFxDb(records, wadBuf, standaloneRecs=[]) / parseFxc / parsePtc / parseMsh — available (extend buildFxDb's coverage; the standaloneRecs 3rd arg was added in 05-02/03).
  The standalone-only families (BFT/BGT from 05-02/03; CNG/FXCF here) enter db.ptc/db.fxc via the standaloneRecs 3rd arg — the caller loads assets/kratos/fx/*.bin and passes [{name, buf, tag:0x1e}]. WAD-native families (flame5, EG) come through the existing WAD loop (Pitfall 1: CNG/FXCF are absent from the WAD).
FxParse.buildMats(records, buf) -> { byName, list }; each mat has { blendColor:[r,g,b,a], materialColor:[r,g,b], mode, ... }.
  MAT_pticleMat (in-WAD @0x134c0, size 120, magic 0x8) is the particle material — its blendColor is the color-provenance candidate.
  MAT_swordtrail (in-WAD @0x28880) — additive; rawFlags0 0x48090080 (already pinned in wad.test.js).
Parsers.decodeTexture(gfxSubarray, palSubarray) -> ImageData(w,h)  (needs the ImageData shim, already in fxdb.test.js).
  GFX_swordtrail: resolve TXR_swordtrail (@0x287e0) → GFX_swordtrail / PAL_swordtrail; decode and inspect for a length-wise ramp.

Remaining priority-corpus records (WAD-native + standalone), all handled by the existing generic decoders:
  fire:  FXC_FXCFemit (.bin, subtype 0x2, ref "FXCFpartShape"), PTC_FXCFpart (.bin), PTC_flame5 (in-WAD @0x1eaa0, 568 B), FXC_EGemit/PTC_EGpart/FXC_EGgrav (in-WAD)
  chain-glow: FXC_CNGemit (.bin, subtype 0x2, slot 0x1, ref "CNGpartShape"), PTC_CNGpart (.bin, 520 B, slot 0x1)
  NOTE: CNG/FXCF are standalone-ONLY (Pitfall 1) — they enter the FxDb via the standaloneRecs 3rd arg exactly like BFT/BGT; flame5/EG are WAD-native.
RESEARCH finding (verified): GFX_swordtrail has NO length-wise age→color ramp — it is a uniform soft additive streak (like chainglow). The white-hot→orange→ember ramp is therefore a RUNTIME tint → INFERRED (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: swordtrail no-ramp proof + MAT_pticleMat color-provenance trace</name>
  <files>tools/kratos-lab/test/fxdb.test.js, tools/kratos-lab/fxparse.js</files>
  <read_first>
    - tools/kratos-lab/test/wad.test.js:336-389 — the chainglow texture-inspection idiom (decodeFxTexture helper + per-column scans) to mirror for GFX_swordtrail.
    - .planning/phases/05-fx-record-decode/05-RESEARCH.md § "Open Questions" Q1 (color source) + § "Common Pitfalls → Pitfall 4" + § "State of the Art" (swordtrail no length-ramp finding).
    - tools/kratos-lab/parsers.js:242-280 — decodeTexture (the CLUT 0x80-alpha rule) and fxparse.js buildMats (MAT blendColor decode).
  </read_first>
  <action>
    In `tools/kratos-lab/test/fxdb.test.js`, add a `// --- swordtrail no-ramp + color provenance ---` block. Decode GFX_swordtrail via the decodeFxTexture idiom (resolve TXR_swordtrail from MAT_swordtrail's idx → GFX/PAL → decodeTexture). Assert the texture carries NO length-wise (U-axis) age→color ramp: compute per-column mean luminance and assert it does NOT vary monotonically white→ember across U (i.e. the bright region is confined / uniform, not a painted gradient — mirror the chainglow "single hot spot" logic). This pins RESEARCH's finding that the ramp is runtime.
    Then trace color to the material: `const mats = FxParse.buildMats(recs, buf)` and assert `mats.byName["MAT_pticleMat"]` exists with a decodable `blendColor`. Assert buildFxDb records a color-provenance note: `db.meta.colorSource` (or `db.ptc[...].colorSource`) === "MAT_pticleMat.blendColor" tagged INFERRED-for-runtime-tint, and that NO FxDb field claims a `real` crimson/ember color from a PTC or the swordtrail texture.
    In `tools/kratos-lab/fxparse.js`, extend `buildFxDb` minimally to populate the color-provenance field: decode MAT_pticleMat via the existing buildMats (or read its blendColor), and record `meta.colorSource = { record:"MAT_pticleMat", field:"blendColor", value:[...], tag:"real", note:"runtime tint applies this; age→color ramp is INFERRED (no painted texture ramp, no static PTC RGBA)" }`. Do not fabricate a per-effect color.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/fxdb.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/fxdb.test.js` exits 0 with the swordtrail-no-ramp and MAT_pticleMat color-provenance assertions green.
    - `grep -c 'GFX_swordtrail' tools/kratos-lab/test/fxdb.test.js` ≥ 1 and `grep -c 'pticleMat' tools/kratos-lab/fxparse.js` ≥ 1.
    - `db.meta.colorSource.record === "MAT_pticleMat"` (assert via a one-line `node -e` that builds the FxDb and checks the field).
    - No FxDb field is a `real`-tagged effect color sourced from a PTC or the swordtrail texture (grep the test's provenance guard).
  </acceptance_criteria>
  <done>swordtrail confirmed ramp-free; effect color traced to MAT_pticleMat.blendColor and recorded as the (runtime-applied) provenance; no fabricated color anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: full priority-corpus coverage (fire + chain-glow as actual keys) + real/INFERRED evidence audit</name>
  <files>tools/kratos-lab/fxparse.js, tools/kratos-lab/test/fxdb.test.js</files>
  <behavior>
    - buildFxDb covers the fire family (WAD: PTC_flame5/EG; standalone via 3rd arg: FXC_FXCFemit/PTC_FXCFpart) and chain-glow (standalone via 3rd arg: FXC_CNGemit/PTC_CNGpart) via the existing generic decoders — no new decoder code. BFT/BGT/CNG/FXCF are ACTUAL keys in db.ptc/db.fxc (not merely proven in isolation).
    - Every field in every FxDb entry has an evidence entry tagged "real" or "INFERRED"; INFERRED entries carry a non-empty corroboration note.
    - The FxDb still JSON.stringify-round-trips with the full corpus.
  </behavior>
  <read_first>
    - tools/kratos-lab/fxparse.js — the buildFxDb assembled in 05-01..05-03 (extend the WAD record-filter loops + the standaloneRecs branch to include the remaining families; the decoders are generic already).
    - .planning/phases/05-fx-record-decode/05-RESEARCH.md § "Validation Architecture → Phase Requirements → Test Map" (the DEC-02 rows) + § "FxDb Shape" (evidence rules).
    - .planning/phases/05-fx-record-decode/05-03-SUMMARY.md — current FxDb coverage.
  </read_first>
  <action>
    In `tools/kratos-lab/test/fxdb.test.js`, add a `// --- full priority corpus + evidence audit ---` block. Build the FULL standalone source (`standaloneRecs` = loadBin for BFT/BGT/CNG/FXCF — both the PTC_* and FXC_* names: PTC_BFTpart1, PTC_BGTpart1, PTC_CNGpart, PTC_FXCFpart, FXC_BFTemit1, FXC_BGTemit1, FXC_CNGemit, FXC_FXCFemit) and call `FxParse.buildFxDb(recs, buf, standaloneRecs)`. Assert the WAD-native `db.ptc["PTC_flame5"]` (and an EG entry) exist with byte-exact magic/subtype/size/shapeRef. CORPUS-KEY ASSERTION (the plan-04 Blocker fix): assert `db.fxc["FXC_CNGemit"]`, `db.ptc["PTC_CNGpart"]` (slot 0x1, ref "CNGpartShape"), `db.fxc["FXC_FXCFemit"]`, `db.ptc["PTC_FXCFpart"]` (ref "FXCFpartShape"), AND `db.fxc["FXC_BFTemit1"]`, `db.ptc["PTC_BFTpart1"]`, `db.fxc["FXC_BGTemit1"]`, `db.ptc["PTC_BGTpart1"]` are ALL ACTUAL KEYS in the built FxDb — not merely proven in isolation via loadBin. Add an EVIDENCE-COMPLETENESS AUDIT: walk every entry of `db.msh`, `db.ptc`, `db.fxc`; for each, assert `evidence` is a non-empty array and every entry has `tag` ∈ {"real","INFERRED"}, and that every INFERRED entry has a non-empty `corrob`/`note` string (the data-first discipline). Assert `JSON.stringify(db)` round-trips.
    In `tools/kratos-lab/fxparse.js`, extend `buildFxDb`'s record-filter/standalone loops so the FXC/PTC coverage admits the fire + chain-glow + EG families (WAD-native flame5/EG already match the `FXC_`/`PTC_` prefixes in the WAD loop; standalone CNG/FXCF flow through the standaloneRecs branch added in 05-02/03 — verify no family is skipped; the generic parseFxc/parsePtc handle subtypes 0x2/0xc). If any decoded field currently lacks an evidence entry, add one (real for byte-decoded, INFERRED with a note for runtime/semantic-inferred like tick-meaning A2 or texFormat A1). Do NOT special-case any family with hand-tuned values.
    Record in the SUMMARY the multi-source coverage: the roadmap SC2 named corpus {flame3, flame6, BDEsparkemit, BFT, BGT} plus the slice-2 CNG/FXCF are ACTUAL keys in FxDb with cross-refs — all present by end of this plan; BFT/BGT/CNG/FXCF via the standalone 3rd arg, flame*/BDEsparkemit/EG via the WAD.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/fxdb.test.js && node tools/kratos-lab/test/wad.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/fxdb.test.js` exits 0 (fire + chain-glow corpus + evidence audit green) AND `node tools/kratos-lab/test/wad.test.js` exits 0.
    - The evidence audit asserts EVERY FxDb entry has a non-empty evidence array with only real/INFERRED tags (grep the audit loop).
    - `node -e "const F=require('./tools/kratos-lab/fxparse.js'),P=require('./tools/kratos-lab/parsers.js'),fs=require('fs');const b=new Uint8Array(fs.readFileSync('assets/wads/R_WPN0_0.WAD'));const L=n=>({name:n,tag:0x1e,buf:new Uint8Array(fs.readFileSync('assets/kratos/fx/'+n+'.bin'))});const sr=['PTC_BFTpart1','PTC_BGTpart1','PTC_CNGpart','PTC_FXCFpart','FXC_BFTemit1','FXC_BGTemit1','FXC_CNGemit','FXC_FXCFemit'].map(L);const db=F.buildFxDb(P.parseWad(b),b,sr);['PTC_flame3','PTC_flame6','FXC_BDEsparkemit','PTC_BFTpart1','PTC_BGTpart1','FXC_BFTemit1','FXC_BGTemit1','FXC_CNGemit','PTC_CNGpart','FXC_FXCFemit','PTC_FXCFpart'].forEach(k=>{if(!db.ptc[k]&&!db.fxc[k])process.exit(1)})"` exits 0 (the SC2-named fire+spark+trail corpus AND the slice-2 CNG/FXCF are ALL actual keys in the built FxDb — not merely proven in isolation).
    - Full suite (`for f in tools/kratos-lab/test/*.test.js; do node "$f" || exit 1; done`) exits 0.
  </acceptance_criteria>
  <done>The full priority corpus (trails+sparks from 05-03, fire + chain-glow here) is in FxDb as actual keys; every field is real-or-INFERRED with corroboration; DEC-02's color clause is closed; full suite green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide whether to fold in PCSX2 GS-dump blend/color corroboration (D-06)</name>
  <files>none (decision only — recorded in the SUMMARY)</files>
  <action>
    Present the decision and options below to the user and WAIT for a selection. This checkpoint is placed AFTER the decode is green (Tasks 1-2) so it never blocks shipping the FxDb — it only decides whether to add the optional HIGH-confidence GS-dump corroboration (D-06 is RECOMMENDED, not blocking). On "capture", record the RESEARCH § "PCSX2 GS-Dump Procedure" reference in the SUMMARY as a follow-up gap-closure item; on "skip", record the MEDIUM-confidence disposition and close DEC-02.
  </action>
  <decision>Can a PCSX2 GS dump of a Level-1 blade swing be captured to corroborate per-effect blend/color? (D-06 — NEEDS USER CONFIRMATION)</decision>
  <context>
    The decode is COMPLETE and green without a GS dump — differential decode + Phase-1 footage anchors + cross-record consistency are the primary corroboration (D-06: GS dump is RECOMMENDED, NOT blocking). A GS dump would upgrade the per-effect blend-config confidence from MEDIUM to HIGH by reading the actual ALPHA (ABCD/FIX), TEX0 (TFX), TEST, and ZBUF registers per FX draw, and would independently reconfirm the NTSC-U region from the dump header's embedded game serial (SCUS-97399). This decision does not gate shipping the FxDb — it only decides whether to fold in the extra HIGH-confidence corroboration.
  </context>
  <options>
    <option id="capture">
      <label>Yes — capture a GS dump</label>
      <pros>Upgrades blend/color confidence MEDIUM→HIGH; resolves the ZTST/LEQUAL A1 assumption carried from 03-02; independent region reconfirmation</pros>
      <cons>Requires PCSX2 ≥ 2.0 + the NTSC-U disc on the user's machine; a follow-up gap-closure task folds the ABCD reads into the FxDb evidence as `corrob:"GS-dump ABCD=…"`</cons>
    </option>
    <option id="skip">
      <label>No — proceed with differential + footage corroboration</label>
      <pros>Zero setup; decode already green and JSON-dumpable; matches the decode-first plan</pros>
      <cons>Per-effect blend confidence stays MEDIUM (documented); Phase 6 uses the MAT-decoded blend already pinned in DEC-01</cons>
    </option>
  </options>
  <resume-signal>Select: capture or skip. If capture, the executor records the GS-dump procedure reference (RESEARCH § "PCSX2 GS-Dump Procedure") in the SUMMARY as a follow-up; if skip, records the MEDIUM-confidence disposition and closes DEC-02.</resume-signal>
  <done>The D-06 GS-dump decision is made and recorded in the SUMMARY; the decode ships regardless of the choice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disc-derived bytes → decoders | Same untrusted-binary boundary as prior slices (size-gated, rec.size-bounded) |
| GFX/PAL bytes → decodeTexture | Texture decode reads size-bounded subarrays (g.dataOff..+g.size); no OOB |
| assets/ fetch policy | GFX_swordtrail / PAL_swordtrail / MAT_pticleMat read from `assets/` only; never `extracted/` or ISO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Integrity (data-first) | inventing a crimson/ember effect color | mitigate | swordtrail-no-ramp proof + MAT_pticleMat provenance + evidence audit: color is INFERRED runtime tint, never a fabricated `real` field |
| T-05-02 | Tampering | decodeTexture OOB on a short GFX/PAL | mitigate | Read size-bounded subarrays `buf.subarray(dataOff, dataOff+size)` (existing decodeFxTexture idiom) |
| T-05-05 | Info-disclosure | asset-path widening beyond assets/ | mitigate | All reads resolve under `assets/`; no `extracted/`/ISO paths introduced |
| T-05-SC | Tampering | supply chain | accept | Zero packages — node:assert + built-ins only |
| T-05-GS | Repudiation | GS-dump provenance (region/blend claim) | accept | GS dump is RECOMMENDED not blocking; if captured, its embedded SCUS-97399 serial self-attests region — recorded as corroboration, not a trust anchor |
</threat_model>

<verification>
- `node tools/kratos-lab/test/fxdb.test.js` green (swordtrail-no-ramp + color provenance + full corpus + evidence audit).
- `node tools/kratos-lab/test/wad.test.js` green (regression guard).
- Full suite green: `for f in tools/kratos-lab/test/*.test.js; do node "$f" || exit 1; done`.
- GS-dump decision recorded in the SUMMARY (capture follow-up OR skip disposition).
</verification>

<success_criteria>
- GFX_swordtrail confirmed to have no painted length-wise ramp; effect color traced to MAT_pticleMat.blendColor (runtime tint = INFERRED).
- The full priority corpus (trails BFT/BGT, sparks, fire flame*/FXCF, chain-glow CNG) is in a JSON-dumpable FxDb as ACTUAL keys (standalone families via the 3rd arg) with cross-refs.
- Every FxDb field is real (byte-exact known-answer) or INFERRED (labeled + corroborated) — DEC-02 evidence-completeness holds.
- The D-06 GS-dump decision is made and recorded; decode does not stall on it.
</success_criteria>

<output>
Create `.planning/phases/05-fx-record-decode/05-04-SUMMARY.md` when done — include the multi-source coverage note (all five SC2-named records + CNG/FXCF present as ACTUAL FxDb keys) and the GS-dump decision.
</output>
