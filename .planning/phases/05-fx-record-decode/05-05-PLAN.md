---
phase: 05-fx-record-decode
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - tools/kratos-lab/fxparse.js
  - tools/kratos-lab/test/anm.test.js
autonomous: true
requirements: [DEC-03]
user_setup: []

must_haves:
  truths:
    - "The type-5 ANM blade-state descriptor record is located in R_WPN0_0.WAD and its framing is byte-diffed against mogaika's anm/ layout as the cross-reference"
    - "The type-5 identity is CONFIRMED before any seed is pinned (the candidate records' first u16 is 0x1/0x3, NOT a literal 5 — 'type-5' is the ANM subtype label, not a magic value)"
    - "parseAnmType5 decodes the show/hide blade-state fields (in-hand during combat vs on-back out of combat) with a per-field evidence table"
    - "The decoded descriptor exposes a queryable state → visibility result the Phase-6 blade presentation can gate on, JSON-dumpable"
    - "Every decoded field is tagged real (byte-exact) or INFERRED (labeled + corroborated); an unconfirmable field is escalated or the wave aborts with a recorded gap — never a fabricated seed"
    - "A bad/short ANM record throws a named error"
  artifacts:
    - path: "tools/kratos-lab/fxparse.js"
      provides: "FxParse.parseAnmType5 (blade-state descriptor decode)"
      contains: "parseAnmType5"
    - path: "tools/kratos-lab/test/anm.test.js"
      provides: "type-5 ANM known-answer suite seeded from the differential-decode findings"
      contains: "parseAnmType5"
  key_links:
    - from: "tools/kratos-lab/test/anm.test.js"
      to: "FxParse.parseAnmType5"
      via: "byte-exact known-answers on the located record"
      pattern: "parseAnmType5"
    - from: "tools/kratos-lab/fxparse.js parseAnmType5"
      to: "Parsers.parseWad / resolve"
      via: "WAD record access for the blade ANM"
      pattern: "resolve|parseWad"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** Phase-6 particle runtime (and the developer wiring it), **I want to** query the game's real decoded particle / emitter / shape values from a JSON-dumpable `FxDb`, **so that** fire, sparks, and trails render from authentic GoW1 data instead of hand-tuned approximations.

This plan covers the **deferred DEC-03 slice** (D-07): the type-5 ANM blade-state descriptor — blade *presentation* (on Kratos's back out of combat, in hands during combat), NOT particles. It is sequenced LAST so it never blocks the DEC-02 payoff data. Decode only — the rendering that consumes it is Phase-6 blade work.
</phase_goal>

<objective>
Decode the type-5 ANM descriptor that drives blade show/hide state into a queryable, JSON-dumpable result with a per-field evidence table, using the differential-decode protocol against mogaika's `pack/wad/anm/` layout as the cross-reference.

Purpose: DEC-03 is required for phase-requirement coverage but deferred to last (blade presentation, not the fire/trail payoff). RESEARCH notes it is lower-risk RE than FXC/PTC/MSH because mogaika's `anm/` decoders exist as a public reference (unlike the FX records). No byte-exact seeds were captured this session, so this plan establishes them via differential decode, then pins them — but only after CONFIRMING the type-5 identity (see the escalation/abort protocol in Task 1; a seed is never invented to force a green).
Output: `FxParse.parseAnmType5`, a new `test/anm.test.js` known-answer suite, and the decoded blade-state descriptor — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-fx-record-decode/05-RESEARCH.md
@.planning/phases/05-fx-record-decode/05-CONTEXT.md
@tools/kratos-lab/fxparse.js
@tools/kratos-lab/parsers.js

<interfaces>
<!-- Contracts + candidate records located this session. -->

Parsers.parseWad(buf) -> records[{ idx, off, tag, size, name, dataOff }]; Parsers.resolve(records, name, fromIdx).
FxParse.readName + the parseTxr size-gate→magic→named-reads idiom (mirror it).

Candidate blade-ANM records in R_WPN0_0.WAD (located this session — the executor differentially confirms which carries the type-5 blade-state descriptor):
  ANMX_R_Wpn      tag 0x1e  off 0x540    size 48    u16@0=0x3
  ANM_maigodblade tag 0x1e  off 0x144a0  size 644   u16@0=0x3   (god tier — the differential PAIR against the level-1 blade ANM)
  maiblade        tag 0x1e  off 0xd160   size 672   u16@0=0x1   (level-1 blade instance descriptor candidate)
  gomaiblade      tag 0x1e  off 0xd580   size 92    u16@0=0x1
  maigodblade / gomaigodblade — god-tier counterparts for stage1-vs-god differential (RESEARCH differential protocol)

NOTE (type-5 identity — confirm before pinning seeds): the candidate records' first u16 is 0x1 or 0x3 (see the `u16@0=` column above), NOT a literal 5. "type-5" is the ANM subtype/category label from mogaika's anm/ taxonomy, not a magic value at +0x00. The executor MUST confirm which located record genuinely IS the type-5 blade-state descriptor (via the differential pass + the mogaika anm/ cross-ref) BEFORE pinning any byte-exact RED seed — do not assume a record is type-5 because a field reads 5, and do not pin a seed on an unconfirmed record.

Cross-reference: mogaika/god_of_war_browser pack/wad/anm/ decodes ANM (incl. type8); the type-5 subtype has partial public precedent there (RESEARCH A7). Use it to interpret the located record's framing — do NOT copy code, read the layout.
Region: NTSC-U 60Hz (D-05) — any timing field in the descriptor is 60Hz ticks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Differential-locate + confirm + frame the type-5 ANM blade-state descriptor (evidence table)</name>
  <files>tools/kratos-lab/test/anm.test.js</files>
  <read_first>
    - .planning/phases/05-fx-record-decode/05-RESEARCH.md § "Differential-Decode Protocol" (byte-diff framing, stage1-vs-god pair-diff) + § "Phase Requirements" (DEC-03 row, A7) + § "ELF-Disassembly Escalation Path" (tiebreaker for a resistant field).
    - tools/kratos-lab/test/wad.test.js:1-114 — the scaffold (WAD read, parseWad, resolve, synthetic throw) to copy for a new suite.
    - tools/kratos-lab/parsers.js:288-324 — parseWad/resolve for locating the blade ANM records.
  </read_first>
  <action>
    Create `tools/kratos-lab/test/anm.test.js` (new suite), node:assert only, header naming `node tools/kratos-lab/test/anm.test.js` + DEC-03. Read the WAD via the wad.test.js pattern.
    Differential-locate the type-5 blade-state descriptor: byte-diff the level-1 blade ANM candidate (`maiblade` / `ANMX_R_Wpn`) against its god-tier counterpart (`maigodblade` / `ANM_maigodblade`) to separate constant framing from the varying show/hide payload (RESEARCH differential protocol step 1-2). Identify the field(s) that encode blade visibility state (on-back vs in-hand) — corroborate the framing against mogaika's `pack/wad/anm/` type-5 layout (A7). Record the recovered offsets, raw bytes, interpretation, corroboration source, and a real/INFERRED tag as a per-field evidence table in a top-of-file comment block (the RESEARCH per-field-table format).
    Write the RED known-answers that PIN the recovered byte-exact values (these become the seeds RESEARCH did not pre-supply): the record's located off/size, the ANM subtype/magic identifying it as type-5, and at least one show/hide state field's byte-exact value. These assertions reference `FxParse.parseAnmType5` and MUST fail RED (decoder not exported yet).

    ESCALATION / ABORT PROTOCOL (Warning 2 — this plan discovers its own seeds at execution time; DEC-03 is deferred / last-wave / non-blocking): FIRST confirm the type-5 identity — the candidate records' first u16 is 0x1/0x3, NOT literal 5, so establish WHICH record genuinely carries the blade-state descriptor via the differential pass + the mogaika anm/ cross-ref BEFORE pinning any RED seed. If differential-locate CANNOT confirm a genuine type-5 blade-state field within this wave: (1) escalate to the mogaika `pack/wad/anm/` type-5 layout cross-ref to interpret the framing; (2) if it still resists, INFERRED-label the unresolved field(s) with a corroboration note, OR abort the wave and record the gap in the SUMMARY — do NOT fabricate byte-exact assertions for an unconfirmed field. For a look-critical visibility field that resists both, note the ELF-escalation path (RESEARCH § "ELF-Disassembly Escalation Path", D-04) in the evidence comment as the tiebreaker. Pin only what genuinely decodes; tag the rest INFERRED with a corroboration note. This plan never blocks the DEC-02 payoff (it is last-wave) and never invents a seed to force a green.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/anm.test.js 2>&1 | grep -Eq 'parseAnmType5|is not a function|TypeError' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/anm.test.js` exits NON-zero (RED) naming `parseAnmType5`.
    - The file contains a per-field evidence table comment (offset / raw bytes / interpretation / corroboration / real-or-INFERRED) for the located record — grep for `offset` and `INFERRED`/`real`.
    - The located record's off + size are asserted with byte-exact literals (grep the hex offset).
    - The differential basis (level-1 vs god pair) is present in the test — grep for both `maiblade`/`ANMX` and a god-tier name.
    - The type-5 identity is confirmed (not assumed from a literal-5 read); the evidence comment states how the record was confirmed as type-5 (differential + mogaika anm/ cross-ref), OR records the escalation/abort disposition.
  </acceptance_criteria>
  <done>The type-5 blade-state descriptor identity is confirmed (not assumed from a literal-5 read) and framed with a per-field evidence table; anm.test.js is RED against the missing decoder and pins only genuinely-recovered seeds (or records the gap per the escalation/abort protocol).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — parseAnmType5 decoder + queryable blade-state result</name>
  <files>tools/kratos-lab/fxparse.js</files>
  <behavior>
    - parseAnmType5(buf, rec) → { subtype, states:[...], size, evidence:[...] } describing blade visibility per combat state.
    - Exposes a queryable result: given "in-combat" vs "out-of-combat", returns the blade visibility (in-hand vs on-back).
    - Result is plain/JSON-dumpable (no GL/DOM).
    - Bad/short ANM record → named throw.
  </behavior>
  <read_first>
    - tools/kratos-lab/fxparse.js:159-196 — parseTxr idiom (size-gate→magic→named reads) + the export return line.
    - tools/kratos-lab/test/anm.test.js (from Task 1) — the per-field evidence table + RED assertions define exactly what to decode.
    - .planning/phases/05-fx-record-decode/05-RESEARCH.md § "Project Constraints" (fail-loud; real/INFERRED tagging; JSON-dumpable, no GL/DOM).
  </read_first>
  <action>
    In `tools/kratos-lab/fxparse.js`, add `parseAnmType5(buf, rec)` inside the FxParse IIFE with a JSDoc byte-layout header citing the Task-1 evidence table + DEC-03. Size-gate first: if `rec.size < <layout-min>` throw `ANM ${rec.name}: size ${rec.size} < <min>`. DataView over buf.buffer/byteOffset/byteLength. Assert the type-5 identifier (the subtype/magic recovered in Task 1); on mismatch throw `ANM ${rec.name}: not a type-5 descriptor (…)`. Decode the show/hide state field(s) into a `states` structure and a helper that maps a combat state (in-combat / out-of-combat) to blade visibility (in-hand / on-back). BOUND every read by `rec.size`. Build `evidence[]`: byte-decoded fields tagged real; any runtime/animated or ELF-pending field tagged INFERRED with a corroboration note. Return the plain descriptor object.
    Extend the FxParse export return line to include `parseAnmType5`. Keep the output JSON-dumpable. Do NOT bump index.html `?v=` — decode-only; Phase-6 wires the blade presentation.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/anm.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/anm.test.js` exits 0 (type-5 known-answers + the state-query result green).
    - Full suite (`for f in tools/kratos-lab/test/*.test.js; do node "$f" || exit 1; done`) exits 0 — no regression to the DEC-02 suites sharing fxparse.js.
    - `grep -c 'parseAnmType5' tools/kratos-lab/fxparse.js` === 1 and the export line lists it; the size-gate throw string contains `ANM ${rec.name}` (grep `ANM .*size`).
    - `node -e "const F=require('./tools/kratos-lab/fxparse.js'),P=require('./tools/kratos-lab/parsers.js'),fs=require('fs');const b=new Uint8Array(fs.readFileSync('assets/wads/R_WPN0_0.WAD'));const recs=P.parseWad(b);const rec=recs.find(r=>/mai(god)?blade|ANM/.test(r.name)&&r.size>0);JSON.stringify(F.parseAnmType5(b,rec))"` exits 0 (decoder runs and is JSON-dumpable) — the executor substitutes the exact located record.
  </acceptance_criteria>
  <done>parseAnmType5 decodes the type-5 blade-state descriptor into a queryable, JSON-dumpable result with a real/INFERRED evidence table; anm.test.js green; full suite green — DEC-03 accounted for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disc-derived bytes → parseAnmType5 | Untrusted binary; short/wrong-subtype ANM must fail loud |
| assets/ fetch policy | Blade ANM read from `assets/wads/R_WPN0_0.WAD` only; never `extracted/` or ISO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Info-disclosure | parseAnmType5 short-record read | mitigate | Size-gate before any field read, throw naming the record; Task-1 synthetic-throw seed + Task-2 gate |
| T-05-02 | Tampering | reading a state field past record end | mitigate | Bound every read by `dataOff + rec.size` |
| T-05-04 | Integrity (data-first) | inventing a blade-state value that did not decode | mitigate | real/INFERRED tag on every field; type-5 identity confirmed before pinning; escalation/abort-with-recorded-gap instead of a fabricated seed; ELF-escalation noted for resistant fields |
| T-05-03 | Tampering | wrong same-name ANM copy (stage1 vs god) | mitigate | Resolve/locate the specific level-1 record by off/idx; differential pair-diff distinguishes the copies |
| T-05-SC | Tampering | supply chain | accept | Zero packages — node:assert + built-ins only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/anm.test.js` green (type-5 framing + state query).
- Full suite green: `for f in tools/kratos-lab/test/*.test.js; do node "$f" || exit 1; done`.
- The decoded descriptor answers "in-combat → in-hand / out-of-combat → on-back" from real bytes (or clearly-labeled INFERRED where a field resisted decode).
</verification>

<success_criteria>
- The type-5 ANM blade-state descriptor is located, its identity confirmed (not assumed from a literal-5 read), framed, and decoded with a per-field evidence table.
- parseAnmType5 exposes a queryable, JSON-dumpable blade-visibility result for Phase-6 to gate on.
- Every field is real or INFERRED (labeled + corroborated); ELF escalation OR wave-abort-with-recorded-gap is documented for any resistant look-critical field (never a fabricated byte-exact seed).
- Bad/short ANM records throw named errors; full suite green.
</success_criteria>

<output>
Create `.planning/phases/05-fx-record-decode/05-05-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 05-CONTEXT.md decisions embodied by this plan:
- D-07: the type-5 ANM blade-state descriptor is decoded LAST (Wave 5), never blocking the DEC-02 payoff.
- D-04: escalation protocol — confirm type-5 identity before pinning seeds; ELF/mogaika anm/ cross-ref; INFERRED-label or abort-with-recorded-gap rather than fabricate a byte-exact seed.
</decisions_honored>
