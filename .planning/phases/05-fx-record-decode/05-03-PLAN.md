---
phase: 05-fx-record-decode
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - tools/kratos-lab/fxparse.js
  - tools/kratos-lab/test/fxdb.test.js
autonomous: true
requirements: [DEC-02]
user_setup: []

must_haves:
  truths:
    - "parseFxc decodes all three FX subtypes: 0x2 emitter (BFT/BGT), 0x3 spark (BDEsparkemit), 0xd poly (BDepoly3 → explicit MSH link)"
    - "The subtype branch reads the reference name at the correct offset (+0x54 for 2/3, +0x58 after a u32 count for 0xd)"
    - "The differential invariant holds: FXC_BFTemit1 vs FXC_BGTemit1 matrices are identical; they differ only at idx/name/shape-block"
    - "buildFxDb ingests the standalone source (3rd arg) so FXC_BFTemit1/FXC_BGTemit1 become real db.fxc keys; it links FXC.slot ↔ PTC.slot (skipping placeholder 0x00 / 0xffff), resolves FXC_BDepoly3 → MSH_BDepoly3Shape (resolved:true), marks flame6Shape runtime-handle, and emits a complete refs[] graph including the real 0x1d FXC_BFTemit1 → PTC_BFTpart1 slot pair"
    - "The whole FxDb (msh+ptc+fxc+refs) is JSON.stringify-able — the Phase-6 hand-off"
  artifacts:
    - path: "tools/kratos-lab/fxparse.js"
      provides: "FxParse.parseFxc + buildFxDb fxc-section (WAD + standalone 3rd arg) + guarded cross-ref graph"
      contains: "function parseFxc"
    - path: "tools/kratos-lab/test/fxdb.test.js"
      provides: "FXC subtype-branch known-answers + differential invariant + full cross-ref (incl. guarded 0x1d slot pair)"
      contains: "MSH_BDepoly3Shape"
  key_links:
    - from: "tools/kratos-lab/fxparse.js buildFxDb"
      to: "Parsers.resolve"
      via: "FXC subtype-0xd shape ref → MSH record"
      pattern: "resolve\\("
    - from: "FXC_BFTemit1"
      to: "PTC_BFTpart1"
      via: "slot id (+0x08) pairing in refs[] (0x1d; placeholder 0x00 / 0xffff skipped)"
      pattern: "slot"
---

<phase_goal>
## Phase Goal (MVP user story)

**As a** Phase-6 particle runtime (and the developer wiring it), **I want to** query the game's real decoded particle / emitter / shape values from a JSON-dumpable `FxDb`, **so that** fire, sparks, and trails render from authentic GoW1 data instead of hand-tuned approximations.

This plan is slice-1 layer 3 of the decode order **MSH → PTC → FXC** (D-02) AND the completion of the **D-01 top-priority slice** (swing trails + their spark particles): the emitters reference the particles/shapes decoded in 05-01/05-02, so this plan closes the emitter → particle → shape graph the runtime walks.
</phase_goal>

<objective>
Decode `FXC_*` emitter records across the three FX subtypes (0x2 emitter, 0x3 spark, 0xd poly), branching the reference-name offset on subtype, and assemble the complete `FxDb` cross-reference graph (slot pairing + shape resolution) for the trail + spark family that Phase-6 renders first — ingesting the standalone-only BFT/BGT emitters via the buildFxDb 3rd arg so they become real db.fxc keys.

Purpose: The emitter is the last decode layer and the FxDb root — it links a trail emitter to its particle def to its shape/texture. After this plan the whole slice-1 payoff data (BFT/BGT trails + BDEsparkemit sparks + BDepoly shapes) is queryable end-to-end.
Output: `FxParse.parseFxc`, `buildFxDb` fxc-section (WAD + standalone) + guarded refs graph, and extended `test/fxdb.test.js` — green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-fx-record-decode/05-RESEARCH.md
@.planning/phases/05-fx-record-decode/05-02-SUMMARY.md
@tools/kratos-lab/fxparse.js
@tools/kratos-lab/parsers.js

<interfaces>
<!-- Contracts from 05-01/05-02 (extend them) + codebase, verified this session. -->

FxParse.buildFxDb(records, wadBuf, standaloneRecs=[]) -> { meta, msh, ptc, fxc, refs }  (extend the fxc + refs sections; the 3rd arg standaloneRecs was added in 05-02)
  standaloneRecs: [{ name, buf, tag }] — BFT/BGT emitters are standalone-ONLY (absent from the WAD, Pitfall 1). The FXC_BFTemit1/FXC_BGTemit1 defs enter db.fxc from this 3rd arg, exactly as the PTC defs did in 05-02. buildFxDb synthesizes a headerless rec { name, idx:0, tag, size:buf.length, dataOff:0 } per element and decodes with that element's buf.
  IMPORTANT slot-pairing guard (Warning 1): the meaningful slot ids (0x1d for BFT/BGT) live ONLY in the standalone files; every WAD-native FXC/PTC carries placeholder slot 0x00. Slot-pairing MUST skip slot 0x00 and the 0xffff root sentinel so no false 0x00×0x00 pairs are fabricated.
FxParse.parsePtc / parseMsh / readName — available from prior slices.
Parsers.resolve(records, name, fromIdx) -> record | null   (only MSH_* refs resolve to a record)

FXC byte layout (VERIFIED first-party; RESEARCH "Record Structure → FXC"; layout after +0x50 BRANCHES on subtype — Pitfall 2):
  +0x00 u16 magic = 0x001e   (constant across all 9 FXC)
  +0x02 u16 subtype          0x2 emitter / 0x3 spark / 0xc grav / 0xd poly
  +0x04 u32 0
  +0x08 u16 slot/class id    (links FXC↔PTC — same id in the paired PTC; standalone BFT/BGT = 0x1d, WAD-native = placeholder 0x00)
  +0x0a u16 per-emitter index / 0xffff root sentinel
  +0x10..0x4f f32[16] 4×4 row-major placement matrix (rows orthonormal for a real rotation)
  +0x50 u32 record size (0xe4=228 for subtype 2/3; 0x88=136 for subtype 0xd)
  SUBTYPE 2/3: +0x54 char[] NUL-terminated particle/shape reference name
  SUBTYPE 0xd: +0x54 u32 count, then +0x58 char[] MSH name (the explicit FXC→MSH link)

Seed values (byte-exact this session):
  FXC_BFTemit1 (standalone .bin, 228 B): magic 0x1e, subtype 0x2, slot 0x1d, idx 0x05, size 228, ref@0x54 "BFTpart1Shape"; matrix row0 ≈ (0.00204, 0.75790, 0.65237)
  FXC_BGTemit1 (standalone .bin): IDENTICAL to BFTemit1 EXCEPT idx (0x06), name byte @0x55 (F→G → "BGTpart1Shape"), shape block. MATRIX IDENTICAL → fire & swoosh share placement.
  FXC_BDEsparkemit (in-WAD @0xcb70, 228 B): subtype 0x3, ref@0x54 "flame6Shape", matrix = identity (row0 = 1,0,0 — parent-origin)
  FXC_BDepoly3 (in-WAD @0xcc80, 136 B): subtype 0xd, u32@0x54 = 1, name@0x58 "MSH_BDepoly3Shape" → explicit FXC→MSH link
  NOTE (A4): BFT and BGT emit1 BOTH carry slot 0x1d — slot is a trail-position group id, not a fire/swoosh discriminator. The shapeRef NAME discriminates; slot is corroboration only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — FXC subtype-branch known-answers + differential invariant + full cross-ref (incl. guarded slot pair)</name>
  <files>tools/kratos-lab/test/fxdb.test.js</files>
  <read_first>
    - tools/kratos-lab/test/fxdb.test.js (from 05-01/05-02) — extend; reuse the `loadBin` helper and the WAD read.
    - .planning/phases/05-fx-record-decode/05-RESEARCH.md § "Per-Field Evidence Tables" (FXC_BFTemit1 / FXC_BGTemit1 / FXC_BDEsparkemit / FXC_BDepoly3 rows) + § "Common Pitfalls → Pitfall 2" (subtype-branched offset) + § "FxDb Shape" (the canonical refs example: FXC_BFTemit1 → PTC_BFTpart1 slot pair).
    - tools/kratos-lab/test/wad.test.js:73-101 — the resolve round-trip that proves MSH_BDepoly3Shape lands on the right copy (reuse for the FXC→MSH link assertion).
  </read_first>
  <action>
    Extend `tools/kratos-lab/test/fxdb.test.js` with an `// --- FXC subtype-branch known-answers ---` block.
    RED assertions (must FAIL — parseFxc not exported; DEC-02):
    (a) subtype 0x2 (standalone): `loadBin("FXC_BFTemit1")` → `FxParse.parseFxc` → magic 0x1e, subtype 0x2, slot 0x1d, idx 0x05, size 228, shapeRef "BFTpart1Shape"; assert matrix row0 ≈ (0.00204, 0.75790, 0.65237) within 1e-4.
    (b) Differential invariant (DEC-02 test-map row 4): `loadBin("FXC_BGTemit1")` → parseFxc; `assert.deepStrictEqual` its 16-float matrix to BFTemit1's matrix (IDENTICAL); assert shapeRef "BGTpart1Shape" and idx 0x06 (differ only at idx/name/shape-block). Assert a matrix orthonormality sanity check on row vectors (each ≈ unit length).
    (c) subtype 0x3 (in-WAD): find FXC_BDEsparkemit (@0xcb70) → parseFxc → subtype 0x3, shapeRef "flame6Shape", matrix identity (row0 = (1,0,0)).
    (d) subtype 0xd branch (in-WAD, Pitfall 2): find FXC_BDepoly3 (@0xcc80) → parseFxc → subtype 0xd, the u32 count @0x54 === 1, shapeRef "MSH_BDepoly3Shape" (read at +0x58, NOT +0x54). This proves the subtype branch.
    (e) buildFxDb cross-ref graph WITH the standalone source: build `standaloneRecs` from loadBin for `FXC_BFTemit1`, `FXC_BGTemit1`, `PTC_BFTpart1`, `PTC_BGTpart1` (each `{name, buf, tag:0x1e}`) and call `FxParse.buildFxDb(recs, buf, standaloneRecs)`. Assert `db.fxc["FXC_BDEsparkemit"]` (in-WAD) AND `db.fxc["FXC_BFTemit1"]` / `db.fxc["FXC_BGTemit1"]` (standalone) exist as real keys. Assert `db.refs` contains `{ from:"FXC_BDepoly3", kind:"shape", to:"MSH_BDepoly3Shape", resolved:true }` AND `{ from:"FXC_BDEsparkemit", kind:"shape", to:"flame6Shape", resolved:false }`. Assert the D-01 trail slot pair is emitted: `db.refs` contains `{ from:"FXC_BFTemit1", kind:"slot", to:"PTC_BFTpart1" }` (both share slot 0x1d — matches the RESEARCH canonical FxDb example). GUARD assertion (Warning 1): assert NO ref in `db.refs` pairs two records that both carry placeholder slot 0x00 or the 0xffff root sentinel (i.e. the WAD-native FXC/PTC with slot 0x00 produce NO false slot pairs). Assert `JSON.stringify(db)` does not throw and its whole graph (msh+ptc+fxc+refs) serializes.
    (f) Fail-loud: a synthetic FXC with magic ≠ 0x1e throws /FXC_.../; a short FXC (size < 0x58) throws named before magic.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/fxdb.test.js 2>&1 | grep -Eq 'parseFxc|is not a function|TypeError' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/fxdb.test.js` exits NON-zero (RED) naming `parseFxc`.
    - Seeds `0x1e`, `BFTpart1Shape`, `BGTpart1Shape`, `flame6Shape`, `MSH_BDepoly3Shape`, `0.75790`, `228`, `136` appear literally (grep each).
    - The differential invariant uses `deepStrictEqual` on the two matrices (grep `deepStrictEqual`).
    - The subtype-0xd assertion reads the ref name at +0x58 after a u32 count (grep the +0x58 / count logic in the test).
    - The test builds `buildFxDb(recs, buf, standaloneRecs)` and asserts the slot pair `{ from:"FXC_BFTemit1", kind:"slot", to:"PTC_BFTpart1" }` (slot 0x1d) is present in db.refs (grep `kind` + `slot` + `PTC_BFTpart1`).
    - The test asserts NO false slot pair over placeholder slot 0x00 / 0xffff exists (grep the guard assertion).
  </acceptance_criteria>
  <done>fxdb.test.js RED against parseFxc; pins all three subtypes, the BFT-vs-BGT matrix-identity invariant, the +0x58 subtype-0xd branch, the standalone BFT/BGT emitters entering db.fxc, the guarded FXC_BFTemit1→PTC_BFTpart1 slot pair (0x1d), and the full cross-ref graph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — parseFxc (subtype branch) + buildFxDb fxc-section (WAD + standalone) + guarded refs graph</name>
  <files>tools/kratos-lab/fxparse.js</files>
  <behavior>
    - parseFxc(buf, rec) → { magic, subtype, slotId, index, matrix:[16], shapeRef, size, evidence:[...] }.
    - subtype 2/3 → shapeRef @+0x54; subtype 0xd → u32 count @+0x54, shapeRef @+0x58.
    - FXC_BFTemit1 & FXC_BGTemit1 decode to IDENTICAL matrices; differ only idx/name/shape-block.
    - buildFxDb.fxc populated from the WAD (BDEsparkemit/BDepoly) AND the standalone 3rd arg (FXC_BFTemit1/FXC_BGTemit1 become real keys); refs[] has shape resolutions (MSH_* resolved:true, others resolved:false) + slot pairings that SKIP placeholder slot 0x00 and the 0xffff root sentinel — so the only slot pair emitted is the real 0x1d BFT/BGT trail pairing, never a fabricated 0x00×0x00 pair.
    - Bad-magic / short FXC → named throw.
  </behavior>
  <read_first>
    - tools/kratos-lab/fxparse.js:159-196 — parseTxr idiom + buildMats batch loop + export return line (now includes parseMsh/parsePtc/buildFxDb).
    - .planning/phases/05-fx-record-decode/05-RESEARCH.md § "Architecture Patterns → Pattern 1" (the parseFxc skeleton) + § "FxDb Shape" (fxc entry + refs rules).
    - tools/kratos-lab/parsers.js:318-324 — resolve, for the subtype-0xd MSH link.
  </read_first>
  <action>
    In `tools/kratos-lab/fxparse.js`, add `parseFxc(buf, rec)` inside the FxParse IIFE with a JSDoc byte-layout header (cite RESEARCH offsets + DEC-02 + Pitfall 2). Size-gate: if `rec.size < 0x58` throw `FXC ${rec.name}: size ${rec.size} < 0x58`. DataView over buf.buffer/byteOffset/byteLength. Assert u16 magic @dataOff must be 0x1e (else throw `FXC ${rec.name}: bad magic 0x… (expected 0x1e)`). Read subtype u16 @+0x02, slotId u16 @+0x08, index u16 @+0x0a, matrix f32[16] @+0x10, size u32 @+0x50. BRANCH on subtype for the reference name: subtype 0x2 or 0x3 → readName @+0x54; subtype 0xd → read u32 count @+0x54 then readName @+0x58; other subtypes (0xc grav) → read name @+0x54 as a best-effort, tag INFERRED. Build `evidence[]`: magic/subtype/slot/idx/size/shapeRef tagged real; matrix rows tagged real (note orthonormality). Return the emitter object.
    Extend `buildFxDb`: for WAD records with `tag === 0x1e && size > 0 && name.startsWith("FXC_")`, decode via parseFxc(wadBuf, r) into `db.fxc[r.name]`, passing the record's `idx` so subtype-0xd resolution can use it. THEN, for each `sr` in `standaloneRecs` whose `name` starts with "FXC_", synthesize `const rec = { name:sr.name, idx:0, tag:sr.tag, size:sr.buf.length, dataOff:0 }` and decode `db.fxc[sr.name] = parseFxc(sr.buf, rec)` — this is how the standalone-only BFT/BGT emitters (absent from the WAD, Pitfall 1) enter db.fxc, completing the D-01 trail slice. Then assemble `refs`:
      - Shape refs: if shapeRef starts with "MSH_", `Parsers.resolve(records, shapeRef, idx)` — on hit push `{ from:name, kind:"shape", to:shapeRef, resolved:true }`; on a true miss for an MSH_* target, THROW naming the record (the resolve "caller decides" contract). Non-MSH refs → `{ ..., resolved:false, note:"runtime handle" }` (do NOT throw — Anti-Pattern "…Shape refs always resolvable").
      - Slot pairs (GUARDED — Warning 1): for each FXC, SKIP if its `slotId === 0x00 || slotId === 0xffff` (placeholder / root sentinel — the WAD-native FXC/PTC all carry slot 0x00, and pairing on it would fabricate false 0x00×0x00 pairs). Otherwise find PTC entries in `db.ptc` sharing the same non-placeholder slotId and push `{ from:FXC name, kind:"slot", to:PTC name }`. With the standalone source merged, the real 0x1d slot links FXC_BFTemit1→PTC_BFTpart1 and FXC_BGTemit1→PTC_BGTpart1 (corroboration; slot is a group id, name is the discriminator — A4).
    Extend the export return line to include `parseFxc`. Keep the output plain/JSON-dumpable.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/fxdb.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/fxdb.test.js` exits 0 (MSH + PTC + all FXC + cross-ref + JSON-dump green).
    - `node tools/kratos-lab/test/wad.test.js` still exits 0 (no regression).
    - `grep -c 'function parseFxc' tools/kratos-lab/fxparse.js` === 1; export line lists `parseFxc`.
    - `grep -Eq "FXC .*expected 0x1e" tools/kratos-lab/fxparse.js` (named bad-magic throw) and the subtype-0xd `+0x58` branch is present (grep `0x58`).
    - `node -e "const F=require('./tools/kratos-lab/fxparse.js'),P=require('./tools/kratos-lab/parsers.js'),fs=require('fs');const b=new Uint8Array(fs.readFileSync('assets/wads/R_WPN0_0.WAD'));const db=F.buildFxDb(P.parseWad(b),b);const s=JSON.stringify(db);if(!/MSH_BDepoly3Shape/.test(s)||!db.refs.some(r=>r.kind==='shape'&&r.resolved===true))process.exit(1)"` exits 0 (whole graph serializes; at least one resolved MSH shape ref).
    - `node -e "const F=require('./tools/kratos-lab/fxparse.js'),P=require('./tools/kratos-lab/parsers.js'),fs=require('fs');const b=new Uint8Array(fs.readFileSync('assets/wads/R_WPN0_0.WAD'));const L=n=>({name:n,tag:0x1e,buf:new Uint8Array(fs.readFileSync('assets/kratos/fx/'+n+'.bin'))});const sr=['FXC_BFTemit1','FXC_BGTemit1','PTC_BFTpart1','PTC_BGTpart1'].map(L);const db=F.buildFxDb(P.parseWad(b),b,sr);const pair=db.refs.some(r=>r.kind==='slot'&&r.from==='FXC_BFTemit1'&&r.to==='PTC_BFTpart1');const bad=db.refs.some(r=>r.kind==='slot'&&(db.fxc[r.from]&&(db.fxc[r.from].slotId===0||db.fxc[r.from].slotId===0xffff)));if(!db.fxc['FXC_BFTemit1']||!pair||bad)process.exit(1)"` exits 0 (BFT/BGT emitters are real db.fxc keys; the guarded 0x1d slot pair exists; no placeholder-slot pair fabricated).
  </acceptance_criteria>
  <done>parseFxc (subtype-branched) + buildFxDb fxc/refs (WAD + standalone 3rd arg) implemented; the BFT/BGT emitters are real db.fxc keys with a guarded 0x1d slot pair to their PTC defs; fxdb.test.js green; wad.test.js green; the full emitter→particle→shape graph is JSON-dumpable — the Phase-6 hand-off for the trail+spark payoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disc-derived bytes → parseFxc | Untrusted binary; subtype-branched offsets must not read a name past record end |
| shape-ref resolution | An unresolvable MSH_* target must fail loud, not silently produce a dangling ref |
| assets/ fetch policy | Reads stay under `assets/`; never widen to `extracted/` or ISO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Info-disclosure | parseFxc short-record read | mitigate | Size-gate `rec.size < 0x58` before magic, throw naming the record; Task-1 (f) test |
| T-05-02 | Tampering | subtype-0xd name read at wrong offset (+0x54 vs +0x58) | mitigate | Branch on subtype BEFORE reading the name (Pitfall 2); known-answer per subtype; readName maxLen bounds the read |
| T-05-03 | Tampering | wrong same-name MSH copy resolved | mitigate | Resolve nearest-preceding from the FXC's idx (size>0, tag 0x70); throw on a true MSH_* miss |
| T-05-04 | Integrity (data-first) | dangling/fabricated cross-ref | mitigate | Only MSH_* refs marked resolved:true (via resolve); runtime handles marked resolved:false, never invented; slot-pairing SKIPS placeholder slot 0x00 and the 0xffff root sentinel so no false 0x00×0x00 pairs are fabricated (Warning 1) |
| T-05-SC | Tampering | supply chain | accept | Zero packages — node:assert + built-ins only |
</threat_model>

<verification>
- `node tools/kratos-lab/test/fxdb.test.js` green (MSH + PTC + FXC + refs).
- `node tools/kratos-lab/test/wad.test.js` green (regression guard).
- The buildFxDb output (WAD + standalone 3rd arg) serializes to JSON with a resolved MSH shape ref and the guarded 0x1d BFT/BGT slot pair present (no placeholder-slot pairs).
</verification>

<success_criteria>
- parseFxc decodes all three subtypes byte-exact, with the +0x58 branch for 0xd.
- The BFT-vs-BGT matrix-identity differential invariant holds.
- buildFxDb emits a complete, JSON-dumpable emitter→particle→shape graph from the WAD + standalone source: MSH_* shape refs resolved, runtime handles marked, the real 0x1d BFT/BGT slot pair recorded, and placeholder slot 0x00 / 0xffff skipped (no fabricated pairs).
- Bad-magic / short FXC records throw named errors.
</success_criteria>

<output>
Create `.planning/phases/05-fx-record-decode/05-03-SUMMARY.md` when done.
</output>

<decisions_honored>
Traceability to 05-CONTEXT.md decisions embodied by this plan:
- D-01: payoff-first — the BFT/BGT trail emitters and the FXC_BFTemit1→PTC_BFTpart1 slot ref land in FxDb.fxc/refs (the visible-trail lever).
- D-02: FXC decoded last, subtype 0x2 first (Wave 3); buildFxDb cross-ref assembly runs only after its referenced records exist.
</decisions_honored>
