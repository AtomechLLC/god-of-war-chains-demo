---
phase: 01-reference-pipeline-validation-criteria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/SETTINGS.md
  - reference/PCSX2.ini
autonomous: false
requirements: [VAL-01]

must_haves:
  truths:
    - "PCSX2 2.6.3 boots the NTSC-U ISO with the software renderer and every capture-relevant setting documented as it was set (not reconstructed later)"
    - "Tools -> Video Capture is functional (FFmpeg DLLs present, sourced ONLY from the official PCSX2 dependencies release)"
    - "An authoritative PCSX2.ini snapshot exists in reference/ so later sessions can diff for config drift"
  artifacts:
    - path: "reference/SETTINGS.md"
      provides: "Setting | Value | Why table, reproducible launch lines, DLL provenance, ini-diff procedure"
      contains: "Software"
    - path: "reference/PCSX2.ini"
      provides: "Verbatim post-configuration snapshot of Documents\\PCSX2\\inis\\PCSX2.ini"
  key_links:
    - from: "reference/SETTINGS.md"
      to: "reference/PCSX2.ini"
      via: "documented values confirmed against the live ini, then snapshotted; sessions start by diffing"
      pattern: "PCSX2\\.ini"
    - from: "reference/SETTINGS.md"
      to: "github.com/PCSX2/pcsx2-windows-dependencies"
      via: "FFmpeg DLL provenance line naming the exact official release URL"
      pattern: "pcsx2-windows-dependencies"
---

<objective>
Stand up the uncontaminated capture pipeline: author SETTINGS.md from the researched configuration table, stage the official FFmpeg DLLs for video capture, have the human perform the one-time PCSX2 first-run configuration (GUI work), then machine-verify the resulting ini, snapshot it, and finalize SETTINGS.md with confirmed values and actual 2.6.3 menu wording.

Purpose: ROADMAP success criterion 1 — "a PCSX2 software-renderer, native-res capture setup exists with documented settings". PCSX2 has never been configured on this machine (inis dir is empty), which is an advantage: settings are documented from a true clean slate.
Output: `reference/SETTINGS.md` (confirmed, no pending markers), `reference/PCSX2.ini` snapshot, FFmpeg DLLs installed beside pcsx2-qt.exe.
</objective>

## Phase Goal

**As a** solo developer validating kratos-lab against the real game, **I want to** build an uncontaminated reference library with measured, written acceptance criteria, **so that** every later visual judgment compares against verified-clean ground truth.

(Restated 1:1 from the ROADMAP Phase 1 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md
@.planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md
@extracted/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SETTINGS.md and stage official FFmpeg DLLs</name>
  <files>reference/SETTINGS.md</files>
  <read_first>
    - .planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md (Pattern 1 settings table lines 197-217, Environment Availability table, Security Domain — the DLL-source rule)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md (section "reference/SETTINGS.md" — provenance-first opening modeled on extracted/README.md)
    - extracted/README.md (the analog doc whose header/provenance style SETTINGS.md copies)
  </read_first>
  <action>
    1. Create `reference/SETTINGS.md`:
       - Provenance-first opening: "Every capture here was produced by PCSX2 2.6.3.0 (`C:\Program Files\PCSX2\pcsx2-qt.exe`), software renderer, from `God of War (USA).iso` (SCUS-973.99, NTSC-U — 60Hz tick units for all later rate interpretation), BIOS scph39001.bin."
       - Setting | Value | Why table prefilled from RESEARCH.md Pattern 1: Renderer = Software; Software Rendering Threads = 2-4; Aspect Ratio = Auto 4:3/3:2 (NO widescreen patch); Deinterlacing = per progressive-scan outcome (filled by plan 01-03); Screenshot Size = Internal Resolution (Aspect Uncorrected); Screenshot Format = PNG; Video capture = MP4/H.264 at maximum UI bitrate; Patches = ALL OFF (Skip Cutscenes optional, document if enabled); Emulation speed = 100%; Hotkeys = Frame Advance bound + F1/F2/F3/F8 confirmed. Mark every Value cell with the token `[PENDING CONFIRM]` after the researched value — Task 3 removes every instance after the human confirms them in the GUI.
       - Reproducible launch lines (fastboot and -statefile forms) exactly as in RESEARCH.md Code Examples.
       - Empty `## Calibration` section stub with the two fields plan 01-03 will fill: actual screenshot dimensions (magick identify output) and horizontal scale factor (512-wide framebuffer: 640-wide screenshots => x0.8; 512-wide => 1.0). A stub here is fine — the no-TBD gate applies to ACCEPTANCE.md, but use the same `[PENDING CONFIRM]`-free wording: label the fields "recorded by capture session (plan 01-03)".
       - `## FFmpeg DLL provenance` section: exact source URL https://github.com/PCSX2/pcsx2-windows-dependencies/releases/tag/FFMPEG, filenames and SHA-256 hashes of the downloaded DLLs (compute with `certutil -hashfile <f> SHA256` or `sha256sum`), and the rule: no other DLL source is ever acceptable (DLL loading = code execution inside PCSX2).
       - `## Session start procedure` section: diff `Documents\PCSX2\inis\PCSX2.ini` against `reference/PCSX2.ini` before every capture session; any diff in [EmuCore/GS] must be resolved before capturing.
       - Italic dated footer.
    2. Download the FFMPEG release asset from the URL above into the session scratchpad, extract, record the DLL filenames + SHA-256 hashes into SETTINGS.md, then attempt to copy the DLLs next to `C:\Program Files\PCSX2\pcsx2-qt.exe`. If the copy fails with access denied (Program Files needs elevation), leave the extracted DLLs in a staging folder, write the staging path into SETTINGS.md under the provenance section, and let Task 2's checkpoint instructions cover the elevated copy. Do NOT search the web for alternative DLL packs under any circumstances (T-1-02).
  </action>
  <verify>
    <automated>grep -qi "renderer" reference/SETTINGS.md && grep -q "pcsx2-windows-dependencies" reference/SETTINGS.md && grep -q "SCUS-973.99" reference/SETTINGS.md && grep -qi "SHA-*256" reference/SETTINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - reference/SETTINGS.md contains a Setting | Value | Why table with a `Renderer` row whose value names `Software`
    - SETTINGS.md contains both launch lines (one with `-fastboot`, one with `-statefile`)
    - SETTINGS.md names https://github.com/PCSX2/pcsx2-windows-dependencies/releases/tag/FFMPEG as the sole DLL source and lists SHA-256 hashes for each downloaded DLL
    - The FFmpeg DLLs are either present beside pcsx2-qt.exe (`ls "C:/Program Files/PCSX2/"av*.dll` non-empty) or staged with the staging path recorded in SETTINGS.md
    - A `## Session start procedure` section describes the ini diff against reference/PCSX2.ini
  </acceptance_criteria>
  <done>SETTINGS.md exists with the full researched configuration ready to apply, and the official DLLs are downloaded, hashed, and installed or staged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human performs PCSX2 first-run configuration</name>
  <files>external: Documents\PCSX2\inis\PCSX2.ini (created by the GUI), C:\Program Files\PCSX2\ (elevated DLL copy if staged)</files>
  <read_first>
    - reference/SETTINGS.md (authored in Task 1 — the exact settings to apply, row by row)
  </read_first>
  <what-built>SETTINGS.md holds the complete target configuration; FFmpeg DLLs are downloaded from the official source and installed or staged for an elevated copy.</what-built>
  <action>
    Human-performed — PCSX2's first-run wizard (BIOS selection, game directory) and settings dialogs are GUI-only with no CLI equivalent, so this one-time configuration cannot be automated. Follow the numbered steps in how-to-verify; Claude machine-verifies the resulting ini in Task 3.
  </action>
  <how-to-verify>
    1. If Task 1 reported the DLL copy needed elevation: copy the staged DLLs (path recorded in reference/SETTINGS.md) into `C:\Program Files\PCSX2\` next to pcsx2-qt.exe (needs admin).
    2. Launch `C:\Program Files\PCSX2\pcsx2-qt.exe`. Complete the first-run wizard: BIOS = scph39001.bin (US), add the repo root as game directory, default controller mapping (use a gamepad if you have one — strongly preferred for clean combat takes; keyboard works).
    3. Apply every row of the SETTINGS.md table via Settings -> Graphics (Renderer=Software, screenshot size/format, aspect ratio) and Settings -> Emulation (100% speed). Leave ALL patches off in game properties.
    4. In Settings -> Hotkeys: confirm Save State (F1), Load State (F3), Screenshot (F8); bind Frame Advance if unbound (note the key you chose).
    5. Where the 2.6.3 menu wording differs from a SETTINGS.md row, jot the actual wording (edit the doc directly or note it here in your reply).
    6. Boot the ISO once (any boot mode) to confirm it launches, then open Tools -> Video Capture and confirm the dialog opens WITHOUT an FFmpeg error. Close PCSX2 (settings persist to the ini on exit).
  </how-to-verify>
  <verify>
    <automated>test -s "$USERPROFILE/Documents/PCSX2/inis/PCSX2.ini" || test -s "$USERPROFILE/OneDrive/Documents/PCSX2/inis/PCSX2.ini"</automated>
  </verify>
  <acceptance_criteria>
    - `Documents\PCSX2\inis\PCSX2.ini` exists and is non-empty (was empty-dir before this phase); if Documents is OneDrive-redirected the gate's fallback path applies, and if both default paths are empty, re-run the gate against the ini path the human reports in the resume signal
    - av*/swresample/swscale DLLs are present in C:\Program Files\PCSX2\ (video capture opens clean)
    - Human confirms every SETTINGS.md row was applied, Frame Advance is bound, and reports any menu-wording differences + the Frame Advance key
  </acceptance_criteria>
  <done>PCSX2 is configured per SETTINGS.md, the ini exists on disk, video capture opens without error, and the human has replied "configured" with the Frame Advance key and any menu-wording corrections.</done>
  <resume-signal>Type "configured" plus: the Frame Advance key you bound, whether Video Capture opened without error, any menu-wording corrections, and the PCSX2.ini path if your Documents folder is redirected somewhere nonstandard.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify ini, snapshot it, finalize SETTINGS.md</name>
  <files>reference/SETTINGS.md, reference/PCSX2.ini</files>
  <read_first>
    - reference/SETTINGS.md (current state with [PENDING CONFIRM] markers)
    - $USERPROFILE/Documents/PCSX2/inis/PCSX2.ini (the live ini written by the GUI session — if not at this path, locate via the PCSX2 data directory; Documents may be OneDrive-redirected)
  </read_first>
  <action>
    1. Read the live PCSX2.ini. Confirm machine-checkable values: the `[EmuCore/GS]` section exists and its `Renderer` key is set to the software renderer's enum (record whatever numeric/string value 2.6.3 writes — document the observed value in SETTINGS.md rather than assuming an enum); screenshot format/size keys match what the GUI set; note the `Aspect Ratio` value. If `Renderer` does not correspond to Software (cross-check: flipping the GUI setting changes this key), STOP and re-open the Task 2 checkpoint rather than documenting a wrong pipeline.
    2. Copy the live ini verbatim to `reference/PCSX2.ini` (tracked snapshot; it contains no secrets — it is emulator configuration).
    3. Update reference/SETTINGS.md: remove every `[PENDING CONFIRM]` token, replacing researched values with confirmed ones; add an `## Observed ini keys` subsection quoting the exact key=value lines for Renderer, screenshot size/format, and aspect ratio (so drift-diffing has named anchors); incorporate the human's menu-wording corrections and the Frame Advance key from the Task 2 resume message; record the DLL install completion.
  </action>
  <verify>
    <automated>test -f reference/PCSX2.ini && test -s reference/PCSX2.ini && ! grep -q "PENDING CONFIRM" reference/SETTINGS.md && grep -q "Renderer" reference/SETTINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - reference/PCSX2.ini is byte-identical to the live Documents\PCSX2\inis\PCSX2.ini at snapshot time (verify: `diff` or `fc` exits 0 immediately after copy)
    - reference/SETTINGS.md contains zero `[PENDING CONFIRM]` tokens
    - SETTINGS.md has an `## Observed ini keys` section quoting the live Renderer key=value line
    - SETTINGS.md records the bound Frame Advance key reported by the human
  </acceptance_criteria>
  <done>The capture pipeline is configured, machine-verified, documented with confirmed values, and snapshotted for drift detection. Plan 01-03's capture session can start by diffing against reference/PCSX2.ini.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> C:\Program Files\PCSX2\ | Downloaded DLLs load into PCSX2's process = arbitrary code execution |
| local repo -> public GitHub (Pages) | reference/PCSX2.ini becomes public — must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering / Elevation of Privilege | FFmpeg DLLs dropped into PCSX2 dir | mitigate | Download ONLY from github.com/PCSX2/pcsx2-windows-dependencies/releases/tag/FFMPEG; record SHA-256 hashes in SETTINGS.md; Task 1 action explicitly forbids alternative DLL sources; human checkpoint sees the provenance section before copying |
| T-1-03 | Information Disclosure | reference/PCSX2.ini published via Pages | accept | Emulator config only — paths and graphics settings, no credentials; ini contains local absolute paths (username visible) which the developer already exposes in tracked docs |
| T-1-SC | Tampering | package installs | accept (n/a) | No npm/pip/cargo installs in this phase (RESEARCH.md audit table: none) |
</threat_model>

<verification>
- `test -s "$USERPROFILE/Documents/PCSX2/inis/PCSX2.ini"` — or the OneDrive-redirected / human-reported ini path (configuration persisted)
- `ls "C:/Program Files/PCSX2/"*.dll` includes av*/sw* FFmpeg DLLs
- All Task verifies green; `! grep -q "PENDING CONFIRM" reference/SETTINGS.md`
</verification>

<success_criteria>
- PCSX2 2.6.3 launches the NTSC-U ISO under software renderer with all patches off
- Video capture is functional with officially-sourced DLLs whose hashes are recorded
- SETTINGS.md documents every capture-relevant setting with confirmed values and actual menu wording; reference/PCSX2.ini snapshot exists for session drift-diffing
</success_criteria>

<output>
Create `.planning/phases/01-reference-pipeline-validation-criteria/01-02-SUMMARY.md` when done.
</output>
