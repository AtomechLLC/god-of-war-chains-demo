---
phase: 01-reference-pipeline-validation-criteria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - reference/TARGET-DEFINITION.md
  - reference/shot-list.md
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "Reference media directories are gitignored BEFORE any capture file exists (repo has GitHub Pages — copyrighted media must never be trackable)"
    - "TARGET-DEFINITION.md locks the target as 'GS output as captured, not CRT' and assigns every footage source an explicit role"
    - "A capture session can be run from the docs alone: shot-list.md enumerates every move x framing with in-game inputs, save-state slots, and a session protocol"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for reference/captures/, reference/frames/, reference/annotated/"
      contains: "reference/captures/"
    - path: "reference/TARGET-DEFINITION.md"
      provides: "Normative target statement + source-roles table + exclusions table"
      contains: "not CRT"
    - path: "reference/shot-list.md"
      provides: "Moves x framings capture matrix + session protocol + hotkey cheat sheet"
      contains: "combo3A"
  key_links:
    - from: "reference/shot-list.md"
      to: "tools/kratos-lab/README.md"
      via: "shot IDs reuse the game's own clip names so Phase 7 clips are directly comparable"
      pattern: "combo3A"
    - from: ".gitignore"
      to: "reference/frames/"
      via: "ignore rule present before first capture lands"
      pattern: "reference/frames/"
---

<objective>
Make the repo capture-safe and fully specify the capture campaign before any emulator work happens: gitignore the media directories (legal hard requirement — GitHub Pages is live on this repo), write the normative target definition ("GS output as captured, not CRT"), and author the shot list that the human capture session in plan 01-03 will execute verbatim.

Purpose: Every later artifact of this phase (frames, clips, measurements, acceptance checklist) depends on these three documents existing first. This is the Wave 0 gap identified in RESEARCH.md.
Output: `.gitignore` block, `reference/TARGET-DEFINITION.md`, `reference/shot-list.md`.
</objective>

## Phase Goal

**As a** solo developer validating kratos-lab against the real game, **I want to** build an uncontaminated reference library with measured, written acceptance criteria, **so that** every later visual judgment compares against verified-clean ground truth.

(Restated 1:1 from the ROADMAP Phase 1 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md
@.planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gitignore reference media and write TARGET-DEFINITION.md</name>
  <files>.gitignore, reference/TARGET-DEFINITION.md</files>
  <read_first>
    - .gitignore (current state — the block appends after the existing extracted/** block, same comment style)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md (sections ".gitignore modification" and "reference/TARGET-DEFINITION.md" — exact block text and doc structure)
    - .planning/research/PITFALLS.md (P8 source-role rules, P9 lock-the-definition language — the content this doc makes normative)
    - .planning/REQUIREMENTS.md ("Out of Scope" table — the Exclusion | Reason table style to copy)
  </read_first>
  <action>
    1. Append to .gitignore (do not touch existing lines) a comment block in the repo's established style — comment explaining WHY (copyrighted game output, local-only, docs stay tracked), then three rules: `reference/captures/`, `reference/frames/`, `reference/annotated/`. Ignore ONLY the three media subdirectories — the tracked .md files and the ini snapshot live directly in reference/ and need no `!` exceptions. Exact block text is in 01-PATTERNS.md lines 43-49.
    2. Create `reference/TARGET-DEFINITION.md` with: (a) a one-sentence target statement containing the literal phrase "GS output as captured, not CRT" (the phase gate greps for "not CRT"); (b) a source-roles table with exactly three rows — PCSX2 PNG stills = color/brightness/geometry truth; PCSX2 clips = motion/timing truth only; YouTube FMGwS-bvNiU = move-selection + motion cross-check only, never color (note it is the PS3 Collection port per .planning/research/FEATURES.md, an additional reason it is motion-only); (c) an Exclusion | Reason table listing: CRT simulation/shaders, HDR bloom/tonemap/soft particles/motion blur/DoF, sRGB color management — each with the reason it breaks the PS2 period look (source: CLAUDE.md anti-features, REQUIREMENTS.md Out of Scope); (d) a rule line: any future CRT flavor is a clearly separated optional post-pass, never active during validation comparisons (P9 language); (e) italic dated footer naming what produced the doc (house style per 01-PATTERNS.md "Footer metadata line").
  </action>
  <verify>
    <automated>grep -q "reference/captures/" .gitignore && grep -q "reference/frames/" .gitignore && grep -q "reference/annotated/" .gitignore && grep -q "not CRT" reference/TARGET-DEFINITION.md && grep -q "FMGwS-bvNiU" reference/TARGET-DEFINITION.md</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the three lines `reference/captures/`, `reference/frames/`, `reference/annotated/` preceded by a comment block mentioning copyright
    - Existing .gitignore lines (ISO, extracted/**, node_modules/) are unchanged
    - reference/TARGET-DEFINITION.md contains the literal string "GS output as captured, not CRT"
    - reference/TARGET-DEFINITION.md contains a table row assigning YouTube FMGwS-bvNiU a motion-only role
    - reference/TARGET-DEFINITION.md contains an Exclusion | Reason table with a bloom/tonemap row
  </acceptance_criteria>
  <done>Repo is capture-safe (media unignorable-by-accident) and the target definition Phase 3/5/7 will cite is normative, before any capture exists.</done>
</task>

<task type="auto">
  <name>Task 2: Author reference/shot-list.md (capture matrix + session protocol)</name>
  <files>reference/shot-list.md</files>
  <read_first>
    - tools/kratos-lab/README.md (implemented move set and real clip names — shot IDs must reuse these: combo3A chain, Triangle launcher, Plume of Prometheus, Circle grab; PS2 pad mapping J/K/L/Space/Shift/R)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md (Pattern 3 progressive-scan procedure + fallback, Pattern 4 Level-1 combat path + save-state library, Pattern 5 "Three framings" definition, Pattern 2 calibration step)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md (section "reference/shot-list.md" — matrix columns and naming convention)
  </read_first>
  <action>
    Create `reference/shot-list.md` with two parts.
    PART A — capture matrix. Rows (shot IDs use game clip names): `idle` (~10s chain drape at rest), `combo3A` light chain (Square, full combo3A→3B→… string), `launcher` (Triangle hold), `plume` (Square Square Triangle — Plume of Prometheus), `grab` (Circle grab/throw — the white-hot extended-chain state). Columns per row: shot ID | in-game inputs (PS2 buttons) | framing (close / mid / wide) | save-state slot | capture type (still burst / clip / cadence burst) | notes | done checkbox. Coverage requirement encoded in the matrix: the three catalog subjects — chain at rest, chain mid-swing, fire close-up — must each appear at all three framings (>= 9 core stills), plus trail mid-swing stills and one >= 120-frame idle cadence burst, plus one clip per move. Framings defined measurably: close = Kratos >= 60% of frame height, mid = 35-60%, wide = < 35% (record which arena/save-state produces each; if no true close framing exists in early combat, relax the band and record actual measured Kratos-heights — label **inferred**).
    PART B — session protocol (the runbook the human follows in plan 01-03): (1) boot line `& "C:\Program Files\PCSX2\pcsx2-qt.exe" -fastboot -- "C:\Projects\GameDesignSkills\GodOfWarChains\God of War (USA).iso"`; (2) progressive-scan experiment: hold X + Triangle from after the PS2 logo; if the 480p prompt appears, accept and immediately save-state; if not, retry with `-slowboot`; if still not, stay 480i and take ALL stills via Pause -> Frame Advance -> F8 (record which route succeeded in the notes column); (3) FIRST capture is always the calibration screenshot (any in-game frame, F8, before anything else); (4) build the save-state library: one state per framing (wide arena / mid corridor / close), all strictly pre-upgrade "Blades Level 1, no orbs spent"; (5) capture workflow per row: load state, perform move, F8 bursts at pause+frame-advance for stills, Tools -> Video Capture for clips; (6) cadence burst: pause during idle-with-drawn-blades, frame-advance through >= 120 consecutive frames pressing F8 each frame; (7) hotkey cheat sheet table: F1 save state, F3 load state, F2 next slot, F8 screenshot, Pause + Frame Advance (bound during plan 01-02); (8) frame naming convention stated once: `<clip-id>_f<frame#>_<subject>_<framing>.png` (e.g. `combo3A_f0142_chain-midswing_mid.png`) — note the human does NOT rename; Claude ingests and renames from Documents\PCSX2\snaps afterward, human only keeps capture ORDER notes in the matrix notes column.
    End with the italic dated footer.
  </action>
  <verify>
    <automated>grep -q "combo3A" reference/shot-list.md && grep -qi "cadence" reference/shot-list.md && grep -Eq "X ?\+ ?Triangle" reference/shot-list.md && grep -q "60%" reference/shot-list.md && grep -q "_f<frame#>_" reference/shot-list.md</automated>
  </verify>
  <acceptance_criteria>
    - shot-list.md contains a matrix with rows for idle, combo3A, launcher, plume, grab
    - The three framings are defined with the numeric bands (>= 60%, 35-60%, < 35% Kratos frame-height)
    - The progressive-scan experiment (X + Triangle hold, -slowboot retry, 480i pause+frame-advance fallback) is written as numbered steps
    - The calibration-screenshot-first rule appears before any move capture step
    - The frame naming convention `<clip-id>_f<frame#>_<subject>_<framing>.png` appears verbatim
    - Every matrix row has a done checkbox and a save-state slot column
  </acceptance_criteria>
  <done>A human with PCSX2 configured (plan 01-02) can execute the entire capture campaign from this document alone, and Claude can ingest/rename the output deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> public GitHub (Pages enabled) | Anything git tracks gets published; captures are copyrighted game output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure (legal) | reference/ media dirs vs .gitignore | mitigate | This plan lands the ignore rules in Wave 1, before any capture file can exist (plan 01-03 is Wave 2 and depends on this plan); Task 1's automated verify greps all three rules |
| T-1-SC | Tampering | package installs | accept (n/a) | No npm/pip/cargo installs anywhere in this phase — RESEARCH.md Package Legitimacy Audit table confirms none |
</threat_model>

<verification>
- `git check-ignore reference/frames/x.png reference/captures/x.mp4 reference/annotated/x.png` exits 0 for all three paths
- All three tracked docs pass their grep gates (see task verifies)
- `git status --porcelain` shows only .gitignore and reference/*.md as changes
</verification>

<success_criteria>
- Repo is publish-safe for the capture phase: all three media dirs ignored
- TARGET-DEFINITION.md and shot-list.md exist, pass their grep gates, and contain no placeholder/TBD content
- Shot IDs match kratos-lab clip names so Phase 7 gets directly comparable clips
</success_criteria>

<output>
Create `.planning/phases/01-reference-pipeline-validation-criteria/01-01-SUMMARY.md` when done.
</output>
