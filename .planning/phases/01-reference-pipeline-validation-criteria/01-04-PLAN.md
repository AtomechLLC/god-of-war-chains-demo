---
phase: 01-reference-pipeline-validation-criteria
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - reference/MEASUREMENTS.md
  - reference/ACCEPTANCE.md
  - reference/annotated/ (gitignored media)
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "Every measured value cites its source frame file + pixel coordinates and applies the calibration factor — no un-cited numbers anywhere"
    - "Captured hues/pitches are cross-checked against decoded texture ground truths; systematic offsets are investigated, never averaged away"
    - "ACCEPTANCE.md is the complete 80-90% checklist Phase 7 judges against: stable IDs, measured targets, tolerances, test methods, zero TBDs"
    - "Colors are sampled ONLY from PNG stills, never video frames"
  artifacts:
    - path: "reference/MEASUREMENTS.md"
      provides: "Link pitch/counts, glow hex samples, trail geometry, flame flicker cadence — each row: metric | value | source citation | method"
      contains: "cycles/s"
    - path: "reference/ACCEPTANCE.md"
      provides: "Grouped binary checks (Chain links / Chain glow / Chain motion / Fire / Trails / Renderer invariants / Global) with ACC- IDs"
      contains: "ACC-"
  key_links:
    - from: "reference/ACCEPTANCE.md"
      to: "reference/MEASUREMENTS.md"
      via: "every checklist target cites its measured row"
      pattern: "MEASUREMENTS"
    - from: "reference/MEASUREMENTS.md"
      to: "reference/frames/*.png"
      via: "every value's source column names an existing frame file"
      pattern: "frames/"
---

<objective>
Extract the numbers from the freeze-frame library — link pitch and on-screen counts, glow/flame/trail hex anchors, trail geometry, flame flicker cadence — into MEASUREMENTS.md with full citations, then write ACCEPTANCE.md: the measured, tolerance-bearing, zero-TBD checklist that Phase 7's 80-90% verdict is judged against.

Purpose: ROADMAP success criteria 2, 3, 4 — the catalog is annotated, the measured values exist in writing, and the acceptance checklist is done. This closes VAL-01 and is the phase gate.
Output: `reference/MEASUREMENTS.md`, `reference/ACCEPTANCE.md`, annotated crops in `reference/annotated/` (gitignored).
</objective>

## Phase Goal

**As a** solo developer validating kratos-lab against the real game, **I want to** build an uncontaminated reference library with measured, written acceptance criteria, **so that** every later visual judgment compares against verified-clean ground truth.

(Restated 1:1 from the ROADMAP Phase 1 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md
@.planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@reference/SETTINGS.md
@reference/TARGET-DEFINITION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure geometry — link pitch, link counts, trail geometry</name>
  <files>reference/MEASUREMENTS.md, reference/annotated/</files>
  <read_first>
    - reference/SETTINGS.md (Calibration section — the mandatory horizontal scale factor)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md (Pattern 5 measurement methodology — link pitch and trail geometry procedures)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-PATTERNS.md (section "reference/MEASUREMENTS.md" — row format and provenance convention; ground-truth anchor list)
    - .planning/research/FEATURES.md (decoded anchors: chainlink 512x32, 16 links/tile, 32px pitch, ~25px link + ~7px gap; swordtrail 64x32 max #F3B012)
    - tools/kratos-lab/README.md (Data provenance table — the **measured**/**decoded**/**inferred** labeling convention to copy)
  </read_first>
  <action>
    Create `reference/MEASUREMENTS.md` with header (provenance-first opening naming SETTINGS.md as the pipeline spec and the calibration factor being applied), a `## Ground-truth anchors` table (the decoded values from FEATURES.md: chainlink pitch/geometry, chainglow ramp #FEE500 -> #FCBE00 -> #E44C00 -> #360600, swordtrail #F3B012, blade light 1.0/0.622/0.288 intensity 2.5 range ~160 — each labeled **decoded**), then the first two measurement sections:
    `## Chain links` — from an at-rest still and a mid-swing still at EACH of the three framings: visible link count per chain, pixel distance between N link centers (measure on 800% nearest-neighbor zooms: `magick <frame> -crop WxH+X+Y +repage -scale 800% reference/annotated/<name>-x8.png`, Read the zoom to count), pitch = distance/(N-1) converted to GS px via the calibration factor, chain on-screen width in GS px. Row format: metric | value (GS px) | source (`**measured** — frames/<file>.png @ (x,y), x<factor> calib`) | method. Cross-check: measured pitch should relate cleanly to the 32px/link texture truth at the chain's on-screen scale — if systematically off, investigate (wrong calib factor? wrong framing?) and document the resolution; never average a discrepancy away.
    `## Trail geometry` — from mid-swing stills at three framings: arc span as fraction of screen width, trail width vs blade length ratio, edge hardness (hard sprite edge expected — note any halation as a finding), and fade duration: frame-advance sequence frames (or clip frames) from swing end until trail gone, reported in frames @60Hz. Same row format and citation discipline.
    Save every annotated crop used for a measurement into reference/annotated/ named after its source frame.
  </action>
  <verify>
    <automated>grep -q "GS px" reference/MEASUREMENTS.md && [ "$(grep -c 'frames/' reference/MEASUREMENTS.md)" -ge 6 ] && grep -q "decoded" reference/MEASUREMENTS.md && ls reference/annotated/*.png >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - MEASUREMENTS.md has a Ground-truth anchors table containing "#FEE500" and "32" (px/link)
    - Chain links section has rows for at-rest AND mid-swing at close, mid, wide (>= 6 cited rows), each citing a frames/ file with coordinates and the calib factor
    - Trail geometry section includes a fade-duration row expressed in frames @60Hz
    - Every numeric row's source column starts with a bold provenance label (**measured**)
    - reference/annotated/ contains the 800% zoom crops used for counting
  </acceptance_criteria>
  <done>Phase 3 has its on-screen link-count/pitch targets; Phase 7 has trail geometry targets — all auditable to source frames.</done>
</task>

<task type="auto">
  <name>Task 2: Measure color and timing — glow hues, flame anchors, flicker cadence</name>
  <files>reference/MEASUREMENTS.md</files>
  <read_first>
    - reference/MEASUREMENTS.md (current state from Task 1 — append, keep format)
    - reference/TARGET-DEFINITION.md (source roles — colors from PNG stills ONLY)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md (Pattern 5 glow-hue sampling coordinates list and cadence procedure)
  </read_first>
  <action>
    Append two sections to MEASUREMENTS.md.
    `## Color anchors` — sample hex sRGB via `magick reference/frames/<f>.png -format "%[hex:p{X,Y}]" info:` at annotated coordinates for each of: chain-glow hot core (during attack/throw stills), glow falloff mid-point, dark link at rest, flame core, flame tip, trail edge. Sample from PNG stills exclusively — if any needed subject exists only in a clip, extract nothing: flag the gap for a supplemental still capture instead (color-from-video is forbidden by TARGET-DEFINITION.md). Cross-anchor each sampled hue against the decoded chainglow ramp / swordtrail max in the Ground-truth anchors table; captured hues should land on or near the ramp — record the comparison verdict per row; a systematic offset = capture or decode bug to investigate and document before recording.
    `## Flame flicker cadence` — from the idle-cadence burst (reference/frames/idle-cadence/, >= 120 frames @60Hz): identify flame pulse peaks by stepping through the frames (Read frames at candidate indices; brightness-sample the flame region with `magick ... -format "%[mean]"` on a fixed crop to make peak detection reproducible), list the peak frame numbers as evidence, compute cycles/s = 60 / mean-frames-per-cycle over >= 5 cycles. Record any visible flipbook frame-advance rate the same way. The literal token `cycles/s` must appear (phase gate greps for it).
  </action>
  <verify>
    <automated>grep -q "cycles/s" reference/MEASUREMENTS.md && grep -qE "#[0-9A-Fa-f]{6}" reference/MEASUREMENTS.md && grep -qi "idle-cadence" reference/MEASUREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Color anchors section has >= 6 sampled hex rows (hot core, falloff, dark link, flame core, flame tip, trail edge), each citing frame + (x,y)
    - Each color row records its cross-anchor verdict against the decoded ramp values
    - Cadence section lists explicit peak frame numbers and reports cycles/s over >= 5 cycles
    - No color row cites a .mp4/.mkv or any captures/ path (PNG stills only)
  </acceptance_criteria>
  <done>Phases 3/5/6/7 have measured color anchors and the flicker cadence target, every value auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Write ACCEPTANCE.md — the Phase-7 checklist (zero TBDs)</name>
  <files>reference/ACCEPTANCE.md</files>
  <read_first>
    - reference/MEASUREMENTS.md (final state — every checklist target cites a row here)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md (Pattern 6 — the full checklist structure with tolerances)
    - .planning/research/PITFALLS.md ("Looks Done But Isn't" checklist — the renderer-invariant items to include verbatim in spirit)
    - .planning/REQUIREMENTS.md (checkbox + bold-ID house style; VAL-01/VAL-02 wording)
  </read_first>
  <action>
    Create `reference/ACCEPTANCE.md`: the checklist Phase 7 judges the 80-90% verdict against. Grouped binary checks with stable IDs in REQUIREMENTS.md style:
    - `ACC-CHAIN-*` (links): on-screen link count within +/-10% of the measured value at each framing; alpha gaps visible; links read near-black warm metal at rest at all three framings.
    - `ACC-GLOW-*`: hot-core hue within the measured hex range; glow confined to the asset-driven streak (no screen-space halo); dark-at-rest / hot-in-attack behavior matches captures.
    - `ACC-MOT-*` (motion): drape shape at rest ~ capture overlay; whip C-curvature present mid-swing; settle without popping (A/B at 60fps).
    - `ACC-FIRE-*`: flicker cadence within +/-0.5 cycles/s of measured; flame silhouette hard-edged; fire hugs blade every combat frame.
    - `ACC-TRL-*`: fade duration within +/-20% of measured frames; stepped extrusion (no spline smoothness); crimson tint + white-hot core reads as in stills.
    - `ACC-REND-*` (renderer invariants, from PITFALLS "Looks Done But Isn't"): magenta-background test passes; particle density identical at 60Hz vs 144Hz; native-res toggle active during all judgments; additive stacks saturate to flat white.
    - `ACC-GLOBAL-*`: side-by-side at three framings, same moves, judged 80-90% in motion; YouTube consulted for motion only.
    Every item carries four fields: metric, measured target (naming its MEASUREMENTS.md row/section), tolerance, test method (A/B flicker / overlay / count). Fill every field from real measured values — if a measurement is missing, go back and measure it (Tasks 1-2 own MEASUREMENTS.md), do not write a placeholder. The literal string "TBD" must not appear anywhere. End with a traceability line to VAL-01/VAL-02 and the dated footer.
  </action>
  <verify>
    <automated>test -f reference/ACCEPTANCE.md && ! grep -q "TBD" reference/ACCEPTANCE.md && [ "$(grep -c 'ACC-' reference/ACCEPTANCE.md)" -ge 15 ] && grep -q "MEASUREMENTS" reference/ACCEPTANCE.md && grep -qi "magenta" reference/ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - ACCEPTANCE.md contains >= 15 ACC- checklist items across the seven groups
    - Zero occurrences of the string "TBD"
    - Tolerances present: "10%" (link count), "0.5 cycles/s" (cadence), "20%" (fade)
    - Renderer-invariant items include magenta-background, 60Hz-vs-144Hz density, native-res toggle, additive-saturation checks
    - Every quantitative item names its MEASUREMENTS.md source row/section
  </acceptance_criteria>
  <done>Phase 7 has its judgment instrument; VAL-01's written-criteria half is complete. Phase gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> public GitHub (Pages) | annotated/ crops are still copyrighted game imagery; docs must not embed image data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure (legal) | reference/annotated/ crops | mitigate | Dir already gitignored (plan 01-01); MEASUREMENTS.md cites filenames + hex values only, never embeds/base64s image content; final `git status --porcelain` gate before commit |
| T-1-SC | Tampering | package installs | accept (n/a) | No registry packages; optional ffmpeg CLI is NOT needed by this plan (ImageMagick + frame bursts cover everything); if ever installed later, winget `Gyan.FFmpeg` only per RESEARCH.md |
</threat_model>

<verification>
Phase-gate sweep (all must pass):
- `test -f reference/SETTINGS.md && grep -qi "renderer" reference/SETTINGS.md`
- `grep -q "not CRT" reference/TARGET-DEFINITION.md`
- `grep -q "cycles/s" reference/MEASUREMENTS.md`
- `test -f reference/ACCEPTANCE.md && ! grep -q "TBD" reference/ACCEPTANCE.md`
- Citation resolution: `grep -oE "frames/[A-Za-z0-9._/-]+\.png" reference/MEASUREMENTS.md | sort -u | while read f; do test -f "reference/$f" || echo "MISSING $f"; done` prints nothing
- `[ -z "$(git status --porcelain | grep -E 'reference/(frames|captures|annotated)/')" ]`
</verification>

<success_criteria>
- MEASUREMENTS.md: link pitch/counts, color anchors, trail geometry, flicker cadence — all cited to frames + coordinates, calibrated, cross-anchored against decoded ground truths
- ACCEPTANCE.md: complete, tolerance-bearing, zero-TBD checklist with stable IDs Phase 7 can reference
- Every VAL-01 clause satisfied; phase gate greps all green
</success_criteria>

<output>
Create `.planning/phases/01-reference-pipeline-validation-criteria/01-04-SUMMARY.md` when done.
</output>
