---
phase: 01-reference-pipeline-validation-criteria
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - reference/shot-list.md
  - reference/SETTINGS.md
  - reference/frames/ (gitignored media)
  - reference/captures/ (gitignored media)
autonomous: false
requirements: [VAL-01]

must_haves:
  truths:
    - "Freeze-frames exist for every shot-list row — chain at rest, mid-swing, and fire close-ups each at three framings — named per the citation convention"
    - "The capture-dimension calibration factor is recorded in SETTINGS.md BEFORE any measurement is taken (Pitfall 1)"
    - "No copyrighted media is tracked by git after the session"
    - "The progressive-vs-480i outcome and any deinterlace mode used are documented (Pitfall 2)"
  artifacts:
    - path: "reference/frames/"
      provides: ">= 9 core catalog stills (3 subjects x 3 framings) + trail stills + >=120-frame cadence burst, named <clip-id>_f<frame#>_<subject>_<framing>.png"
    - path: "reference/captures/"
      provides: "One motion clip per shot-list move at 59.94fps high bitrate"
    - path: "reference/SETTINGS.md"
      provides: "Filled Calibration section (actual WxH + horizontal scale factor) and progressive-scan outcome"
      contains: "scale factor"
    - path: "reference/shot-list.md"
      provides: "All done-checkboxes ticked with per-row capture notes"
  key_links:
    - from: "reference/frames/*.png"
      to: "reference/shot-list.md"
      via: "frame filenames embed the shot ID so MEASUREMENTS.md citations are self-locating"
      pattern: "_f[0-9]+_"
    - from: "reference/SETTINGS.md Calibration"
      to: "reference/frames/calib.png"
      via: "magick identify output recorded verbatim"
      pattern: "magick identify"
---

<objective>
Run the human capture session against the shot list, then ingest, rename, and verify the resulting library: calibrate actual screenshot dimensions (the 512-vs-640 width trap), check stills for interlace combing, organize frames per the citation naming convention, and confirm git tracks none of it.

Purpose: ROADMAP success criteria 1 and 2 — Level-1 blade combat is captured and the freeze-frame library catalogs chain at rest, mid-swing, and fire close-ups at three framings. Every plan-01-04 measurement cites these files.
Output: populated `reference/frames/` + `reference/captures/` (gitignored), calibration + progressive outcome in SETTINGS.md, fully ticked shot-list.md.
</objective>

## Phase Goal

**As a** solo developer validating kratos-lab against the real game, **I want to** build an uncontaminated reference library with measured, written acceptance criteria, **so that** every later visual judgment compares against verified-clean ground truth.

(Restated 1:1 from the ROADMAP Phase 1 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md
@reference/shot-list.md
@reference/SETTINGS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human capture session (play GoW1 per the shot list)</name>
  <files>external: Documents\PCSX2\snaps\*.png (F8 output), video capture output files; repo: reference/shot-list.md notes column (order notes)</files>
  <read_first>
    - reference/shot-list.md (the complete session protocol and capture matrix — authored in plan 01-01)
    - reference/SETTINGS.md (session start procedure: diff live ini against reference/PCSX2.ini first)
  </read_first>
  <what-built>The capture pipeline is configured and verified (plan 01-02), and shot-list.md contains the full session protocol: boot line, progressive-scan experiment, calibration-first rule, save-state plan, per-move capture steps, cadence-burst procedure, and hotkey cheat sheet.</what-built>
  <action>
    Human-performed — playing GoW1 combat on cue cannot be automated (PCSX2 mainline has no supported input-playback workflow, and the shots require human timing/judgment). The human executes reference/shot-list.md PART B exactly as written; Claude ingests, renames, calibrates, and verifies everything afterward in Tasks 2-3. The human only plays, presses F8/records, and keeps order notes.
  </action>
  <how-to-verify>
    Follow reference/shot-list.md PART B top to bottom. Summary of the non-skippable rules:
    1. Diff check first: confirm PCSX2 settings match reference/PCSX2.ini (SETTINGS.md session start procedure).
    2. Progressive experiment: hold X + Triangle after the PS2 logo; note in the shot-list which route worked (480p accepted / -slowboot / 480i fallback).
    3. FIRST F8 screenshot = the calibration frame, before anything else.
    4. New Game -> Aegean Sea opening. Capture EVERYTHING before spending any red orbs ("Blades Level 1, no orbs spent").
    5. Build the three save states (wide / mid / close framing spots) and note slot numbers in the matrix.
    6. Per matrix row: perform the move; stills via Pause -> Frame Advance -> F8 bursts; one clip per move via Tools -> Video Capture. Cover chain-at-rest, chain-mid-swing, and fire close-up at ALL THREE framings, plus trail mid-swing stills.
    7. Cadence burst: >= 120 consecutive frame-advance F8 shots during idle-with-drawn-blades.
    8. In the matrix notes column, record capture ORDER per row (e.g. "shots 14-32") — Claude maps raw snap filenames to shots from these notes. Do not rename files.
    9. Move/copy nothing else; leave screenshots in Documents\PCSX2\snaps and clips where the capture dialog saved them, and tell Claude both paths.
  </how-to-verify>
  <verify>
    <automated>ls "$USERPROFILE/Documents/PCSX2/snaps/"*.png >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - PCSX2 snaps directory contains PNG screenshots including the calibration frame (first by timestamp)
    - At least one video clip file exists from Tools -> Video Capture
    - shot-list.md matrix notes column has an order note for every captured row
    - Human reports: progressive-scan outcome, save-state slot numbers, snaps + clips paths
  </acceptance_criteria>
  <done>The raw capture session output exists on disk (snaps + clips), order notes map raw files to shot rows, and the human has replied "captured" with the progressive outcome and paths.</done>
  <resume-signal>Type "captured" plus: progressive outcome (480p / 480i+mode), the snaps and clips paths, and any rows you could not capture (e.g. no true close framing found).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Ingest, rename, and catalog the library</name>
  <files>reference/frames/, reference/captures/, reference/shot-list.md</files>
  <read_first>
    - reference/shot-list.md (matrix order notes from Task 1 — the filename mapping key; and the naming convention in PART B)
    - reference/SETTINGS.md (paths and session context)
  </read_first>
  <action>
    1. Create reference/frames/ and reference/captures/ (no .gitkeep files — these trees are ignored; per 01-PATTERNS.md do not put tracked files inside them).
    2. Copy screenshots from the snaps path (from Task 1 resume message) into reference/frames/, renaming per `<clip-id>_f<frame#>_<subject>_<framing>.png`. Map raw files to shot rows using: timestamp order + the matrix order notes + visual inspection (Read each candidate PNG to confirm subject and framing — e.g. chain drape vs mid-swing vs flame close-up). Keep the calibration frame as `calib.png` (first screenshot by timestamp). Put the cadence burst under `reference/frames/idle-cadence/` keeping frame order in the filename (`idle_f0001_...` sequential).
    3. Copy clips into reference/captures/ named `<clip-id>_<framing>.<ext>`.
    4. Update shot-list.md: tick the done checkbox for every row whose frames/clip landed; for any row the human reported un-capturable, leave unticked and add a note line (e.g. "close framing relaxed to 55% — **inferred** band" per the shot-list's framing-relaxation rule).
    5. Verify coverage: the three catalog subjects (chain-rest, chain-midswing, fire) each present at three framings — if a gap exists that the human did NOT flag, stop and report; do not silently accept a hole in the catalog.
  </action>
  <verify>
    <automated>test -f reference/frames/calib.png && [ "$(ls reference/frames | grep -Ec '_f[0-9]+_.+_(close|mid|wide)\.png')" -ge 9 ] && ls reference/captures/* >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - reference/frames/calib.png exists
    - >= 9 files in reference/frames/ match the `<clip-id>_f<frame#>_<subject>_<framing>.png` pattern covering all three framings (close, mid, wide each appear >= 3 times across subjects)
    - reference/frames/idle-cadence/ contains >= 120 sequentially named frames
    - reference/captures/ contains >= 1 clip per captured move row
    - Every captured shot-list row's checkbox is ticked; uncaptured rows carry an explanatory note
  </acceptance_criteria>
  <done>The freeze-frame library is organized and self-citing: any MEASUREMENTS.md value can name its frame file and be audited.</done>
</task>

<task type="auto">
  <name>Task 3: Calibrate dimensions, check contamination, confirm git safety</name>
  <files>reference/SETTINGS.md</files>
  <read_first>
    - reference/SETTINGS.md (the Calibration section stub from plan 01-02)
    - reference/frames/calib.png (via `magick identify` — and Read it to sanity-check content)
    - .planning/phases/01-reference-pipeline-validation-criteria/01-RESEARCH.md (Pattern 2 calibration procedure, Pitfall 1, Pitfall 2, assumption A5)
  </read_first>
  <action>
    1. Run `magick identify reference/frames/calib.png`; record the verbatim output in SETTINGS.md's Calibration section. Compute and record the horizontal scale factor: 640-wide => GS px = screenshot px x (512/640) = x0.8; 512-wide => x1.0; any other width => record it, derive factor as 512/width, and flag it as unexpected. State explicitly: "every horizontal pixel measurement in MEASUREMENTS.md multiplies by this factor".
    2. Record the progressive-scan outcome (from Task 1 resume message) in SETTINGS.md: which route succeeded; if 480i fallback, record the deinterlacing mode used for clips and that all stills were taken paused via frame advance.
    3. Combing check (assumption A5): pick the fastest-motion still (a mid-swing trail frame), crop a 128x64 region on the blade/chain edge, `-scale 800%` into the scratchpad, Read the zoomed image, and inspect for horizontal field-comb double-edges. Record verdict in SETTINGS.md ("no combing at 800% on <frame>" or, if combing IS visible, mark affected stills unusable for geometry, and report that re-capture in progressive mode is required — this re-opens Task 1).
    4. Git safety: run `git status --porcelain` and confirm zero entries under reference/frames/, reference/captures/, reference/annotated/; run `git check-ignore reference/frames/calib.png` (exit 0). If any media path shows as untracked-but-listable by status, the plan 01-01 ignore rules are broken — stop and fix before any commit.
  </action>
  <verify>
    <automated>grep -qi "scale factor" reference/SETTINGS.md && grep -q "magick identify" reference/SETTINGS.md && git check-ignore -q reference/frames/calib.png && [ -z "$(git status --porcelain | grep -E 'reference/(frames|captures|annotated)/')" ]</automated>
  </verify>
  <acceptance_criteria>
    - SETTINGS.md Calibration section quotes the verbatim `magick identify` output and states the numeric scale factor (0.8 or 1.0 or derived)
    - SETTINGS.md records the progressive-scan outcome and (if 480i) the deinterlace mode used for clips
    - SETTINGS.md records the 800%-zoom combing verdict naming the inspected frame
    - `git status --porcelain` contains no reference/frames|captures|annotated paths
  </acceptance_criteria>
  <done>Measurements can begin: the pixel space is calibrated, stills are verified comb-free (or flagged), and the repo is publish-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> public GitHub (Pages) | Captured game frames/clips are copyrighted output; must stay local-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure (legal) | reference media landing in git | mitigate | Task 3 runs `git check-ignore` + `git status --porcelain` gates after media exists; commits in this plan list files explicitly (shot-list.md, SETTINGS.md only) |
| T-1-SC | Tampering | package installs | accept (n/a) | No registry packages installed (RESEARCH.md audit: none); no downloads in this plan |
</threat_model>

<verification>
- All three task verifies green
- Library coverage: 3 subjects x 3 framings present (or gaps documented as human-flagged with **inferred** relaxations)
- `git log --stat -1` for this plan's commits shows only tracked .md files
</verification>

<success_criteria>
- Freeze-frame library exists per shot list, self-citing filenames, cadence burst included
- Calibration factor + progressive outcome + combing verdict recorded in SETTINGS.md before any measurement
- Zero media files visible to git
</success_criteria>

<output>
Create `.planning/phases/01-reference-pipeline-validation-criteria/01-03-SUMMARY.md` when done.
</output>
