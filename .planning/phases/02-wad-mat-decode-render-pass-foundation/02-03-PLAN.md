---
phase: 02-wad-mat-decode-render-pass-foundation
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - tools/kratos-lab/loop.js
  - tools/kratos-lab/test/loop.test.js
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: true
requirements: [REND-03]

must_haves:
  truths:
    - "Simulation advances in exact 1/60s steps regardless of display refresh rate — proven by a Node test feeding 60Hz, 144Hz, and stall-shaped wall-clock deltas to the pure accumulator"
    - "A 2-second stall produces at most 15 catch-up steps (0.25s frame clamp) — no spiral of death"
    - "Camera autospin and distance easing remain wall-clock presentation — camera feel does not change with monitor refresh rate"
    - "KratosLab.step() stays deterministic for verification scripts: exactly one fixed sim step plus one render"
  artifacts:
    - path: "tools/kratos-lab/loop.js"
      provides: "Loop.makeAccumulator({step, maxFrame}) pure fixed-timestep accumulator + Loop.STEP"
      contains: "makeAccumulator"
    - path: "tools/kratos-lab/test/loop.test.js"
      provides: "Known-answer accumulator tests: 60Hz, 144Hz, stall clamp, remainder carry"
      contains: "144"
    - path: "tools/kratos-lab/app.js"
      provides: "simStep/renderFrame split; rAF loop consuming accumulator; simStepCount hook"
      contains: "Loop.makeAccumulator"
  key_links:
    - from: "tools/kratos-lab/app.js"
      to: "tools/kratos-lab/loop.js"
      via: "accum = Loop.makeAccumulator(...) in the main loop"
      pattern: "Loop\\.makeAccumulator"
    - from: "tools/kratos-lab/test/loop.test.js"
      to: "tools/kratos-lab/loop.js"
      via: "Node require through the module.exports guard"
      pattern: "require\\(.*loop"
    - from: "tools/kratos-lab/app.js simStep"
      to: "combat machine"
      via: "machine.tick(1/60) inside simStep only"
      pattern: "machine\\.tick"
---

<objective>
Replace the variable-dt rAF loop with a fixed 1/60s accumulator (sim decoupled from render), splitting app.js into sim consumers (machine.tick, pose, blade tracks, trail history, heat) and wall-clock presentation (autospin, camera easing) — with the accumulator as a pure Node-tested function in a new `loop.js`.

Purpose: REND-03's timestep half. Rates/lifetimes in Phases 4-6 are authored per-tick; retrofitting a fixed step later means re-tuning everything (PITFALLS P5 — highest retrofit cost in the project). Trail history must be recorded per sim tick now so TRL-01's stepped-60Hz extrusion works later.
Output: `loop.js` (pure accumulator), `test/loop.test.js`, restructured app.js loop, simStepCount hook.
</objective>

## Phase Goal

**As a** solo developer building kratos-lab toward footage-grade fidelity, **I want to** drive every FX draw from its material's real decoded blend/depth state with the PS2-authentic rendering conventions locked, **so that** no visual tuning ever happens on top of approximated render state.

(Restated 1:1 from the ROADMAP Phase 2 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md
@.planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md
@tools/kratos-lab/app.js
</context>

<interfaces>
<!-- Contract this plan creates: -->
```javascript
// tools/kratos-lab/loop.js — pure, no DOM/GL, Node-requireable
Loop.STEP                                     // 1 / 60
Loop.makeAccumulator({ step = 1/60, maxFrame = 0.25 } = {})
  // -> { advance(dtSeconds) -> integer stepCount }
  // advance() adds min(dtSeconds, maxFrame) to an internal accumulator,
  // returns floor(acc / step), keeps the fractional remainder for the next call.
```

<!-- app.js internal split (consumed by plan 02-04's renderFrame wrap): -->
```javascript
simStep()            // fixed 1/60: lastState capture, machine.tick(STEP), heat decay,
                     // pose compute -> skin.lastWorld, blade track sample + driveBlade,
                     // trail age/shift/push, skin.blendLeft -= STEP, simStepCount++
renderFrame(wallDt)  // per rAF: canvas resize, autoSpin yaw, auto-frame dist easing,
                     // uploadSkinnedVerts() (skinPose + blend mix + bufferSubData), draw passes, drawFx
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accumulator test first, then loop.js</name>
  <files>tools/kratos-lab/test/loop.test.js, tools/kratos-lab/loop.js, tools/kratos-lab/index.html</files>
  <read_first>
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Pattern 6 — the accumulator body with the 0.25s stall clamp; §Validation Architecture REND-03 row — the accumulator must be a pure exported function)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md (sections "test/wad.test.js + test/loop.test.js" and "Module shell + dual-environment export")
    - tools/kratos-lab/fxparse.js (module shell + export guard convention from plan 02-01 — mirror it)
    - tools/kratos-lab/index.html (script block — insert loop.js before app.js, bump cache-buster)
  </read_first>
  <behavior>
    - 600 calls of advance(1/60) yield a total of 600 ± 1 steps (float drift tolerated, never more)
    - 144 calls of advance(1/144) yield 60 ± 1 steps total (144Hz display still sims at 60Hz)
    - advance(2.0) returns exactly 15 steps (2s stall clamped to maxFrame 0.25 → floor(0.25 / (1/60)))
    - advance(0.001) returns 0 and the remainder carries: enough consecutive 1ms calls eventually return 1
    - advance never returns a negative number; advance(0) returns 0
    - Custom step/maxFrame options are honored (e.g. step 0.1, maxFrame 0.3 → advance(1.0) === 3)
  </behavior>
  <action>
    RED first: create tools/kratos-lab/test/loop.test.js with node:assert asserting every behavior above via `require("../loop.js")`. Run `node tools/kratos-lab/test/loop.test.js` — must fail (loop.js absent). Commit red.

    GREEN: create tools/kratos-lab/loop.js as `const Loop = (() => { const STEP = 1 / 60; function makeAccumulator({ step = STEP, maxFrame = 0.25 } = {}) { let acc = 0; return { advance(dt) { acc += Math.min(Math.max(dt, 0), maxFrame); let n = 0; while (acc >= step) { acc -= step; n++; } return n; } }; } return { STEP, makeAccumulator }; })();` plus the module.exports guard. Keep it pure (no DOM, no GL, no performance.now — wall time arrives as a parameter) and commented as the "Fix Your Timestep" pattern with the stall-clamp rationale (course-exercise readability). index.html: insert `<script src="loop.js?v=19"></script>` after fx.js, before app.js; bump all tags to ?v=19. Run the test — green.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/loop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/loop.test.js` exits 0; a red commit precedes the green commit for test/loop.test.js
    - loop.js exports STEP and makeAccumulator through the module.exports guard (grep both identifiers)
    - `grep -Eq "document\.|window\.|performance\." tools/kratos-lab/loop.js` exits 1 (pure module)
    - index.html loads loop.js before app.js at ?v=19
  </acceptance_criteria>
  <done>The fixed-timestep core is a pure, tested function — 60Hz semantics are provable without a browser.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure the app.js loop — simStep / renderFrame split on the accumulator</name>
  <files>tools/kratos-lab/app.js</files>
  <read_first>
    - tools/kratos-lab/app.js (lines 804-818 — the variable-dt loop being replaced; lines 419-457 updateSkinning — the sim/render mix to split; lines 539-580 render() — resize/autoSpin/easing at its head; lines 370-400 driveBlade; lines 820-825 KratosLab.step hook whose meaning must be preserved)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Pattern 6 — the target loop shape and the sim/derived-state split; §Pitfall 5 — camera/autoSpin stay wall-clock, KratosLab.step semantics)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md (section "app.js modification sites" item 2 — the line-pinned sim vs presentation split list)
  </read_first>
  <action>
    Split per PATTERNS item 2 and RESEARCH Pattern 6:

    1. `simStep()` (fixed dt = Loop.STEP, no parameter): move from step() — lastState capture (line 806), `machine.tick(STEP)` (line 807), heat decay (line 808). Move from updateSkinning — the sim half: `const world = rig.computePose(machine.st.current, machine.st.t); skin.lastWorld = world;`, the blade block (track sample, `driveBlade(bladeSim[key], world, hand, tp, STEP)`, trail aging/shift and attack-time trail push, lines 441-455), and the blend-window bookkeeping `skin.blendLeft -= STEP` (moved out of the render-side mix). Increment a `simStepCount` counter. Trail history now advances per sim tick — this is load-bearing for TRL-01 later; do not leave it in the render path.

    2. `uploadSkinnedVerts()` (render-side, derived state): the remaining updateSkinning body — prev-pose evaluation when `skin.blendLeft > 0 && skin.prevAct` (skinPose of the prev act), `skinPose(skin.lastWorld)`, the mix with `f = 1 - skin.blendLeft / skin.blendDur` (read blendLeft, do NOT decrement here), and the `bufferSubData` upload. CPU skinning is the expensive part — it runs once per rendered frame, not per sim tick.

    3. `renderFrame(wallDt)`: the current render(dt) body with `updateSkinning(dt)` replaced by `uploadSkinnedVerts()`; autoSpin yaw (line 544) and the auto-frame dist easing (lines 554-556) keep consuming wallDt (presentation feel unchanged across refresh rates — RESEARCH Pitfall 5).

    4. Main loop: `const accum = Loop.makeAccumulator({ step: Loop.STEP, maxFrame: 0.25 }); let last = performance.now(); function loop(now) { const wallDt = (now - last) / 1000; last = now; const n = accum.advance(wallDt); for (let i = 0; i < n; i++) simStep(); renderFrame(wallDt); renderTimeline(); requestAnimationFrame(loop); }`. Delete the old `dt = Math.min(0.05, …)` clamp and the old step(dt). Render runs every rAF (camera/autospin stay smooth); sim runs 0..n fixed steps — on a 144Hz display the sim still advances exactly 60 steps/s.

    5. Hooks: redefine `KratosLab.step` as `step() { simStep(); renderFrame(Loop.STEP); renderTimeline(); }` with a comment: one deterministic fixed sim step + one render, for automated verification (the old dt parameter is gone — document that in the comment). Add `KratosLab.simStepCount` (getter returning the counter) and `KratosLab.STEP = Loop.STEP`.

    Sanity notes: `machine.tick` and heat already take dt values — passing the constant STEP changes cadence, not APIs; combat.js is untouched. driveBlade's `Math.max(dt, 1e-3)` guard may stay (STEP is constant and > 1e-3). The `frame N @30fps` timeline readout derives from machine.st.t and keeps working.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/loop.test.js && node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js && grep -q "Loop.makeAccumulator" tools/kratos-lab/app.js && grep -q "function simStep" tools/kratos-lab/app.js && grep -q "uploadSkinnedVerts" tools/kratos-lab/app.js && ! grep -q "Math.min(0.05" tools/kratos-lab/app.js && grep -q "simStepCount" tools/kratos-lab/app.js</automated>
  </verify>
  <acceptance_criteria>
    - app.js contains `simStep`, `renderFrame`, `uploadSkinnedVerts`, and `Loop.makeAccumulator`; the old variable-dt clamp `Math.min(0.05` is gone
    - `machine.tick` is called exactly once in app.js, inside simStep, with the fixed STEP constant (grep -c "machine.tick" returns 1)
    - Trail history push/age and driveBlade calls live inside simStep (not renderFrame/uploadSkinnedVerts): grep context around "trailHist" shows it under simStep
    - autoSpin yaw and dist easing consume the wall-clock dt parameter of renderFrame (grep shows them inside renderFrame)
    - `skin.blendLeft` is decremented in simStep and only read in uploadSkinnedVerts
    - window.KratosLab exposes step, simStepCount, and STEP; all three Node suites green
  </acceptance_criteria>
  <done>Sim runs on a fixed 60Hz accumulator decoupled from rAF; presentation stays wall-clock; per-tick trail recording is in place for later phases. Browser-side 60±1 steps/s confirmation happens at plan 02-04's checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wall clock → sim | Stalls/tab-switches deliver huge dt values to the accumulator |
| verification scripts → KratosLab hooks | Automated checks depend on stable hook semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-08 | DoS (spiral of death) | accumulator catch-up | mitigate | maxFrame 0.25s clamp caps catch-up at 15 steps; Node test asserts advance(2.0) === 15; negative dt clamped to 0 |
| T-2-09 | Tampering (semantic drift) | KratosLab.step hook | mitigate | Hook redefined explicitly as one fixed step + one render with an in-code comment; simStepCount gives scripts an independent 60Hz witness |
| T-2-SC | Tampering | package installs | accept (n/a) | Zero installs; loop.js is ~20 lines of vanilla JS |
</threat_model>

<verification>
- All three Node suites green: wad.test.js, fx.test.js, loop.test.js
- `git status --porcelain` shows only files in files_modified
- Manual smoke (optional, non-blocking): open the lab, trigger combo — motion cadence unchanged, no console errors
</verification>

<success_criteria>
- Phase success criterion 4 (timestep half): simulation runs on a fixed 60Hz accumulator decoupled from rAF, proven at 60Hz/144Hz/stall inputs by `node tools/kratos-lab/test/loop.test.js`
- Trail history records per sim tick (TRL-01 prerequisite); camera feel unchanged
- KratosLab.step remains a deterministic verification hook
</success_criteria>

<output>
Create `.planning/phases/02-wad-mat-decode-render-pass-foundation/02-03-SUMMARY.md` when done.
</output>
