---
phase: 02-wad-mat-decode-render-pass-foundation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tools/kratos-lab/fx.js
  - tools/kratos-lab/test/fx.test.js
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: false
requirements: [DEC-01, REND-01]

must_haves:
  truths:
    - "The chain draw's blend/depth state comes from decoded MAT_chainlink (usual, depth-write ON) and the trail's from MAT_swordtrail (additive, depth-write OFF) — no hardcoded blendFunc/depthMask calls remain in drawFx"
    - "An unmapped blend mode throws a named error instead of silently defaulting to alpha blend"
    - "Changing the page background color does not alter the render (context created with alpha:false)"
    - "After the FX block every frame, GL state is restored: blendEquation FUNC_ADD, depthMask true, BLEND disabled"
    - "Stacked additive trail layers saturate toward flat white — no bloom, no tonemap, no sRGB anywhere"
  artifacts:
    - path: "tools/kratos-lab/fx.js"
      provides: "Fx.applyMaterial(gl, mat) MAT→GL mapping table + Fx.restoreFxState(gl); throw-on-unknown contract"
      contains: "applyMaterial"
    - path: "tools/kratos-lab/test/fx.test.js"
      provides: "Mock-gl Node test proving the exact GL calls per mode and the unknown-mode throw"
      contains: "blendFuncSeparate"
    - path: "tools/kratos-lab/app.js"
      provides: "alpha:false context, opaque clear, uMaterialColor/uLayerColor/uCutoff FX shader, drawFx driven by MatDb, KratosLab fxLog + fxState hooks"
      contains: "alpha: false"
  key_links:
    - from: "tools/kratos-lab/app.js"
      to: "tools/kratos-lab/fx.js"
      via: "Fx.applyMaterial called per FX pass in drawFx"
      pattern: "Fx\\.applyMaterial"
    - from: "tools/kratos-lab/app.js"
      to: "MatDb (plan 02-01)"
      via: "matDb.byName.MAT_chainlink / MAT_swordtrail passed to applyMaterial"
      pattern: "byName\\.MAT_chainlink"
    - from: "tools/kratos-lab/test/fx.test.js"
      to: "tools/kratos-lab/fx.js"
      via: "Node require through the module.exports guard"
      pattern: "require\\(.*fx"
---

<objective>
Replace the hardcoded `uAdd` FX state with the single MAT→GL mapping table driven by decoded MAT records, and lock the PS2-authentic compositing invariants: `alpha:false` context, opaque clear, TFX-MODULATE-shaped shader math with uMaterialColor/uLayerColor, full state restore after the FX block. A human checkpoint verifies the live blend state, the magenta-background test, and additive saturation to flat white.

Purpose: DEC-01's render half ("every FX draw uses its material's real blend/depth state") and REND-01 (PS2-authentic compositing, locked before any visual tuning). The chain draw gains its real usual-blend + depth-write-ON state; the trail's former guess becomes data-confirmed.
Output: new `fx.js` + mock-gl test, reworked FX shader and drawFx in app.js, context/clear changes, verification hooks.
</objective>

## Phase Goal

**As a** solo developer building kratos-lab toward footage-grade fidelity, **I want to** drive every FX draw from its material's real decoded blend/depth state with the PS2-authentic rendering conventions locked, **so that** no visual tuning ever happens on top of approximated render state.

(Restated 1:1 from the ROADMAP Phase 2 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md
@.planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md
@tools/kratos-lab/app.js
@CLAUDE.md
</context>

<interfaces>
<!-- Contract consumed from plan 02-01 (already implemented): -->
```javascript
// mat objects from FxParse.buildMats():
// { name, mode: "usual"|"additive"|"subtract"|"strange", disableDepthWrite: bool,
//   filter, haveTexture, texName, blendColor: [r,g,b,a], materialColor: [r,g,b], rawFlags0 }
// app.js scope already has: matDb.byName.MAT_chainlink / MAT_chainglow / MAT_swordtrail
```

<!-- Contract this plan creates (fx.js — Phase 3/6 will reuse for chainglow + particles): -->
```javascript
Fx.applyMaterial(gl, mat)   // sets FULL state: enable(BLEND) + blendEquation + blendFuncSeparate + depthMask(!mat.disableDepthWrite)
                            // usual:    FUNC_ADD + blendFuncSeparate(SRC_ALPHA, ONE_MINUS_SRC_ALPHA, ONE, ONE)
                            // additive: FUNC_ADD + blendFuncSeparate(SRC_ALPHA, ONE, ONE, ONE)
                            // subtract: FUNC_REVERSE_SUBTRACT + blendFunc(SRC_ALPHA, ONE)   [untested-but-present, hero-side MATs may need it]
                            // "strange" or anything else: throw Error(`Unmapped blend mode '${mat.mode}' in ${mat.name}`)
Fx.restoreFxState(gl)       // blendEquation(FUNC_ADD); depthMask(true); disable(BLEND)
// fx.js has NO top-level gl/DOM access (gl arrives as a parameter) -> Node-requireable with a mock gl
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mock-gl test first, then fx.js matgl table</name>
  <files>tools/kratos-lab/test/fx.test.js, tools/kratos-lab/fx.js, tools/kratos-lab/index.html</files>
  <read_first>
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Pattern 3 — the MATGL table to lift verbatim including its comments; §Pitfall 4 state leaks; §Pitfall 7 — subtract stays present, strange throws; the assert is the contract, never remove it)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md (section "fx.js" — the drawFx blend block it replaces; "Shared Patterns > GL state set/restore per pass"; "Module shell + dual-environment export")
    - tools/kratos-lab/fxparse.js (module shell + export-guard style established by plan 02-01 — mirror it)
    - tools/kratos-lab/index.html (script block — insert fx.js after fxparse.js, bump cache-buster)
    - CLAUDE.md (Part 1 ABCD→WebGL table — the authority the mapping encodes; keep the shader-premultiply note for alpha>0x80 as a comment for Phase 5)
  </read_first>
  <behavior>
    - applyMaterial(mockGl, {name:"MAT_chainlink", mode:"usual", disableDepthWrite:false}) records, in order: enable(BLEND), blendEquation(FUNC_ADD), blendFuncSeparate(SRC_ALPHA, ONE_MINUS_SRC_ALPHA, ONE, ONE), depthMask(true)
    - applyMaterial(mockGl, {name:"MAT_swordtrail", mode:"additive", disableDepthWrite:true}) records blendFuncSeparate(SRC_ALPHA, ONE, ONE, ONE) and depthMask(false)
    - applyMaterial(mockGl, {name:"MAT_Csmoke", mode:"subtract", disableDepthWrite:true}) records blendEquation(FUNC_REVERSE_SUBTRACT) and blendFunc(SRC_ALPHA, ONE)
    - applyMaterial(mockGl, {name:"MAT_x", mode:"strange"}) and mode:"garbage" both throw /Unmapped blend mode/ containing the material name
    - restoreFxState(mockGl) records blendEquation(FUNC_ADD), depthMask(true), disable(BLEND)
    - depthMask value derives ONLY from disableDepthWrite, never from the mode (RESEARCH anti-pattern: bit 19 is a separate flag)
  </behavior>
  <action>
    RED first: create tools/kratos-lab/test/fx.test.js (node:assert only). Build a mock gl: an object carrying the real WebGL enum values as properties (BLEND 0x0BE2, FUNC_ADD 0x8006, FUNC_REVERSE_SUBTRACT 0x800B, SRC_ALPHA 0x0302, ONE_MINUS_SRC_ALPHA 0x0303, ONE 1) plus recording stubs for enable, disable, blendEquation, blendFunc, blendFuncSeparate, depthMask that push `[methodName, ...args]` onto a calls array. Assert all behaviors above against the mock's own constants (never hardcode the numbers twice). Run `node tools/kratos-lab/test/fx.test.js` — must fail (fx.js absent). Commit red.

    GREEN: create tools/kratos-lab/fx.js as `const Fx = (() => { ... return { applyMaterial, restoreFxState }; })();` + module.exports guard. Lift RESEARCH Pattern 3 verbatim: internal MATGL table keyed by mode with usual/additive/subtract entries; applyMaterial looks up mat.mode, throws `Unmapped blend mode '${mat.mode}' in ${mat.name}` when absent, applies the table entry, then `gl.depthMask(!mat.disableDepthWrite)`. Include the Pattern 3 comments (depthWrite carried separately from mode; premultiply + ONE,ONE documented for future alpha>0x80 FIX cases — architecture must not preclude it). No top-level gl/DOM references — the file must be Node-requireable. Add restoreFxState per the Shared Pattern (FUNC_ADD + depthMask(true) + disable(BLEND) — gl.clear respects depthMask, so a leaked false mask breaks next frame's depth clear).

    index.html: insert `<script src="fx.js?v=18"></script>` after fxparse.js, before app.js; bump all tags ?v=17 → ?v=18. Run the test — green. Also re-run wad.test.js (regression).
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/wad.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/kratos-lab/test/fx.test.js` exits 0; a red commit precedes the green commit in git log for test/fx.test.js
    - fx.js contains the string `Unmapped blend mode` and a `subtract` table entry (grep both)
    - `grep -Eq "document\.|window\." tools/kratos-lab/fx.js` exits 1 (no DOM); no top-level `gl` identifier outside function parameters
    - index.html loads fx.js between fxparse.js and app.js at ?v=18
  </acceptance_criteria>
  <done>The single MAT→GL mapping table exists as the ONLY place PS2 blend semantics touch WebGL calls, with its throw-on-unknown contract machine-verified.</done>
</task>

<task type="auto">
  <name>Task 2: alpha:false context, MODULATE-shaped FX shader, drawFx driven by MatDb</name>
  <files>tools/kratos-lab/app.js</files>
  <read_first>
    - tools/kratos-lab/app.js (lines 100-101 context creation — the ONE place alpha:false can go, RESEARCH Pitfall 3; line 321 clearColor; lines 289-313 FX program/shader/fxLocs; lines 470-537 drawFx — the uAdd pushes and hardcoded blend block being replaced; lines 820-825 KratosLab hooks)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Pattern 4 — per-stage 0x80 audit incl. "already correct, do not fix" rows; §Pattern 5 — canvas/compositing invariants and the magenta test; Assumption A3 — cutout threshold is INFERRED)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md (sections "app.js modification sites" items 1, 3, 8; "fx.js" shader excerpt showing the uAdd branch being replaced)
  </read_first>
  <action>
    Context (line 101): add `alpha: false` as the first attribute — `getContext("webgl", { alpha: false, antialias: true, preserveDrawingBuffer: true })` (attributes fix at first creation; this is the only getContext call). Line 321: `gl.clearColor(0, 0, 0, 1)`. Add a one-line comment at the context site locking the gamma stance: naive 8-bit gamma-space math IS the target — no sRGB, no tonemap (authority: reference/TARGET-DEFINITION.md).

    FX fragment shader (lines 296-304): replace uAdd with three uniforms — `uniform vec3 uMaterialColor; uniform vec4 uLayerColor; uniform float uCutoff;`. New body: sample `vec4 c = texture2D(uTex, vT.xy);` then `vec3 rgb = c.rgb * uLayerColor.rgb * uMaterialColor;` and `float a = c.a * uLayerColor.a * vT.z;` then `if (a < uCutoff) discard;` then `gl_FragColor = vec4(rgb, a);`. This is TFX MODULATE shape (tex × layer × material, multiplied in-shader BEFORE blending so decoded overbright values like 2.0 survive — RESEARCH Pattern 4). Comment the discard line: cutout 0.35 is INFERRED — GS TEST-register alpha test is not in MAT records (RESEARCH A3); Phase 5's GS dump reads the real value. Update fxLocs (lines 306-313): drop uAdd; add uMaterialColor, uLayerColor, uCutoff.

    drawFx (lines 470-537): chain pass — before the draw call: `Fx.applyMaterial(gl, matDb.byName.MAT_chainlink);` then `gl.uniform3fv(fxLocs.uMaterialColor, matDb.byName.MAT_chainlink.materialColor); gl.uniform4fv(fxLocs.uLayerColor, matDb.byName.MAT_chainlink.blendColor); gl.uniform1f(fxLocs.uCutoff, 0.35);`. Trail pass — same shape with MAT_swordtrail and uCutoff 0.0. DELETE the hardcoded `gl.uniform1f(fxLocs.uAdd, …)`, `gl.enable(gl.BLEND)`, `gl.blendFunc(gl.SRC_ALPHA, gl.ONE)`, `gl.depthMask(false/true)`, `gl.disable(gl.BLEND)` lines. After both passes (before `gl.useProgram(prog)`): `Fx.restoreFxState(gl);`. Note the intentional visual change: the chain now renders with real usual alpha blending + depth-write ON (it previously had no blending at all) — this is the decoded MAT_chainlink state, not a bug. Remove every remaining `uAdd` occurrence in app.js including comments.

    Hooks (lines 821-825): extend window.KratosLab with `gl`, `fxLog` (an array app.js rewrites at the top of each drawFx call, pushing `{name, mode, depthWrite}` per applyMaterial'd pass — gives the checkpoint console-visible proof of per-pass state without mid-frame GL reads), and `fxState()` returning `{ alpha: gl.getContextAttributes().alpha, blendEnabled: gl.isEnabled(gl.BLEND), blendEquation: gl.getParameter(gl.BLEND_EQUATION_RGB), blendSrcRGB: gl.getParameter(gl.BLEND_SRC_RGB), blendDstRGB: gl.getParameter(gl.BLEND_DST_RGB), depthMask: gl.getParameter(gl.DEPTH_WRITEMASK) }` (sampled between frames it proves the restore discipline).
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/wad.test.js && grep -q "alpha: false" tools/kratos-lab/app.js && ! grep -q "uAdd" tools/kratos-lab/app.js && grep -q "Fx.applyMaterial" tools/kratos-lab/app.js && grep -q "restoreFxState" tools/kratos-lab/app.js && grep -q "uCutoff" tools/kratos-lab/app.js && grep -q "clearColor(0, 0, 0, 1)" tools/kratos-lab/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getContext("webgl"' tools/kratos-lab/app.js` returns 1 (single webgl creation site; the two 2D-canvas getContext calls at lines 85/258 are unrelated and must not be touched) and that call contains `alpha: false`
    - Zero occurrences of `uAdd`, `gl.blendFunc(gl.SRC_ALPHA, gl.ONE)` and standalone `gl.depthMask(false)` remain in app.js drawFx (state now flows only through Fx.applyMaterial/restoreFxState)
    - The FX fragment shader multiplies `uLayerColor` and `uMaterialColor` before output (grep both identifiers in app.js)
    - drawFx passes `matDb.byName.MAT_chainlink` and `matDb.byName.MAT_swordtrail` to Fx.applyMaterial (grep both names)
    - window.KratosLab exposes gl, fxLog, and fxState (grep all three)
  </acceptance_criteria>
  <done>Every FX draw takes its blend/depth state from decoded MAT data through the single mapping table; the compositing invariants (opaque canvas, MODULATE math, state restore) are in code, ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies live blend state, magenta test, and saturation</name>
  <files>none (verification only — browser session against the running lab)</files>
  <read_first>
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Pattern 5 verification list — magenta test, Spector.js expectations, saturation behavior)
  </read_first>
  <what-built>Data-driven FX blend state (chain = usual/depth-write-ON from MAT_chainlink, trail = additive/depth-write-OFF from MAT_swordtrail), alpha:false opaque canvas, WAD tuple inventory in the stats card, GL-state restore each frame.</what-built>
  <action>
    Present the how-to-verify steps below to the human and wait. These checks are manual-only because they inspect perceptual output (tint, saturation) and mid-frame GPU state that automated hooks cannot sample (RESEARCH Validation Architecture: DEC-01 live draws + REND-01 magenta test are checkpoint:human-verify).
  </action>
  <how-to-verify>
    1. Run `node tools/kratos-lab/server.js`, open http://localhost:4173/tools/kratos-lab/ — the lab must load with no console errors and the status line reaching "ready".
    2. Stats card (left panel): confirm the line `blend tuples: 2 (usual/dw-on ×18, additive/dw-off ×6)` and a console.table of the two tuples in DevTools.
    3. DevTools console: `KratosLab.gl.getContextAttributes().alpha` → must be `false`. `KratosLab.fxState()` → blendEnabled `false`, depthMask `true`, blendEquation `32774` (FUNC_ADD) — proves per-frame restore.
    4. Press J a few times (combo3A chain). During the swing run `KratosLab.fxLog` → two entries: `{name:"MAT_chainlink", mode:"usual", depthWrite:true}` and `{name:"MAT_swordtrail", mode:"additive", depthWrite:false}`.
    5. Magenta test: console `document.body.style.background="magenta"` — the render (chain, trail, hero) must NOT change tint at all. Reset with `document.body.style.background=""`.
    6. Saturation: mash J/K for overlapping swings — where trail layers stack, color must clip toward flat white (no gray haze, no glow halo beyond the texture).
    7. Chain read: the chain ribbon should now show alpha-blended link edges and correctly occlude/be occluded by the hero (depth-write ON). Optional: Spector.js frame capture — chain draw shows blendFuncSeparate(770, 771, 1, 1) + depthMask true; trail shows (770, 1, 1, 1) + depthMask false.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms every numbered step above, or reports the deviating step verbatim
    - Steps 3-4 console outputs match the stated values exactly
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what deviates (which step, what you saw)</resume-signal>
  <done>A human has confirmed the decoded blend state is live per-pass, the canvas is opaque, and additive stacking saturates authentically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decoded MAT data → GL state | Unknown/garbage mode strings must never silently become a default blend |
| FX pass state → rest of frame | Leaked blend/depthMask state corrupts subsequent passes and clears |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Tampering (silent mistranslation) | Fx.applyMaterial | mitigate | Throw-on-unknown mode with material name (DEC-01 contract); mock-gl test asserts the throw; "strange" has no GL path by design |
| T-2-06 | DoS (render corruption) | GL state leaks between passes | mitigate | applyMaterial sets FULL state per pass; Fx.restoreFxState after the FX block; fxState() hook makes restore console-assertable |
| T-2-07 | Information Disclosure | public-build fail-safe | accept | No fetch-path changes this plan; regex already widened in 02-01; error messages carry record names only (no paths beyond repo-relative) |
| T-2-SC | Tampering | package installs | accept (n/a) | Zero installs; mock gl is hand-rolled with node:assert |
</threat_model>

<verification>
- Both Node suites green: `node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js`
- All checkpoint steps confirmed by human (magenta test, fxLog modes, saturation)
- `git status --porcelain` shows only files in files_modified
</verification>

<success_criteria>
- Phase success criterion 2 fully satisfied: existing chain/trail draws use their MAT's real blend/depth state via the single MAT→GL table; unknown tuples assert
- Phase success criterion 3 satisfied: additive stacks saturate flat white in clamped LDR gamma space, canvas alpha:false, no bloom/tonemap/soft-particles anywhere
- The former uAdd guess is gone; trail state is now data-confirmed, chain state is corrected to usual/depth-write-ON
</success_criteria>

<output>
Create `.planning/phases/02-wad-mat-decode-render-pass-foundation/02-02-SUMMARY.md` when done.
</output>
