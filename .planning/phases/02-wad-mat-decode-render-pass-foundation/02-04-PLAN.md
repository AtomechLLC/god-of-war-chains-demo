---
phase: 02-wad-mat-decode-render-pass-foundation
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - tools/kratos-lab/app.js
  - tools/kratos-lab/index.html
autonomous: false
requirements: [REND-03]

must_haves:
  truths:
    - "Pressing N toggles rendering into a 512×448 RGBA/UNSIGNED_BYTE target displayed at 4:3 with bilinear upscale (letterboxed); default is OFF (full-res inspect)"
    - "FBO completeness is asserted at startup — an incomplete framebuffer throws a named error instead of rendering black silently"
    - "The sim-step counter reads 60±1 steps per second with the toggle on or off, on any display"
    - "The blit uses its own trivial program — FX/hero program state is not coupled to the upscale pass"
  artifacts:
    - path: "tools/kratos-lab/app.js"
      provides: "512×448 FBO + depth renderbuffer + completeness assert, blit program, N keybind, setNativeRes/isNativeRes hooks"
      contains: "DEPTH_COMPONENT16"
    - path: "tools/kratos-lab/index.html"
      provides: "Footer hint for the N keybind"
      contains: "native"
  key_links:
    - from: "tools/kratos-lab/app.js renderFrame"
      to: "native-res FBO"
      via: "bindFramebuffer + viewport(0,0,512,448) when toggled on, then blit quad to canvas"
      pattern: "bindFramebuffer"
    - from: "keydown handler"
      to: "setNativeRes"
      via: "key 'n' toggles the target"
      pattern: "setNativeRes"
---

<objective>
Add the native-res (512×448) render-target toggle: render all passes into an offscreen FBO at the GS's storage resolution and blit it to the canvas stretched to 4:3 with bilinear filtering — the authentic soft look Phase 7's side-by-side comparison requires. Close the phase with the human checkpoint verifying the toggle, the 60Hz sim counter, and that earlier invariants still hold.

Purpose: REND-03's second half — the toggle must exist before formal footage comparison begins. 4:3 display of the 512×448 target is the default per RESEARCH open question 1 (revisit when Phase 1's stills land; the aspect is one constant).
Output: FBO + blit pass + N keybind + hooks in app.js, footer hint, phase-closing human verification.
</objective>

## Phase Goal

**As a** solo developer building kratos-lab toward footage-grade fidelity, **I want to** drive every FX draw from its material's real decoded blend/depth state with the PS2-authentic rendering conventions locked, **so that** no visual tuning ever happens on top of approximated render state.

(Restated 1:1 from the ROADMAP Phase 2 Goal; nothing added.)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md
@.planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md
@tools/kratos-lab/app.js
</context>

<interfaces>
<!-- Contract this plan creates (Phase 7's comparison harness consumes the toggle): -->
```javascript
// app.js — native-res target
const NATIVE = { w: 512, h: 448, displayAspect: 4 / 3 };  // 4:3 stretched display of the
// 512×448 GS target (RESEARCH A2 / Open Q1 — NTSC non-square pixels). 8:7 raw option =
// change displayAspect to 8/7; revisit when Phase-1 capture stills land. Do not block on it.
window.KratosLab.setNativeRes(on /* boolean */)   // toggle programmatically
window.KratosLab.isNativeRes()                    // -> boolean, default false
// keybind: "n" toggles + status() line feedback
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 512×448 FBO, blit program, N-key toggle</name>
  <files>tools/kratos-lab/app.js, tools/kratos-lab/index.html</files>
  <read_first>
    - tools/kratos-lab/app.js (renderFrame — the head established by plan 02-03 where the FBO bind wraps: resize/viewport/clear and the `M.persp(0.9, w / h, 0.05, 50)` projection; shader()/createProgram helpers at lines 131-141 to reuse for the blit program; makeTex at lines 267-276 — the texture-parameter analog, FBO texture differs only in CLAMP on both axes + null upload; keydown handler around line 773 for the N key; KratosLab hook object)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Pattern 7 — the FBO setup to lift verbatim incl. the completeness assert and WebGL1 NPOT notes for the 448 dimension; §Pitfall 6 — FBO vs screenshot/verify workflows, toggle default-off, restore viewport after blit)
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-PATTERNS.md (section "app.js modification sites" items 4-5 — where the wrap goes and the texture-state analog)
  </read_first>
  <action>
    FBO setup (once, after program creation): lift RESEARCH Pattern 7 verbatim — 512×448 texture, `gl.RGBA`/`gl.UNSIGNED_BYTE` (8-bit clamped like the canvas — REND-01's saturation must survive; NO float formats), LINEAR min+mag (bilinear upscale = the authentic soft look), CLAMP_TO_EDGE both axes (448 is NPOT on T — fine for an FBO texture with clamp + no mips), DEPTH_COMPONENT16 renderbuffer, framebufferTexture2D + framebufferRenderbuffer, then `if (gl.checkFramebufferStatus(gl.FRAMEBUFFER) !== gl.FRAMEBUFFER_COMPLETE) throw new Error("native-res FBO incomplete");` and rebind the null framebuffer.

    Blit program: its OWN trivial program via the existing shader() helper (do not reuse fxProg — state-coupling risk per RESEARCH Pattern 7): vertex `attribute vec2 aPos; uniform vec2 uScale; varying vec2 vUV; void main() { gl_Position = vec4(aPos * uScale, 0.0, 1.0); vUV = aPos * 0.5 + 0.5; }`, fragment sampling `uTex` at vUV. One static buffer holding a fullscreen two-triangle quad (-1..1).

    renderFrame wrap: `let nativeRes = false;` (default OFF — full-res inspect until Phase 7 comparisons). Declare `const NATIVE = { w: 512, h: 448, displayAspect: 4 / 3 };` with the comment from the interfaces block (A2/Open-Q1 provenance, 8:7 option). When on: bind the FBO, `gl.viewport(0, 0, NATIVE.w, NATIVE.h)`, clear, build the projection with `M.persp(0.9, NATIVE.displayAspect, 0.05, 50)` (4:3-intent non-square pixels — NOT 512/448); render all passes as normal. Then: bind null framebuffer, `gl.viewport(0, 0, w, h)`, clear (opaque black gives the letterbox bars), `gl.disable(gl.DEPTH_TEST)`, draw the blit quad with letterbox uScale — `const ca = w / h;` then uScale `[NATIVE.displayAspect / ca, 1]` when ca > displayAspect else `[1, ca / NATIVE.displayAspect]` — sampling the FBO texture on unit 0; then `gl.enable(gl.DEPTH_TEST)` and rebind the hero program. When off: the existing full-res path, byte-for-byte untouched. Per RESEARCH Pitfall 6: with the toggle on, the canvas (and preserveDrawingBuffer screenshots) contain the blit result — that IS the output; any readPixels check reads after blit.

    Toggle: in the existing keydown handler add `if (k === "n") { nativeRes = !nativeRes; status(nativeRes ? "native res ON — 512×448 → 4:3 (bilinear)" : "native res OFF — full canvas res"); }`. Hooks: add `setNativeRes(on)` (sets the flag + same status line) and `isNativeRes()` to window.KratosLab.

    index.html: append a short footer hint `press N — native-res 512×448 toggle`; bump all script tags to ?v=20.
  </action>
  <verify>
    <automated>node tools/kratos-lab/test/wad.test.js && node tools/kratos-lab/test/fx.test.js && node tools/kratos-lab/test/loop.test.js && grep -q "DEPTH_COMPONENT16" tools/kratos-lab/app.js && grep -q "FRAMEBUFFER_COMPLETE" tools/kratos-lab/app.js && grep -q "setNativeRes" tools/kratos-lab/app.js && grep -q "displayAspect" tools/kratos-lab/app.js && grep -qi "native" tools/kratos-lab/index.html</automated>
  </verify>
  <acceptance_criteria>
    - app.js creates the FBO with gl.RGBA + gl.UNSIGNED_BYTE and a DEPTH_COMPONENT16 renderbuffer, and throws "native-res FBO incomplete" on a failed completeness check (grep the literal strings)
    - The native-pass projection uses NATIVE.displayAspect (4/3), not 512/448 (grep shows `NATIVE.displayAspect` inside the persp call and no `512 / 448` anywhere)
    - The blit has its own program (a new createProgram call — not fxProg reuse for the quad)
    - Default state is OFF: `nativeRes = false` at declaration; KratosLab.isNativeRes() exists
    - index.html footer mentions the N keybind; all script tags at ?v=20
    - All three Node suites still green (regression)
  </acceptance_criteria>
  <done>The native-res toggle renders 512×448 with authentic bilinear 4:3 upscale behind one keybind and one hook, defaulting to full-res inspect mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies native-res toggle, 60Hz counter, and phase regressions</name>
  <files>none (verification only — browser session against the running lab)</files>
  <read_first>
    - .planning/phases/02-wad-mat-decode-render-pass-foundation/02-RESEARCH.md (§Validation Architecture REND-03 rows — the toggle and accumulator checks; §Pitfall 6 — what the canvas contains with the toggle on)
  </read_first>
  <what-built>The complete Phase 2 foundation: WAD-decoded MAT state on every FX draw (plans 01-02), fixed 60Hz sim accumulator (plan 03), and the native-res 512×448 → 4:3 toggle (this plan).</what-built>
  <action>
    Present the how-to-verify steps below to the human and wait. Manual-only because the checks are perceptual (bilinear softness, letterbox framing) and wall-clock (steps-per-second on the human's actual display refresh rate).
  </action>
  <how-to-verify>
    1. Run `node tools/kratos-lab/server.js`, open http://localhost:4173/tools/kratos-lab/ — loads clean, status reaches "ready", no console errors (the FBO completeness assert passed at startup).
    2. 60Hz proof: in DevTools console run `const a = KratosLab.simStepCount; setTimeout(() => console.log((KratosLab.simStepCount - a) / 5), 5000)` → prints 60 ± 1 (valid regardless of your monitor's refresh rate).
    3. Press N: the image visibly softens (bilinear 512×448 upscale) and letterboxes/pillarboxes to a 4:3 frame; status line reads "native res ON — 512×448 → 4:3 (bilinear)". Trigger a combo (J J J): chain, trail, and hero all render correctly inside the native target.
    4. Press N again: crisp full-resolution rendering returns, status reads OFF.
    5. Regression spot-checks: `KratosLab.isNativeRes()` → false after step 4; `KratosLab.gl.getContextAttributes().alpha` → still false; `KratosLab.fxLog` during a swing still shows MAT_chainlink usual + MAT_swordtrail additive; the timeline "frame N / M @30fps" readout still tracks clip time during a move.
    6. Optional: with native ON, a readPixels sanity check from the console (any pixel where the hero is must be non-black after a frame) — confirms the blit path, not a black FBO.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms every numbered step, or reports the deviating step verbatim
    - Step 2 prints 60 ± 1; step 3 shows visible softening plus the exact status line text
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 2 execution, or describe what deviates (which step, what you saw)</resume-signal>
  <done>A human has confirmed the native-res toggle, the 60Hz sim rate on real hardware, and that all earlier phase invariants still hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offscreen FBO → canvas | Silent FBO failure would render black and hide every earlier invariant |
| server.js file serving | Unchanged this phase — path-traversal guard must stay as-is |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-10 | DoS (silent black render) | FBO setup | mitigate | checkFramebufferStatus assert throws "native-res FBO incomplete" at startup; toggle defaults OFF so the lab never boots into a broken target |
| T-2-11 | Tampering (state coupling) | blit pass | mitigate | Dedicated blit program; DEPTH_TEST disabled/re-enabled around the quad; viewport restored to canvas size; full-res path untouched when toggled off |
| T-2-12 | Elevation/Path traversal | server.js static serving | accept | No server changes in this phase; existing `filePath.startsWith(ROOT)` guard remains; localhost-only dev tool |
| T-2-SC | Tampering | package installs | accept (n/a) | Zero installs anywhere in Phase 2 |
</threat_model>

<verification>
- Full Node suite green: `for f in tools/kratos-lab/test/*.test.js; do node "$f" || exit 1; done`
- All checkpoint steps confirmed by human (60±1 counter, native softness, 4:3 letterbox, regressions)
- `git status --porcelain` shows only app.js and index.html
</verification>

<success_criteria>
- Phase success criterion 4 fully satisfied: fixed 60Hz accumulator decoupled from rAF (proven in-browser via simStepCount) and a working native-res 512×448-class render-target toggle
- All four phase success criteria verified — criteria 1-2 via plan 02-01/02-02 artifacts and the fxLog/stats-card checks, criterion 3 via the alpha:false + saturation checks re-confirmed at this checkpoint
- Phase 2's locked conventions (blend table, 0x80 rules, opaque canvas, 60Hz tick, native-res toggle) are all in code before any Phase 3+ visual work
</success_criteria>

<output>
Create `.planning/phases/02-wad-mat-decode-render-pass-foundation/02-04-SUMMARY.md` when done.
</output>
