---
phase: quick
plan: 260924-pl8
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true
requirements: [QUICK-260924-PL8]

must_haves:
  truths:
    - "The GoW3 disc folder `God of War III (USA, Canada) (v02.00)/` can never be staged — git check-ignore matches every path inside it"
    - "`git status` no longer lists the GoW3 folder as untracked"
    - "No currently tracked file becomes ignored (no over-broad pattern regression)"
  artifacts:
    - path: ".gitignore"
      provides: "GoW3 disc folder ignore rule inside the existing 'Game disc contents' never-commit block, plus a future-proof extracted-gow3/ local-only rule"
      contains: "God of War III (USA, Canada) (v02.00)/"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git check-ignore pattern match on the GoW3 folder"
      pattern: "God of War III \\(USA, Canada\\) \\(v02\\.00\\)/"
---

<objective>
Add the newly present PS3 disc folder `God of War III (USA, Canada) (v02.00)/` to `.gitignore` under the existing "Game disc contents" never-commit block, so multi-GB copyrighted disc data can never be staged or pushed. Also add a future-proof `extracted-gow3/` local-only rule mirroring the existing `extracted/` pattern.

Purpose: The repo's never-commit disc policy (`.gitignore` lines 1-7) predates this folder — it covers the PS2 disc (`God of War (USA)/`, `*.iso`, `ps2bios/`) but not the GoW3 PS3 disc contents now sitting untracked in the working tree (`PS3_GAME/`, `PS3_DISC.SFB`, a `.dec.iso`). One accidental `git add .` would stage gigabytes of copyrighted game data.

Output: Updated `.gitignore` with the GoW3 folder unignorable-proofed and an `extracted-gow3/**` rule ready for future extraction work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.gitignore
@CLAUDE.md

Facts the executor should rely on (verified during planning, no exploration needed):
- Current `.gitignore` "Game disc contents" block spans lines 1-7: comment (lines 1-3), then `God of War (USA).iso`, `God of War (USA)/`, `ps2bios/`, `*.iso`.
- `*.iso` (line 7) ALREADY matches the folder's `*.dec.iso` file — gitignore `*` matches any filename ending in `.iso` at any depth. No new extension rule is needed; the verify step confirms this.
- gitignore patterns treat parentheses, commas, and interior spaces literally — no escaping needed for `God of War III (USA, Canada) (v02.00)/`. Only trailing whitespace would be a problem; do not add any.
- The existing local-only extraction pattern to mirror is lines 15-16: `extracted/**` + `!extracted/README.md`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GoW3 disc folder + extracted-gow3 rules to the never-commit block</name>
  <files>.gitignore</files>
  <action>
    Edit `.gitignore` with two additions, preserving all existing lines and comments byte-for-byte:

    1. In the "Game disc contents" block (after line 6 `ps2bios/`, before or after line 7 `*.iso` — keep disc folders grouped together, so insert directly below `God of War (USA)/`), add exactly:
       `God of War III (USA, Canada) (v02.00)/`
       The trailing `/` scopes it to the directory; the existing block comment ("NEVER push game data... no exception exists or will be added") already documents the policy — do NOT rewrite or duplicate that comment. Do not add a `.dec.iso` rule: `*.iso` on line 7 already matches it (confirm in verify, not by adding a redundant pattern).

    2. Below the existing `extracted/**` / `!extracted/README.md` pair (lines 15-16), add a mirrored future-proof rule for GoW3 extraction work:
       `extracted-gow3/**`
       `!extracted-gow3/README.md`
       This keeps a future local-only GoW3 extraction dir unstageable from day one while allowing a tracked README, exactly like the existing `extracted/` convention (assets/README.md curated-subset policy).

    Constraints: no trailing whitespace on new lines; do not reorder or reflow existing entries; do not touch `node_modules/` or the reference-captures block.
  </action>
  <verify>
    <automated>git check-ignore "God of War III (USA, Canada) (v02.00)/PS3_DISC.SFB" && git check-ignore "God of War III (USA, Canada) (v02.00)/PS3_GAME/USRDIR/EBOOT.BIN" && git check-ignore "God of War III (USA, Canada) (v02.00)/God of War III (USA, Canada) (v02.00).dec.iso" && git check-ignore "extracted-gow3/part1.wad" && ! git check-ignore "extracted-gow3/README.md" && test -z "$(git ls-files -ci --exclude-standard)" && ! git status --porcelain | grep -F "God of War III"</automated>
  </verify>
  <done>
    - Every path inside `God of War III (USA, Canada) (v02.00)/` is matched by check-ignore (folder rule) and the `.dec.iso` is matched (via existing `*.iso`)
    - `git status --porcelain` no longer lists the GoW3 folder
    - `git ls-files -ci --exclude-standard` is empty — zero tracked files became ignored (regression guard)
    - `extracted-gow3/README.md` is NOT ignored (negation works, mirroring `extracted/`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public remote | copyrighted multi-GB PS3 disc data must never cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Information Disclosure | git index / public remote | mitigate | directory-scoped ignore rule inside the never-commit block; check-ignore + porcelain verify proves unstageable |
| T-quick-02 | Tampering (over-broad pattern breaking tracked assets/) | .gitignore | mitigate | `git ls-files -ci --exclude-standard` must be empty — proves no tracked file (e.g., assets/, extracted/README.md) became ignored |
</threat_model>

<verification>
Single automated compound check in Task 1 covers: ignore coverage (3 representative paths incl. the `.dec.iso`), extracted-gow3 rule + README negation, tracked-file regression guard, and clean `git status`.
</verification>

<success_criteria>
- `.gitignore` contains `God of War III (USA, Canada) (v02.00)/` inside the existing "Game disc contents" comment block (comment unmodified)
- `.gitignore` contains `extracted-gow3/**` + `!extracted-gow3/README.md` mirroring the `extracted/` convention
- Task 1 automated verify command exits 0
- No other `.gitignore` lines changed
</success_criteria>

<output>
Create `.planning/quick/260924-pl8-add-god-of-war-iii-disc-folder-to-gitign/260924-pl8-SUMMARY.md` when done
</output>
